# iam-jit declarative config — generated by `iam-jit init`.
# Apply with:  iam-jit doctor apply-config --config /var/lib/iam-jit/iam-jit.yaml
# Edit at your own risk; this file is operator-owned.
#
# shape:              local-solo
# mode:               discovery
# bouncers:           ibounce
# harness:            claude-code
# accounts_detected:  (none — edit accounts.yaml)
iam-jit:
  schema_version: '1.0'
  enabled: true
  posture: ambient
  bouncers:
    ibounce:
      enabled: true
      mode: discovery
