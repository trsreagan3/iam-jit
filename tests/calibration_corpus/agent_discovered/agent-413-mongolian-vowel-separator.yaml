name: agent-413-mongolian-vowel-separator
description: |
  WHITE-BOX round 7: same class as agent-412 — MONGOLIAN VOWEL
  SEPARATOR (`U+180E`) is a Cf-category invisible character, not in
  `_ZERO_WIDTH_CHARS`. NFKC preserves it.

  Action `iam<U+180E>:CreateAccessKey` lower()s to a string that
  contains the U+180E byte and is NOT in `_CATASTROPHIC_ACTIONS_LC`.

  Source-code rule exploited: `_norm_grammar_str` /
  `_ZERO_WIDTH_CHARS`.

  Research-doc pattern: §13.

  Real risk: 9 — catastrophic action bypass via Cf character.

  Det score: 4 — same as agent-412.

  Proposed fix: same as agent-412 — Cf-category strip.

policy:
  Version: "2012-10-17"
  Statement:
    - Effect: Allow
      Action: "iam᠎:CreateAccessKey"
      Resource: "*"

request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1

expected:
  score_min: 8
  must_auto_approve: false
