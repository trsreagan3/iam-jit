# Agent-discovered scorer blind spots

Each finding below is a real attack policy the deterministic scorer
ranks ≥2 below a thoughtful security reviewer's estimate. The
corresponding YAML files in this directory pin the gap as test
failures; closing them requires changes in src/iam_jit/review.py.

Severity ordering: highest-impact / largest-gap first.

## agent-01-s3-cross-bucket-prefix-wildcard

- Policy summary: s3:GetObject,DeleteObject on arn:aws:s3:::*/sensitive-data/* — cross-bucket wildcard hitting every bucket's /sensitive-data/* path.
- Det score: 3 (just narrow-write floor)
- Real risk: 8
- Why scorer misses it: _is_broad_resource requires resource_spec.count('/') <= 1 to trip the bucket-level wildcard branch. Cross-bucket patterns with deeper paths (`*/path/*`, 2+ slashes) escape the check entirely and look "narrow."
- Suggested scorer fix: Treat an ARN as broad if the bucket-name portion (between `:::` and the first `/`) contains `*`, regardless of trailing slash count. Same fix applies to all S3 ARNs.

## agent-02-s3-bucket-name-prefix-wildcard

- Policy summary: s3:GetObject,ListBucket on arn:aws:s3:::prod-* — bucket-NAME-prefix wildcard matching every prod--named bucket.
- Det score: 1
- Real risk: 7
- Why scorer misses it: _is_broad_resource only catches bucket-level wildcards of the form `bucket/*` (one slash, ends in `/*`). A bucket-name wildcard with no slash (`prod-*`) doesn't end in `/*`, so neither _is_broad_resource nor _is_strict_wildcard fires.
- Suggested scorer fix: For S3 ARNs, detect `*` anywhere in the bucket-name component (the segment after `:::` up to the first `/`). Same as agent-01.

## agent-32-s3-bucket-name-prefix-objects

- Policy summary: s3:GetObject on arn:aws:s3:::prod-*/* — every object in every prod-* bucket.
- Det score: 1
- Real risk: 7
- Why scorer misses it: _is_broad_resource returns True (good!) but _is_strict_wildcard returns False because the resource_spec is `prod-*/*` not `*` or `<type>:*`. The cross-resource-read flag and high-risk-action-on-wildcard flag both gate on wildcard_resource (strict). For Read actions, broad_blast_resource is never consulted (those rules only check destructive verbs and Write/Permissions/Tagging).
- Suggested scorer fix: Extend the strict-wildcard predicate to recognize bucket-name-prefix patterns. OR consult broad_blast_resource for the cross-resource-read rule when the action is a high-risk content read.

## agent-28-notaction-narrow-resource

- Policy summary: NotAction: [iam:*, kms:*, organizations:*] on Resource: arn:aws:s3:::company-data/* — admin-minus-set within one S3 path.
- Det score: 1
- Real risk: 7
- Why scorer misses it: The NotAction rule only fires when the resource set contains `*` or ends in `:*` (lines 502-503). A narrow Resource ARN escapes the check, even though the action set is "every action in every service minus a tiny exclusion list." Effectively the same as `s3:*` (and arguably broader, because it includes services like cognito/ses/lambda).
- Suggested scorer fix: Remove the resource-wildcard gate. NotAction itself is the footgun — flag it whenever the exclusion list is small enough that the residual is still wide. A reasonable rule: NotAction with ≤10 exclusions and an admin-y excluded set is always ≥7.

## agent-03-lambda-create-passrole-admin

- Policy summary: lambda:CreateFunction + InvokeFunction + UpdateFunctionCode on a narrow lambda ARN, plus iam:PassRole on a narrow admin-role ARN.
- Det score: 5
- Real risk: 9
- Why scorer misses it: The scorer fires the PassRole-narrow floor (4) and the Lambda high-impact-mutation floor (5), then takes max. It never composes "I can create code" + "I can pass a role to it" = "RCE as that role."
- Suggested scorer fix: Add a "code-execution-via-role" composition check: if any statement contains an action from {lambda:CreateFunction, lambda:UpdateFunctionCode, ec2:RunInstances, ecs:RunTask, codebuild:StartBuild, glue:StartJobRun, sagemaker:CreateNotebookInstance, states:CreateStateMachine, cloudformation:CreateStack/UpdateStack, apprunner:CreateService, batch:SubmitJob} AND any statement contains iam:PassRole (any resource), floor at 8 (or 9 if the PassRole resource is a wildcard).

## agent-04-cfn-create-passrole-admin

- Policy summary: CloudFormation Create/Update/ChangeSet on a narrow stack ARN, plus iam:PassRole on a narrow admin role.
- Det score: 5
- Real risk: 9
- Why scorer misses it: Same as agent-03 — no composition recognition. CFN provisions every resource in the template under the passed role, which is full account compromise in one API call.
- Suggested scorer fix: Same composition rule as agent-03.

## agent-08-ec2-runinstances-passrole-admin

- Policy summary: ec2:RunInstances + CreateLaunchTemplate + CreateTags on Resource:* with iam:PassRole on a narrow admin instance role.
- Det score: 6
- Real risk: 9
- Why scorer misses it: EC2 RunInstances is just a generic Write action on a non-sensitive service — no special handling for "boot an instance with an admin instance profile then harvest creds via 169.254.169.254."
- Suggested scorer fix: Same composition rule as agent-03. ec2:RunInstances is the most classic instance-profile escalation primitive.

## agent-27-cfn-stackset-org-wide

- Policy summary: cloudformation:CreateStackSet + CreateStackInstances + UpdateStackSet on Resource:* — deploys arbitrary infrastructure to every member account in the org.
- Det score: 6
- Real risk: 9
- Why scorer misses it: StackSet variants aren't in _HIGH_IMPACT_MUTATION_ACTIONS (only single-stack CFN actions are). StackSets have org-wide blast radius — much larger than CreateStack.
- Suggested scorer fix: Add cloudformation:CreateStackSet, CreateStackInstances, UpdateStackSet, UpdateStackInstances to _CATASTROPHIC_ACTIONS (org-wide blast).

## agent-30-iam-recon-then-assume

- Policy summary: Statement 1 = iam:Get/ListRole* on Resource:*. Statement 2 = sts:AssumeRole on arn:aws:iam::*:role/*.
- Det score: 6
- Real risk: 9
- Why scorer misses it: Two issues — (a) sts:AssumeRole on arn:aws:iam::*:role/* doesn't trip _is_strict_wildcard because resource_spec is `role/*` (not `<type>:*`). So the HIGH_RISK_ACTIONS-on-wildcard 7-floor never fires. (b) No composition recognition: "enumerate roles + assume any of them" is the textbook lateral movement, but each statement scores on its own.
- Suggested scorer fix: (a) Treat `role/*` and `user/*` resource_specs (IAM-ARN trailing-wildcard forms) as strict wildcards in _is_strict_wildcard. (b) Add composition rule: iam-read-broad + sts:AssumeRole-on-wildcard-role = lateral-movement primitive, floor 8+.

## agent-31-orgs-accept-handshake

- Policy summary: organizations:AcceptHandshake, InviteAccountToOrganization, RegisterDelegatedAdministrator on Resource:*.
- Det score: 6
- Real risk: 9
- Why scorer misses it: AcceptHandshake is irreversible (account becomes member of another org) but isn't in _CATASTROPHIC_ACTIONS. Only organizations:LeaveOrganization and organizations:CreateAccount/MoveAccount are.
- Suggested scorer fix: Add organizations:AcceptHandshake, organizations:InviteAccountToOrganization, organizations:RegisterDelegatedAdministrator, organizations:DeregisterDelegatedAdministrator, organizations:EnableAWSServiceAccess, organizations:DisableAWSServiceAccess to _CATASTROPHIC_ACTIONS.

## agent-09-ec2-getconsoleoutput-secret-leak

- Policy summary: ec2:GetConsoleOutput, GetConsoleScreenshot, GetLaunchTemplateData, DescribeInstanceAttribute on Resource:* — read-only.
- Det score: 4
- Real risk: 7
- Why scorer misses it: All four are IAM-class Read on a non-sensitive service. The scorer only flags the broad-cross-resource-read flag (4). In practice these read boot logs and user-data scripts which routinely contain cleartext secrets (AWS keys, DB passwords, API tokens) printed by cloud-init.
- Suggested scorer fix: Add a new class _SECRET_BEARING_READS for IAM-Read actions that read content commonly containing secrets: ec2:GetConsoleOutput, ec2:GetConsoleScreenshot, ec2:GetLaunchTemplateData, ec2:GetPasswordData, ssm:GetCommandInvocation, ssm:GetParameterHistory, logs:GetLogEvents, logs:FilterLogEvents. Floor at 7 on Resource:*.

## agent-10-ebs-direct-snapshot-read

- Policy summary: ebs:ListChangedBlocks, ListSnapshotBlocks, GetSnapshotBlock on Resource:* — read-only.
- Det score: 4
- Real risk: 8
- Why scorer misses it: `ebs` (EBS direct API) is not in _SENSITIVE_SERVICES. GetSnapshotBlock reads RAW DISK BYTES out of any snapshot — bypasses the OS, filesystem, encryption-at-rest. Treated as plain reads.
- Suggested scorer fix: Add `ebs` to _SENSITIVE_SERVICES. OR add ebs:GetSnapshotBlock to _HIGH_RISK_ACTIONS (same tier as secretsmanager:GetSecretValue).

## agent-14-logs-filter-events-secret-exfil

- Policy summary: logs:FilterLogEvents, GetLogEvents, GetLogRecord, StartQuery on Resource:* — read-only.
- Det score: 4
- Real risk: 7
- Why scorer misses it: CloudWatch Logs not in sensitive services. App logs commonly leak secrets (stack traces with conn strings, debug-level auth headers, lambda envvar dumps, API-key-bearing request bodies).
- Suggested scorer fix: Same as agent-09 — add the logs reads to _SECRET_BEARING_READS.

## agent-15-logs-put-subscription-filter-exfil

- Policy summary: logs:PutSubscriptionFilter, PutDestination, PutDestinationPolicy on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: These look like generic Write actions but they install a PERSISTENT outbound stream of every log line to an attacker-controlled Kinesis/Lambda. Survives the grant expiring.
- Suggested scorer fix: Add these to _HIGH_IMPACT_MUTATION_ACTIONS with "persistence + exfil" semantics; floor at 7 on any resource scope.

## agent-16-sqs-receive-all-queues

- Policy summary: sqs:ReceiveMessage, ListQueues, GetQueueAttributes on Resource:* — read-only.
- Det score: 4
- Real risk: 7
- Why scorer misses it: SQS not in sensitive services; ReceiveMessage is IAM-class Read. But receiving DRAINS messages from prod consumers (data loss + DoS) AND queues frequently carry credentials, webhooks, PII.
- Suggested scorer fix: sqs:ReceiveMessage on Resource:* should floor at 7. Either add SQS to sensitive services or add sqs:ReceiveMessage to a new "destructive-read" list (read that also mutates state by removing the message).

## agent-17-dynamodb-stream-exfil

- Policy summary: dynamodb:GetRecords, GetShardIterator, DescribeStream, ListStreams on Resource:* — read-only.
- Det score: 4
- Real risk: 7
- Why scorer misses it: DynamoDB not in sensitive services; stream reads are IAM-class Read. But this is a silent CDC tap into every table — every write captured in real time. Persistent until stream closes (24-hour window per shard).
- Suggested scorer fix: Add dynamodb:GetRecords (and similar stream-read actions across kinesis, dynamodbstreams, kafka) to _HIGH_RISK_ACTIONS. The semantic is "I can silently observe every write to this datastore."

## agent-05-glue-job-rce-passrole

- Policy summary: glue:CreateJob, UpdateJob, StartJobRun on Resource:* + iam:PassRole on a narrow Glue role.
- Det score: 6
- Real risk: 8
- Why scorer misses it: Glue runs arbitrary Python/Scala under the job's IAM role. Same composition gap as agent-03.
- Suggested scorer fix: Add glue:CreateJob, glue:UpdateJob, glue:StartJobRun to the code-execution-via-role composition rule.

## agent-06-sagemaker-notebook-rce

- Policy summary: sagemaker:CreateNotebookInstance, CreatePresignedNotebookInstanceUrl, UpdateNotebookInstance on Resource:* + iam:PassRole.
- Det score: 6
- Real risk: 8
- Why scorer misses it: SageMaker not in sensitive services. Notebook instances give interactive Jupyter shells under the notebook role's privileges — RCE-as-role through a presigned URL.
- Suggested scorer fix: Add SageMaker notebook actions to the code-execution composition rule. Optionally add `sagemaker` to _SENSITIVE_SERVICES.

## agent-07-codebuild-create-rce

- Policy summary: codebuild:CreateProject, UpdateProject, StartBuild on Resource:* + iam:PassRole.
- Det score: 6
- Real risk: 8
- Why scorer misses it: codebuild:StartBuild IS in _HIGH_IMPACT_MUTATION_ACTIONS (floors at 5) but the composition with CreateProject + PassRole is RCE-as-role; same composition gap.
- Suggested scorer fix: Same as agent-03 — add codebuild project/build actions to the composition rule.

## agent-11-rds-snapshot-share-cross-account

- Policy summary: rds:ModifyDBSnapshotAttribute, ModifyDBClusterSnapshotAttribute, CopyDBSnapshot, CopyDBClusterSnapshot on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: ModifyDBSnapshotAttribute adds a `restore` grant for any AWS account number — full DB exfil via cross-account snapshot share. The scorer treats it as generic Permissions Management.
- Suggested scorer fix: Add rds:ModifyDBSnapshotAttribute, rds:ModifyDBClusterSnapshotAttribute, rds:CopyDBSnapshot, rds:CopyDBClusterSnapshot to a new "cross-account data exfil" list, floor 8.

## agent-12-ec2-modify-snapshot-share

- Policy summary: ec2:ModifySnapshotAttribute, ModifyImageAttribute on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: Same as agent-11 but for EBS/AMI. Share snapshot with attacker account, they mount and read.
- Suggested scorer fix: Same exfil-action list as agent-11; add ec2:ModifySnapshotAttribute, ec2:ModifyImageAttribute.

## agent-19-lambda-function-url-public

- Policy summary: lambda:CreateFunctionUrlConfig, UpdateFunctionUrlConfig, AddPermission on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: lambda:AddPermission IS in _HIGH_IMPACT_MUTATION_ACTIONS (floor 5) but CreateFunctionUrlConfig is not — it's the persistent unauthenticated public RCE primitive (auth_type=NONE).
- Suggested scorer fix: Add lambda:CreateFunctionUrlConfig, lambda:UpdateFunctionUrlConfig to _HIGH_IMPACT_MUTATION_ACTIONS; floor 7 on Resource:*.

## agent-20-cognito-admin-user-takeover

- Policy summary: cognito-idp:AdminCreateUser, AdminSetUserPassword, AdminUpdateUserAttributes, AdminAddUserToGroup, AdminConfirmSignUp on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: Cognito-idp not in sensitive services. The Admin* actions perform application-level account takeover for any app backed by Cognito (create user, set their password, promote to admin group).
- Suggested scorer fix: Add cognito-idp:Admin* actions (or `cognito-idp` as a service) to _HIGH_IMPACT_MUTATION_ACTIONS. The pattern of needing-admin-in-the-action-name is a useful signal.

## agent-21-ses-phish-from-prod-domain

- Policy summary: ses:SendEmail, SendRawEmail, SendBulkTemplatedEmail, SendBulkEmail on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: SES not in sensitive services. SendEmail on a verified prod domain produces SPF/DKIM/DMARC-passing email from noreply@bigco.com — authoritative phishing.
- Suggested scorer fix: Add ses:SendEmail and bulk variants to _HIGH_IMPACT_MUTATION_ACTIONS with a phishing-via-verified-identity semantic.

## agent-22-ssm-document-rce-persistence

- Policy summary: ssm:CreateDocument, UpdateDocument, UpdateDocumentDefaultVersion, ModifyDocumentPermission on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: SSM is in sensitive services so the broad-resource-touch floor fires at 6, but these specific actions install PERSISTENT malicious automation that runs whenever an operator triggers the named document. Plus cross-account sharing.
- Suggested scorer fix: Add ssm:CreateDocument, ssm:UpdateDocument, ssm:UpdateDocumentDefaultVersion, ssm:ModifyDocumentPermission to _HIGH_IMPACT_MUTATION_ACTIONS.

## agent-23-imagebuilder-recipe-persistent

- Policy summary: imagebuilder:CreateComponent, CreateImageRecipe, CreateImagePipeline, UpdateImagePipeline on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: Image Builder not in sensitive services. A malicious component baked into the recipe taints every future AMI — supply-chain persistence in the golden image pipeline.
- Suggested scorer fix: Add imagebuilder Create/Update actions to _HIGH_IMPACT_MUTATION_ACTIONS; this is supply-chain tier.

## agent-24-ecr-set-repository-policy

- Policy summary: ecr:SetRepositoryPolicy, PutRegistryPolicy on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: ECR repo policy lets the attacker open the registry to other accounts (push or pull). Push = supply-chain image hijack; pull = read prod binaries.
- Suggested scorer fix: Add ecr:SetRepositoryPolicy, ecr:PutRegistryPolicy to _HIGH_IMPACT_MUTATION_ACTIONS. Also pair with ecr:PutImage (which IS there) for the full attack.

## agent-25-acm-pca-issue-cert

- Policy summary: acm-pca:IssueCertificate, GetCertificate on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: ACM Private CA not in sensitive services. Issuing a cert under the org's private CA enables MITM of any internal service that trusts the CA.
- Suggested scorer fix: Add acm-pca:IssueCertificate to _HIGH_RISK_ACTIONS. Optionally add `acm-pca` to _SENSITIVE_SERVICES.

## agent-26-iam-instance-profile-bind

- Policy summary: iam:CreateInstanceProfile, AddRoleToInstanceProfile, RemoveRoleFromInstanceProfile on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: AddRoleToInstanceProfile is the missing step in the EC2 priv-esc chain. Floor fires at 6 (sensitive-service Resource:*) but should be higher — this is a direct precursor to admin-RCE.
- Suggested scorer fix: Add iam:AddRoleToInstanceProfile, iam:CreateInstanceProfile to _HIGH_IMPACT_MUTATION_ACTIONS; pair with ec2:RunInstances in the composition rule.

## agent-29-ec2-multi-wildcard-multi-region

- Policy summary: s3:GetObject, ListBucket on arn:aws-cn:s3:::* and arn:aws-us-gov:s3:::*.
- Det score: 4
- Real risk: 6
- Why scorer misses it: _is_broad_resource handles resource_spec == `*` correctly so this case partly fires (cross-resource-read flag). But the scorer treats it the same as any read-only-on-S3, missing that BOTH government and China partitions are listed (operator probably never deploys to either, so this is dead code unless the attacker has accounts there — which is a red flag). More importantly, partition-prefixed wildcards demonstrate the helper doesn't inspect partition; a future attacker could use this for govcloud accounts.
- Suggested scorer fix: Recognize non-aws partitions in ARNs as a signal (especially aws-cn and aws-us-gov in a non-govcloud account). The strict-wildcard checks should still apply to all partitions.

---

## Cross-cutting scorer recommendations

The blind spots above cluster into 8 fixable rule classes:

1. Composition rule for code-execution-via-role. Many findings (agent-03, 04, 05, 06, 07, 08) share the pattern "compute-resource creation + iam:PassRole = RCE as the passed role." A single composition rule across the policy (not per-statement) closes this whole class. Trigger actions: lambda:CreateFunction, lambda:UpdateFunctionCode, lambda:CreateFunctionUrlConfig, ec2:RunInstances, ecs:RunTask, ecs:StartTask, codebuild:CreateProject, codebuild:StartBuild, glue:CreateJob, glue:StartJobRun, sagemaker:CreateNotebookInstance, sagemaker:CreatePresignedNotebookInstanceUrl, states:CreateStateMachine, cloudformation:CreateStack, cloudformation:UpdateStack, cloudformation:CreateStackSet, apprunner:CreateService, batch:SubmitJob.

2. Sensitive-service additions. Add `ebs` (raw disk reads), `acm-pca` (cert authority), `cognito-idp` (app-level admin), `imagebuilder` (golden-image supply chain), `sagemaker` (RCE), `glue` (RCE) to _SENSITIVE_SERVICES. Each gets the floor-6-on-broad-resource treatment automatically.

3. _CATASTROPHIC_ACTIONS additions. Add organizations:AcceptHandshake, InviteAccountToOrganization, RegisterDelegatedAdministrator, EnableAWSServiceAccess, cloudformation:CreateStackSet, CreateStackInstances, UpdateStackSet.

4. New action class: _SECRET_BEARING_READS. IAM-class-Read actions that in practice read secret-bearing content. Floor at 7 on Resource:*. Members: ec2:GetConsoleOutput, ec2:GetConsoleScreenshot, ec2:GetLaunchTemplateData, ec2:GetPasswordData, ssm:GetCommandInvocation, ssm:GetParameterHistory, logs:GetLogEvents, logs:FilterLogEvents, logs:GetLogRecord, logs:StartQuery.

5. New action class: _CROSS_ACCOUNT_EXFIL_ACTIONS. Permissions-Management actions that add cross-account share grants to data-bearing resources. Floor 8. Members: rds:ModifyDBSnapshotAttribute, rds:ModifyDBClusterSnapshotAttribute, rds:CopyDBSnapshot, rds:CopyDBClusterSnapshot, ec2:ModifySnapshotAttribute, ec2:ModifyImageAttribute, ecr:SetRepositoryPolicy, ecr:PutRegistryPolicy, elasticfilesystem:PutFileSystemPolicy, backup:PutBackupVaultAccessPolicy.

6. New action class: _PERSISTENT_EXFIL_PRIMITIVES. Actions that install ongoing outbound data streams. Floor 8. Members: logs:PutSubscriptionFilter, logs:PutDestination, logs:PutDestinationPolicy, lambda:CreateFunctionUrlConfig, lambda:UpdateFunctionUrlConfig, events:PutPermission.

7. Precision-helper fixes (S3 / IAM ARNs).
   - _is_broad_resource and _is_strict_wildcard should treat any S3 ARN with `*` in the bucket-name component as broad/strict, regardless of trailing slashes (catches agent-01, agent-02, agent-32).
   - _is_strict_wildcard should additionally recognize IAM trailing-wildcard forms like arn:aws:iam::*:role/* and arn:aws:iam::*:user/* (catches agent-30 part (a)).
   - NotAction rule should fire regardless of resource scope when the exclusion set is small (catches agent-28).

8. Stream-read primitive class. dynamodb:GetRecords and similar event-stream reads silently observe every write — closer in semantics to secret read than to metadata. Add to _HIGH_RISK_ACTIONS (catches agent-17). Same treatment for sqs:ReceiveMessage — it's destructive-read (removes the message) on Resource:* (catches agent-16).


# ===========================================================
# ROUND 2 — adversarial agent, 2026-05-13
# ===========================================================
# Round 1 closed 30 blind spots; round 2 hunts what slipped through.
# Severity ordering: highest-impact / largest-gap first.
#
# NOTE: The case-insensitive findings (agent-33 / 34 / 36 / 37) were
# the largest blind spot uncovered in round 2 — a textbook RCE-as-admin
# policy collapsed to score 1 against the round-1 scorer simply by
# lower-casing action names. The maintainer applied a case-insensitivity
# patch to review.py during round 2 (lowercase-mirror sets for every
# action-membership check). The YAMLs here pin the regression so the
# patch can't be undone without breaking the tests.

## agent-33-case-insensitive-catastrophic-action

- Policy summary: iam:attachrolepolicy (lower-cased) on arn:aws:iam::*:role/admin-deploy — attach AdministratorAccess to any role.
- Det score: 1
- Real risk: 9
- Why scorer misses it: _CATASTROPHIC_ACTIONS uses exact-string membership against canonical-cased entries (`action in _CATASTROPHIC_ACTIONS`). AWS treats action names case-insensitively, but the scorer's check is case-sensitive. Same gap for _HIGH_IMPACT_MUTATION_ACTIONS, _HIGH_RISK_ACTIONS, _SECRET_BEARING_READS, _CROSS_ACCOUNT_EXFIL_ACTIONS, _CODE_EXECUTION_PRIMITIVES, _DECEPTIVE_WRITE_ACTIONS, the literal-string PassRole composition check, the literal-string sts:AssumeRole composition check, the iam_recon_actions composition set, and the _SENSITIVE_SERVICES service comparison.
- Suggested scorer fix: Normalize action strings to lowercase at the entry point of `_deterministic` (and the composition pass), OR build lowercase-mirror sets and compare `action.lower() in _SET_LC`. The latter preserves the original casing for display in factors strings. Same fix needs to apply to the literal-string compares for `iam:PassRole`, `sts:AssumeRole`, etc.

## agent-34-case-insensitive-code-exec-passrole

- Policy summary: Lambda:UpdateFunctionCode + Iam:PassRole (mixed casing) — RCE-as-admin via code update.
- Det score: 1
- Real risk: 9
- Why scorer misses it: Same root cause as agent-33. The composition rule checks `action_names & _CODE_EXECUTION_PRIMITIVES` and `"iam:PassRole" in action_names`; both fail on case-tampered actions. The catastrophic-action check, the high-impact-mutation check, and the high-risk-action-on-wildcard check ALL miss too. The scorer falls back to the "all statements scoped, no broad patterns" branch and returns 1 — the most innocuous possible score for the textbook RCE-as-admin attack.
- Suggested scorer fix: Same as agent-33. Demonstrates that the case-sensitivity gap collapses ALL the round-1 composition defences in a single attack.

## agent-36-case-insensitive-cloudtrail-stop

- Policy summary: cloudtrail:stoplogging + cloudtrail:deletetrail (lower-cased) on Resource:* — disable audit logging.
- Det score: 4
- Real risk: 9
- Why scorer misses it: _CATASTROPHIC_ACTIONS check is case-sensitive (misses both). The destructive-verb prefix matcher uses `startswith(("Delete", "Stop", ...))` — capital-only — so lowercase verbs `stoplogging`/`deletetrail` don't trip the destructive-on-broad rule either. Only the generic "broad cross-resource read/access" flag fires (4).
- Suggested scorer fix: Same as agent-33 (case-insensitive set membership). ALSO, the destructive-verb prefix matcher should compare against the action name's lowercase prefix (or use a case-insensitive prefix matcher).

## agent-37-case-insensitive-secret-bearing-reads

- Policy summary: LOGS:filterlogevents + ec2:getconsoleoutput etc. (mixed casing) — secret exfil from broad reads.
- Det score: 4
- Real risk: 7
- Why scorer misses it: _SECRET_BEARING_READS check is case-sensitive. Falls back to the generic broad-cross-resource-read flag.
- Suggested scorer fix: Same as agent-33 (case-insensitive set membership).

## agent-44-cfn-register-type-rce

- Policy summary: cloudformation:RegisterType + ActivateType + SetTypeDefaultVersion + PublishType on Resource:*.
- Det score: 6
- Real risk: 9
- Why scorer misses it: RegisterType registers a CloudFormation extension (custom resource type / hook) whose handler is a Lambda function the caller specifies. Every subsequent CreateStack/UpdateStack/ExecuteChangeSet using that type runs the caller's handler under the DEPLOYING team's CFN service role — persistent supply-chain backdoor across the entire CFN pipeline. Not in any high-risk set; falls through to generic Write-on-broad (6).
- Suggested scorer fix: Add cloudformation:RegisterType, cloudformation:ActivateType, cloudformation:SetTypeDefaultVersion, cloudformation:PublishType to _CATASTROPHIC_ACTIONS. Same supply-chain tier as ssm:CreateDocument (already catastrophic).

## agent-40-iam-login-profile-mfa-bypass

- Policy summary: iam:UpdateLoginProfile + iam:CreateLoginProfile + iam:ChangePassword on arn:aws:iam::*:user/* — set passwords for every IAM user.
- Det score: 6
- Real risk: 9
- Why scorer misses it: None of these are in _CATASTROPHIC_ACTIONS or _HIGH_IMPACT_MUTATION_ACTIONS. They land at floor 6 from sensitive-service-iam + Resource:*. But the impact is full console-takeover of every IAM user with a console presence — same blast as iam:CreateAccessKey (catastrophic).
- Suggested scorer fix: Add iam:UpdateLoginProfile, iam:CreateLoginProfile, iam:DeleteLoginProfile, iam:ChangePassword, iam:DeactivateMFADevice, iam:DeleteVirtualMFADevice, iam:ResyncMFADevice to _CATASTROPHIC_ACTIONS. They're the console-takeover equivalent of access-key admin.

## agent-38-iam-oidc-provider-takeover

- Policy summary: iam:CreateOpenIDConnectProvider + UpdateOpenIDConnectProviderThumbprint + AddClientIDToOpenIDConnectProvider on Resource:*.
- Det score: 6
- Real risk: 9
- Why scorer misses it: Federation-IdP takeover primitive. Attacker registers their own OIDC IdP, then any role whose trust policy references it (or with a hijacked thumbprint pointing at attacker JWKS) becomes assumable via attacker-minted tokens. Not in any high-risk set; sensitive-service-iam floor (6) is the only signal.
- Suggested scorer fix: Add iam:CreateOpenIDConnectProvider, iam:UpdateOpenIDConnectProviderThumbprint, iam:AddClientIDToOpenIDConnectProvider, iam:DeleteOpenIDConnectProvider to _CATASTROPHIC_ACTIONS. Account-takeover tier via federation.

## agent-39-iam-saml-provider-takeover

- Policy summary: iam:CreateSAMLProvider + UpdateSAMLProvider on Resource:*.
- Det score: 6
- Real risk: 9
- Why scorer misses it: SAML analogue of agent-38. Same blast — federated-identity takeover.
- Suggested scorer fix: Add iam:CreateSAMLProvider, iam:UpdateSAMLProvider, iam:DeleteSAMLProvider to _CATASTROPHIC_ACTIONS.

## agent-61-disable-security-services-narrow

- Policy summary: guardduty:DisassociateFromMasterAccount, guardduty:UpdateDetector, config:PutConfigurationRecorder, access-analyzer:UpdateAnalyzer, inspector2:DisassociateMember on Resource:*. Non-destructive-verb variants — they DISABLE security plane without using a Delete/Stop/Disable prefix.
- Det score: 6
- Real risk: 9
- Why scorer misses it: The destructive-on-broad rule catches Delete*/Stop*/Disable* variants. But Disassociate*, Update* (setting Enable=false), Put* (overwriting a recorder to record nothing) all stealthily disable security without a destructive-verb prefix. Score falls to 6.
- Suggested scorer fix: Add a class of security-monitoring-disable actions to _CATASTROPHIC_ACTIONS regardless of verb prefix: guardduty:DisassociateFromMasterAccount, guardduty:UpdateDetector, config:PutConfigurationRecorder, config:PutDeliveryChannel, access-analyzer:UpdateAnalyzer, inspector2:Disable, inspector2:DisassociateMember, securityhub:UpdateSecurityHubConfiguration, macie2:UpdateMacieSession.

## agent-43-resource-policy-cross-account-share

- Policy summary: secretsmanager:PutResourcePolicy + kms:CreateGrant + glue:PutResourcePolicy on narrow resource ARNs.
- Det score: 3
- Real risk: 8
- Why scorer misses it: Resource-policy writes on narrow ARNs only fire the narrow-write floor (3). But each one grants cross-account access to the resource in a single API call: secret content exfil, KMS-key decrypt grant, Glue catalog cross-account. Semantically identical to ecr:SetRepositoryPolicy (which IS in _CROSS_ACCOUNT_EXFIL_ACTIONS).
- Suggested scorer fix: Add to _CROSS_ACCOUNT_EXFIL_ACTIONS: secretsmanager:PutResourcePolicy, kms:CreateGrant, kms:PutKeyPolicy (already in _CATASTROPHIC), glue:PutResourcePolicy, codeartifact:PutDomainPermissionsPolicy, codeartifact:PutRepositoryPermissionsPolicy, events:PutPermission, sns:AddPermission, sqs:AddPermission, sqs:SetQueueAttributes (the queue-policy variant), efs:PutFileSystemPolicy, opensearch:UpdateDomainConfig (access policies).

## agent-60-kms-create-grant-narrow

- Policy summary: kms:CreateGrant + kms:RetireGrant on a single narrow key ARN.
- Det score: 3
- Real risk: 8
- Why scorer misses it: kms:CreateGrant isn't in any high-risk set. It issues a KMS grant (separate authorization channel from key policy) naming any principal as GranteePrincipal — including the attacker's own AWS account. Persistent until explicitly revoked; survives the JIT grant expiring.
- Suggested scorer fix: Add kms:CreateGrant to _CROSS_ACCOUNT_EXFIL_ACTIONS (floor 8). Equivalent in blast to kms:PutKeyPolicy (catastrophic) via the alternate grant mechanism.

## agent-42-s3-putbucketpolicy-narrow-bucket

- Policy summary: s3:PutBucketPolicy + s3:DeleteBucketPolicy on a narrow bucket ARN (arn:aws:s3:::corporate-pii-data).
- Det score: 5
- Real risk: 8
- Why scorer misses it: s3:PutBucketPolicy IS in _HIGH_IMPACT_MUTATION_ACTIONS so it floors at 5. But the action's effect isn't "modify resource configuration" — it's "rewrite the bucket's authorization model in one call with arbitrary content." A bucket policy of `{Effect:Allow, Principal:*, Action:s3:*}` makes every object in the targeted bucket public. Narrow-bucket-ARN scope doesn't help — the bucket IS the target.
- Suggested scorer fix: Add s3:PutBucketPolicy and s3:PutBucketAcl to _CROSS_ACCOUNT_EXFIL_ACTIONS (floor 8 regardless of resource scope). They're single-API-call public-exposure primitives.

## agent-58-wildcard-service-in-action

- Policy summary: Action: ['*:Decrypt', '*:GetSecretValue', '*:PassRole'] on Resource:*.
- Det score: 5
- Real risk: 8
- Why scorer misses it: AWS IAM allows wildcards on the LEFT of the colon. `*:Decrypt` matches kms:Decrypt + every other service's Decrypt action. `*:GetSecretValue` matches secretsmanager:GetSecretValue. `*:PassRole` matches iam:PassRole. The scorer parses the service as the part-before-colon (which is "*" here), so `service in effective_sensitive` is False — and the destructive-verb prefix matcher doesn't fire because "Decrypt" isn't a destructive verb. Falls into the generic action-name-wildcard branch (5).
- Suggested scorer fix: When the left-half is "*" or contains "*", treat it as a service-wildcard equivalent to "*:Operation" — apply the service-wildcard rules to every service that has the matched operation. Cheapest fix: if `action.startswith("*:")`, score it as if it were applied to every sensitive service simultaneously (floor 8).

## agent-59-wildcard-glob-broad-action

- Policy summary: Action: '*:Create*' on Resource:* — match every Create* action in every AWS service.
- Det score: 5
- Real risk: 9
- Why scorer misses it: Same as agent-58 but with a glob on both sides. `*:Create*` matches iam:CreateAccessKey + iam:CreateRole + iam:CreatePolicy + organizations:CreateAccount + sso-admin:CreatePermissionSet + every other catastrophic Create-prefixed action. The generic wildcard branch scores 5.
- Suggested scorer fix: Same as agent-58. Also: if the action-glob pattern (lowered) would match ANY entry in _CATASTROPHIC_ACTIONS_LC by fnmatch, floor at 9. The matching can be done at scorer init time (pre-expand the catastrophic set against the action universe via policy_sentry, then check glob membership).

## agent-45-eventbridge-api-destination-exfil

- Policy summary: events:CreateApiDestination + events:CreateConnection + events:PutRule + events:PutTargets + scheduler:CreateSchedule on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: These set up persistent outbound HTTPS calls to attacker-controlled URLs on cron — exact analogue of logs:PutSubscriptionFilter (which IS in _CROSS_ACCOUNT_EXFIL_ACTIONS). Floor 6 only.
- Suggested scorer fix: Add events:CreateApiDestination, events:CreateConnection, events:PutTargets (when target is API destination or cross-account ARN — hard to detect), scheduler:CreateSchedule, scheduler:UpdateSchedule to _CROSS_ACCOUNT_EXFIL_ACTIONS.

## agent-46-ec2-vpc-peering-bridge

- Policy summary: ec2:CreateVpcPeeringConnection + AcceptVpcPeeringConnection + CreateTransitGatewayPeeringAttachment + AcceptTransitGatewayPeeringAttachment + CreateRoute on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: Bridge to attacker-controlled VPC at the network layer. Once peered + routed, attacker has network access to every private-subnet resource in prod — bypasses all security-group rules that assumed "only internal IPs can reach me." Persistent past the JIT grant.
- Suggested scorer fix: Add ec2:CreateVpcPeeringConnection, ec2:AcceptVpcPeeringConnection, ec2:CreateTransitGatewayPeeringAttachment, ec2:AcceptTransitGatewayPeeringAttachment, ec2:CreateClientVpnEndpoint, ec2:CreateNetworkInterface (when paired with attaching to running instance) to _CROSS_ACCOUNT_EXFIL_ACTIONS or a new _NETWORK_BRIDGE class.

## agent-47-rds-modify-master-password

- Policy summary: rds:ModifyDBInstance + ModifyDBCluster + RestoreDBInstanceFromDBSnapshot + RestoreDBClusterFromSnapshot on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: rds:ModifyDBInstance accepts a MasterUserPassword parameter that overwrites the DB master password in one call. Restore-from-snapshot accepts a fresh master password too — creates a new instance with attacker-known credentials cloned from prod data. Generic Write floor (6) only.
- Suggested scorer fix: Add rds:ModifyDBInstance, rds:ModifyDBCluster, rds:RestoreDBInstanceFromDBSnapshot, rds:RestoreDBClusterFromSnapshot, rds:RestoreDBInstanceToPointInTime, rds:RestoreDBClusterToPointInTime to _HIGH_IMPACT_MUTATION_ACTIONS (floor 5-6), or better: _CROSS_ACCOUNT_EXFIL_ACTIONS when the action permits cross-account snapshot or restore.

## agent-48-ecs-execute-command-rce

- Policy summary: ecs:ExecuteCommand on Resource:* — interactive shell into every running container.
- Det score: 6
- Real risk: 8
- Why scorer misses it: ECS Exec is the ECS-data-plane RCE primitive — equivalent to ssm:StartSession for EC2 (which IS in _HIGH_IMPACT_MUTATION_ACTIONS). Inside the container, attacker has task-role credentials + in-container secrets. Floor 6 only.
- Suggested scorer fix: Add ecs:ExecuteCommand to _HIGH_IMPACT_MUTATION_ACTIONS (floor 5) and treat it as a code-execution primitive in the composition rule when paired with iam:PassRole.

## agent-49-eks-kubernetes-api-access

- Policy summary: eks:AccessKubernetesApi + eks:CreateAccessEntry + eks:AssociateAccessPolicy + eks:UpdateAccessEntry on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: eks:CreateAccessEntry + AssociateAccessPolicy lets the caller grant k8s cluster-admin (e.g. AmazonEKSClusterAdminPolicy) to any principal. Then eks:AccessKubernetesApi gives them the API endpoint to use it. Single-API-call escalation to k8s cluster-admin → RCE on every pod via kubectl exec. Not in any high-risk set.
- Suggested scorer fix: Add eks:AccessKubernetesApi, eks:CreateAccessEntry, eks:AssociateAccessPolicy, eks:UpdateAccessEntry to _HIGH_IMPACT_MUTATION_ACTIONS. Better: treat eks:AccessKubernetesApi + eks:Create/AssociateAccessEntry as a composition (analogous to iam-recon + sts:AssumeRole).

## agent-50-codeartifact-supply-chain

- Policy summary: codeartifact:PublishPackageVersion + PutDomainPermissionsPolicy + PutRepositoryPermissionsPolicy on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: CodeArtifact is the org's internal package registry (npm/pip/maven/nuget). Publish poisoned package versions → every developer/CI/build pulls attacker code on next install. Cross-account permissions-policy grants persistent attacker publish rights. Same tier as ecr:PutImage (which IS in _HIGH_IMPACT_MUTATION_ACTIONS) and imagebuilder pipeline poisoning (catastrophic).
- Suggested scorer fix: Add codeartifact:PublishPackageVersion, codeartifact:UpdatePackageVersionsStatus to _HIGH_IMPACT_MUTATION_ACTIONS. Add codeartifact:PutDomainPermissionsPolicy, codeartifact:PutRepositoryPermissionsPolicy to _CROSS_ACCOUNT_EXFIL_ACTIONS.

## agent-51-transfer-family-sftp-user

- Policy summary: transfer:CreateUser + UpdateUser + ImportSshPublicKey + CreateServer on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: Transfer Family CreateUser binds an SFTP user (with attacker SSH key) to an existing Transfer server with an existing IAM role. Attacker gets SFTP + SSH access to the S3 buckets the server fronts, with the credentials of the Transfer-Family role. Looks like a routine user record in CloudTrail.
- Suggested scorer fix: Add transfer:CreateUser, transfer:UpdateUser, transfer:ImportSshPublicKey, transfer:CreateServer, transfer:UpdateServer to _HIGH_IMPACT_MUTATION_ACTIONS. Could also be paired with PassRole composition (transfer:CreateUser passes the assigned role to the SFTP user).

## agent-52-bedrock-agent-rce-rag-poisoning

- Policy summary: bedrock:CreateAgent + CreateAgentActionGroup + CreateKnowledgeBase + StartIngestionJob on Resource:* + iam:PassRole on bedrock-execution-role.
- Det score: 6
- Real risk: 8
- Why scorer misses it: Bedrock IS in _SENSITIVE_SERVICES (good, floor 6). But: (a) bedrock:CreateAgent + CreateAgentActionGroup composes with iam:PassRole the same way Lambda CreateFunction does (Agent action groups invoke arbitrary Lambdas under the Agent's role) — but bedrock actions aren't in _CODE_EXECUTION_PRIMITIVES, so the composition rule misses. (b) bedrock:CreateKnowledgeBase + StartIngestionJob poisons RAG context for every downstream agent query — prompt-injection at scale.
- Suggested scorer fix: Add bedrock:CreateAgent, bedrock:UpdateAgent, bedrock:CreateAgentActionGroup, bedrock:UpdateAgentActionGroup to _CODE_EXECUTION_PRIMITIVES (so they trigger the composition rule with iam:PassRole). Add bedrock:CreateKnowledgeBase, bedrock:UpdateKnowledgeBase, bedrock:CreateDataSource, bedrock:StartIngestionJob to _HIGH_IMPACT_MUTATION_ACTIONS — RAG poisoning is supply-chain tier for AI workloads.

## agent-53-sagemaker-domain-rce

- Policy summary: sagemaker:CreateDomain + CreateUserProfile + CreateApp + CreatePresignedDomainUrl + CreateSpace on Resource:* + iam:PassRole on sagemaker-execution-role.
- Det score: 6
- Real risk: 8
- Why scorer misses it: The composition rule only covers the legacy SageMaker NotebookInstance actions. The current SageMaker Studio uses Domain + UserProfile + App + PresignedDomainUrl — same blast (interactive Jupyter shell under the execution role) but missed by the composition pass.
- Suggested scorer fix: Add sagemaker:CreateDomain, sagemaker:CreateUserProfile, sagemaker:CreateApp, sagemaker:CreatePresignedDomainUrl, sagemaker:CreateSpace, sagemaker:CreateImage to _CODE_EXECUTION_PRIMITIVES.

## agent-54-emr-runjobflow-rce-passrole

- Policy summary: elasticmapreduce:RunJobFlow + AddJobFlowSteps on Resource:* + iam:PassRole on EMR_EC2_DefaultRole.
- Det score: 6
- Real risk: 8
- Why scorer misses it: EMR is the original Glue: a managed Spark/Hadoop cluster that runs caller-supplied bootstrap actions and steps. The EMR EC2 role often has broad S3 read for "any bucket the job needs." Composition rule doesn't include EMR.
- Suggested scorer fix: Add elasticmapreduce:RunJobFlow, elasticmapreduce:AddJobFlowSteps, elasticmapreduce:CreateCluster to _CODE_EXECUTION_PRIMITIVES.

## agent-56-backup-cross-account-copy

- Policy summary: backup:PutBackupVaultAccessPolicy + StartCopyJob + StartBackupJob + CreateBackupVault on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: AWS Backup lets you take a fresh backup of any resource into a vault you own, then share the vault cross-account (PutBackupVaultAccessPolicy), then attacker copies + restores in their own account. Full data exfil via the backup plane. Same tier as rds:ModifyDBSnapshotAttribute (in _CROSS_ACCOUNT_EXFIL_ACTIONS).
- Suggested scorer fix: Add backup:PutBackupVaultAccessPolicy, backup:StartCopyJob (cross-account variant), backup:StartBackupJob to _CROSS_ACCOUNT_EXFIL_ACTIONS.

## agent-57-iam-service-cert-mitm

- Policy summary: iam:UploadServerCertificate + UpdateServerCertificate + UploadSigningCertificate + UpdateSigningCertificate + UploadSSHPublicKey on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: These are key-material installation primitives. UploadSSHPublicKey on an IAM user grants attacker's SSH key permanent git-push access to CodeCommit repos as that user. UploadServerCertificate installs a TLS cert + private key in IAM usable by Classic ELB / CloudFront for MITM impersonation. UpdateSigningCertificate updates code-signing identity. Floor 6 only.
- Suggested scorer fix: Add iam:UploadServerCertificate, iam:UpdateServerCertificate, iam:UploadSigningCertificate, iam:UpdateSigningCertificate, iam:UploadSSHPublicKey to _CATASTROPHIC_ACTIONS or _CROSS_ACCOUNT_EXFIL_ACTIONS. They install authenticated identity material the attacker controls.

## agent-41-iam-password-policy-weaken

- Policy summary: iam:UpdateAccountPasswordPolicy on Resource:* — account-wide password policy rewrite.
- Det score: 6
- Real risk: 8
- Why scorer misses it: Single API call sets MinimumPasswordLength=6, RequireSymbols=false, MaxPasswordAge=0 — every subsequent password reset can use trivial passwords. Persistent past the JIT grant. Common evidence-evasion step in account-takeover playbooks. Floor 6 only (sensitive-service iam + Resource:*).
- Suggested scorer fix: Add iam:UpdateAccountPasswordPolicy, iam:DeleteAccountPasswordPolicy to _HIGH_IMPACT_MUTATION_ACTIONS (floor at minimum 7).


# ===========================================================
# Round 2 — cross-cutting scorer recommendations
# ===========================================================
# The new findings cluster into 6 fixable rule classes:
#
# A. Case-insensitive action membership. AWS treats action names case-
#    insensitively; the scorer must too. Lowercase all sets at module
#    load and compare `action.lower() in _SET_LC`. Apply to
#    _CATASTROPHIC_ACTIONS, _HIGH_IMPACT_MUTATION_ACTIONS, _HIGH_RISK_ACTIONS,
#    _SECRET_BEARING_READS, _CROSS_ACCOUNT_EXFIL_ACTIONS,
#    _CODE_EXECUTION_PRIMITIVES, _DECEPTIVE_WRITE_ACTIONS, the
#    `iam:PassRole` literal comparison, the `sts:AssumeRole` literal,
#    the iam_recon_actions set, and the destructive-verb prefix matcher
#    (compare action_name.lower() against ("delete","stop",...lowercased)).
#    Closes agent-33, 34, 36, 37 and prevents this entire class.
#
# B. _CATASTROPHIC_ACTIONS additions. Federation IdP takeover, IAM user
#    console takeover, CFN extension registration, account-wide cert/key
#    installation. Add: iam:CreateOpenIDConnectProvider, iam:Update-
#    OpenIDConnectProviderThumbprint, iam:AddClientIDToOpenIDConnectProvider,
#    iam:CreateSAMLProvider, iam:UpdateSAMLProvider, iam:UpdateLoginProfile,
#    iam:CreateLoginProfile, iam:ChangePassword, iam:DeactivateMFADevice,
#    iam:DeleteVirtualMFADevice, iam:ResyncMFADevice, iam:UploadServer-
#    Certificate, iam:UploadSSHPublicKey, iam:UpdateAccountPasswordPolicy,
#    cloudformation:RegisterType, cloudformation:ActivateType,
#    cloudformation:SetTypeDefaultVersion, cloudformation:PublishType.
#    Closes agent-38, 39, 40, 41, 44, 57.
#
# C. _CROSS_ACCOUNT_EXFIL_ACTIONS additions. Resource-policy writes that
#    cross account, KMS grant issuance, S3 bucket-policy rewrite, network
#    bridging, backup cross-account copy. Add: secretsmanager:Put-
#    ResourcePolicy, kms:CreateGrant, glue:PutResourcePolicy,
#    codeartifact:PutDomainPermissionsPolicy, codeartifact:PutRepository-
#    PermissionsPolicy, events:PutPermission, events:CreateApiDestination,
#    s3:PutBucketPolicy (already in HIGH_IMPACT — move to CROSS_ACCOUNT_EXFIL),
#    s3:PutBucketAcl, ec2:CreateVpcPeeringConnection, ec2:AcceptVpcPeering-
#    Connection, ec2:CreateTransitGatewayPeeringAttachment, ec2:Accept-
#    TransitGatewayPeeringAttachment, backup:PutBackupVaultAccessPolicy,
#    backup:StartCopyJob. Closes agent-42, 43, 45, 46, 56, 60.
#
# D. _CODE_EXECUTION_PRIMITIVES additions. New compute primitives missed
#    by round 1. Add: bedrock:CreateAgent, bedrock:UpdateAgent,
#    bedrock:CreateAgentActionGroup, sagemaker:CreateDomain, sagemaker:
#    CreateUserProfile, sagemaker:CreateApp, sagemaker:CreatePresigned-
#    DomainUrl, sagemaker:CreateSpace, elasticmapreduce:RunJobFlow,
#    elasticmapreduce:AddJobFlowSteps. Closes agent-52, 53, 54.
#
# E. _HIGH_IMPACT_MUTATION_ACTIONS additions. Data-plane RCE / data-plane
#    takeover. Add: ecs:ExecuteCommand, eks:AccessKubernetesApi,
#    eks:CreateAccessEntry, eks:AssociateAccessPolicy, eks:UpdateAccessEntry,
#    transfer:CreateUser, transfer:UpdateUser, transfer:ImportSshPublicKey,
#    transfer:CreateServer, codeartifact:PublishPackageVersion,
#    codeartifact:UpdatePackageVersionsStatus, rds:ModifyDBInstance,
#    rds:ModifyDBCluster, rds:RestoreDBInstanceFromDBSnapshot, rds:Restore-
#    DBClusterFromSnapshot, bedrock:CreateKnowledgeBase, bedrock:Create-
#    DataSource, bedrock:StartIngestionJob. Closes agent-47, 48, 49, 50,
#    51, 52, 53.
#
# F. Wildcard-in-service handling. Action strings with `*` on the LEFT
#    of the colon (`*:Decrypt`, `*:GetSecretValue`, `*:Create*`) are
#    unbounded service-wildcards in disguise. Cheapest fix: if the
#    action string starts with "*:" OR contains "*" in the service
#    portion, treat as floor 8 (or 9 if the right-half wildcard would
#    match any catastrophic action). Closes agent-58, 59.
#
# G. Defense-disablement actions without destructive-verb prefix. Add
#    `Disassociate*`, `Update*-Detector/Recorder/Analyzer` to a stealth-
#    disable class with floor 9. Closes agent-61.


# ===========================================================
# Round 3 — adversarial agent findings, 2026-05-13
# ===========================================================
# Round 3 ran black-box against the scorer with rounds 1+2's 57 fixes
# in place. Found 24 additional blind spots with gap >= 2. They cluster
# into 6 classes (H-M, continuing the round-2 numbering):

## agent-62-notresource-narrow-action-bypass

- Policy summary: s3:DeleteObject + s3:DeleteBucket + s3:PutObject with NotResource: [arn:aws:s3:::audit-bucket].
- Det score: 3 (narrow-write floor)
- Real risk: 8
- Why scorer misses it: The NotResource branch only fires when `any("*" in a for a in actions)`. Specific (non-wildcard) actions skip the check entirely. Resource list is empty, so wildcard_resource=False and broad_blast_resource=False; the destructive-on-broad rule and high-risk-on-broad rule both gate on these. NotResource semantically means "every resource EXCEPT" — broader than Resource: '*' minus a few — yet treated as if no resource was specified at all. Five-point gap.
- Suggested scorer fix: When `NotResource` is present and `Resource` is absent/empty, treat the effective resource set as broad. Run the destructive-verb / sensitive-on-broad / high-risk-on-broad / state-changing-on-broad / catastrophic floors against this synthetic-broad set.

## agent-63-notresource-passrole-broad

- Policy summary: iam:PassRole with NotResource: [arn:aws:iam::123:role/SystemRole]. Effectively broad PassRole.
- Det score: 4 (narrow-PassRole floor)
- Real risk: 9
- Why scorer misses it: PassRole rule reads `resources` (empty list). `wildcard_resource = any(... for r in resources)` is False — so the broad-PassRole 9-floor never fires. NotResource on PassRole is the iconic "I excluded ONE role I shouldn't pass" footgun, and it's the broadest possible PassRole.
- Suggested scorer fix: when iam:PassRole appears with NotResource and empty Resource, floor at 9. Same fix as agent-62 generalized for PassRole specifically.

## agent-64-iam-group-user-policy-attach

- Policy summary: iam:AttachUserPolicy + iam:PutUserPolicy + iam:AttachGroupPolicy + iam:PutGroupPolicy + iam:AddUserToGroup on Resource:*.
- Det score: 6
- Real risk: 9
- Why scorer misses it: _CATASTROPHIC_ACTIONS includes only the ROLE variants. AWS IAM has three principal types (role/user/group) and policy-attachment is symmetric across all three. Attacker can attach AdministratorAccess to a user, attach to a group containing themselves, or add themselves to an existing admin group — same primitive, different principal type, no catastrophic floor fires.
- Suggested scorer fix: add iam:AttachUserPolicy, iam:PutUserPolicy, iam:AttachGroupPolicy, iam:PutGroupPolicy, iam:AddUserToGroup, iam:DetachUserPolicy, iam:DetachGroupPolicy, iam:RemoveUserFromGroup to _CATASTROPHIC_ACTIONS.

## agent-65-rolesanywhere-federation-takeover

- Policy summary: rolesanywhere:CreateTrustAnchor + CreateProfile + UpdateTrustAnchor + EnableTrustAnchor on Resource:*.
- Det score: 6
- Real risk: 9
- Why scorer misses it: IAM Roles Anywhere is the X.509-cert federation primitive — the exact analogue of iam:CreateOpenIDConnectProvider and iam:CreateSAMLProvider (both in _CATASTROPHIC_ACTIONS), but for client certificates. Attacker registers their CA as a trust anchor and creates a profile mapping to any role; subsequent rolesanywhere:CreateSession with attacker-signed certs mints role credentials. Not in any list.
- Suggested scorer fix: add rolesanywhere:CreateTrustAnchor, rolesanywhere:UpdateTrustAnchor, rolesanywhere:EnableTrustAnchor, rolesanywhere:CreateProfile, rolesanywhere:UpdateProfile, rolesanywhere:EnableProfile to _CATASTROPHIC_ACTIONS.

## agent-66-cognito-identity-pool-takeover

- Policy summary: cognito-identity:CreateIdentityPool + UpdateIdentityPool + SetIdentityPoolRoles on Resource:*.
- Det score: 6
- Real risk: 9
- Why scorer misses it: Round 1 added cognito-idp:Admin* to catastrophic (user-pool admin). But cognito-identity (identity pool / federation-to-IAM-role side) has its own role-binding primitive that's MORE dangerous: SetIdentityPoolRoles binds any role to a pool. With unauthenticated identities enabled, literally anyone can call GetCredentialsForIdentity and get those role credentials. Same primitive as iam:CreateOpenIDConnectProvider but worse (no auth required).
- Suggested scorer fix: add cognito-identity:CreateIdentityPool, cognito-identity:UpdateIdentityPool, cognito-identity:SetIdentityPoolRoles to _CATASTROPHIC_ACTIONS.

## agent-67-lambda-layer-rce-via-passrole

- Policy summary: lambda:PublishLayerVersion + lambda:AddLayerVersionPermission + lambda:UpdateFunctionConfiguration on Resource:*, plus iam:PassRole on a narrow lambda role.
- Det score: 6
- Real risk: 9
- Why scorer misses it: Lambda Layers are a code-injection primitive not in _CODE_EXECUTION_PRIMITIVES. PublishLayerVersion + UpdateFunctionConfiguration replaces the runtime of an existing function with attacker-controlled code, running under whatever role the function already has. This is RCE-as-target-function-role without ever calling UpdateFunctionCode. The PassRole composition rule doesn't fire because none of these actions are in the code-exec set.
- Suggested scorer fix: add lambda:PublishLayerVersion, lambda:UpdateFunctionConfiguration, lambda:AddLayerVersionPermission to _CODE_EXECUTION_PRIMITIVES. lambda:AddLayerVersionPermission with broad principal also fits _CROSS_ACCOUNT_EXFIL_ACTIONS (account-wide layer sharing).

## agent-68-ssm-start-session-account-wide

- Policy summary: ssm:StartSession + ssm:SendCommand on Resource:* — interactive shell on every managed instance.
- Det score: 6
- Real risk: 9
- Why scorer misses it: ssm:StartSession and ssm:SendCommand are in _HIGH_IMPACT_MUTATION_ACTIONS (floor 5) but not _CATASTROPHIC_ACTIONS. ecs:ExecuteCommand IS catastrophic (interactive shell on ECS task); the SSM-equivalent for EC2 fleets is treated as merely high-impact. On Resource:*, this is account-wide RCE on every managed instance.
- Suggested scorer fix: add ssm:StartSession and ssm:SendCommand to _CATASTROPHIC_ACTIONS. OR keep them in high-impact but escalate to 9 when paired with Resource:*. Sibling of ecs:ExecuteCommand which is already catastrophic.

## agent-69-glue-dev-endpoint-jupyter-rce

- Policy summary: glue:CreateDevEndpoint + UpdateDevEndpoint + CreateSession + iam:PassRole on glue-data-access-role.
- Det score: 6
- Real risk: 9
- Why scorer misses it: Glue Dev Endpoints are persistent Jupyter/Zeppelin notebooks with an attached IAM role — identical primitive to sagemaker:CreateNotebookInstance (which IS in code-exec). The composition rule fires for SageMaker; Glue's analogous path isn't in the list.
- Suggested scorer fix: add glue:CreateDevEndpoint, glue:UpdateDevEndpoint, glue:CreateSession, glue:RunStatement to _CODE_EXECUTION_PRIMITIVES.

## agent-70-mwaa-airflow-dag-rce

- Policy summary: airflow:CreateEnvironment + UpdateEnvironment with iam:PassRole on mwaa-execution-role.
- Det score: 6
- Real risk: 9
- Why scorer misses it: MWAA (Managed Workflows for Apache Airflow) runs caller-supplied Python DAGs as the execution role. Same primitive as lambda:CreateFunction (code from caller-chosen S3 bucket + execution role from PassRole). Not in _CODE_EXECUTION_PRIMITIVES.
- Suggested scorer fix: add airflow:CreateEnvironment, airflow:UpdateEnvironment, airflow:CreateCliToken, airflow:CreateWebLoginToken to _CODE_EXECUTION_PRIMITIVES.

## agent-71-lakeformation-data-lake-grant

- Policy summary: lakeformation:GrantPermissions + PutDataLakeSettings + RegisterResource on Resource:*.
- Det score: 6
- Real risk: 9
- Why scorer misses it: Lake Formation is the data-lake access-control plane. GrantPermissions = grant any IAM principal SELECT/ALTER/DROP on any registered table. PutDataLakeSettings = make caller an LF admin (persistent escalation). Identical severity to iam:AttachRolePolicy but in the data-lake plane.
- Suggested scorer fix: add lakeformation:GrantPermissions, lakeformation:PutDataLakeSettings to _CATASTROPHIC_ACTIONS. Add lakeformation:RegisterResource, lakeformation:UpdateResource to _HIGH_IMPACT_MUTATION_ACTIONS. Consider adding `lakeformation` to _SENSITIVE_SERVICES.

## agent-72-service-catalog-cfn-rce

- Policy summary: servicecatalog:CreateProduct + ProvisionProduct + iam:PassRole on launch-role.
- Det score: 6
- Real risk: 9
- Why scorer misses it: Service Catalog is a managed CloudFormation wrapper. ProvisionProduct = ExecuteChangeSet under a launch role. The composition with PassRole is full account compromise, identical to cfn:ExecuteChangeSet + PassRole (which DOES fire the composition rule). Service Catalog actions aren't in code-exec.
- Suggested scorer fix: add servicecatalog:CreateProduct, servicecatalog:UpdateProduct, servicecatalog:ProvisionProduct, servicecatalog:AssociatePrincipalWithPortfolio to _CODE_EXECUTION_PRIMITIVES.

## agent-73-iam-action-glob-matches-catastrophic

- Policy summary: iam:Create* + iam:*AccessKey* on Resource:*.
- Det score: 7 (sensitive-service-inside-wildcard floor)
- Real risk: 10
- Why scorer misses it: The sensitive-service-inside-wildcard rule floors at 7, but iam:Create* matches iam:CreateAccessKey, iam:CreateOpenIDConnectProvider, iam:CreateSAMLProvider, iam:CreateLoginProfile, iam:CreateInstanceProfile — every one IS in _CATASTROPHIC_ACTIONS. The glob is a superset of catastrophic coverage; floor should be 9-10.
- Suggested scorer fix: when an action contains an action-name wildcard (after the `:`), check if the glob would match ANY entry in _CATASTROPHIC_ACTIONS_LC via fnmatch. If yes, floor at 9. Same for _HIGH_IMPACT and _CODE_EXECUTION_PRIMITIVES (the latter would catch `lambda:*` matching Create/UpdateFunction).

## agent-74-passrole-case-insensitivity

- Policy summary: iam:passrole (lowercase) on Resource:*.
- Det score: 7 (high-risk-action-on-broad via _HIGH_RISK_ACTIONS_LC)
- Real risk: 9
- Why scorer misses it: Round 2 case-insensitivity fix lowered the set lookups but missed the literal-string compare `if action == "iam:PassRole":` at line ~1110. So `iam:passrole` falls through to the `elif action.lower() in _HIGH_RISK_ACTIONS_LC` branch (which floors at 7), instead of the broad-PassRole 9-floor. The 2-point gap crosses the auto-approve threshold for some configurations.
- Suggested scorer fix: change `if action == "iam:PassRole":` to `if action.lower() == "iam:passrole":`. Same pattern for any other literal-string action compares that escaped the round-2 sweep.

## agent-75-iam-create-user-create-role

- Policy summary: iam:CreateUser + iam:CreateRole on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: iam:CreateAccessKey is catastrophic; iam:CreateOpenIDConnectProvider is catastrophic; iam:AttachRolePolicy is catastrophic — but iam:CreateUser and iam:CreateRole, the "step 1" persistence primitives, are NOT. The scorer recognizes the policy-attach step but not the principal-creation step. CreateUser is a persistent backdoor (survives JIT revocation). CreateRole with attacker-controlled trust policy is a federation-takeover primitive.
- Suggested scorer fix: add iam:CreateUser, iam:CreateRole to _CATASTROPHIC_ACTIONS.

## agent-76-account-segment-wildcard-arn

- Policy summary: lambda:UpdateFunctionCode + InvokeFunction on arn:aws:lambda:us-east-1:*:function:my-deploy-bot.
- Det score: 5 (high-impact-mutation floor)
- Real risk: 7
- Why scorer misses it: The wildcard is in the ACCOUNT-ID segment of the ARN. _is_broad_resource and _is_strict_wildcard inspect `resource_spec` (parts[5]) but not `parts[4]` (account-id). So `arn:aws:lambda:us-east-1:*:function:my-deploy-bot` looks narrow (specific function name), but applies to that function in EVERY account.
- Suggested scorer fix: in _is_broad_resource and _is_strict_wildcard, also check parts[4] (account-id) for `*`. If account is wildcarded and the requester's own account isn't implied by request context, treat as broad. Region wildcard (parts[3]) is also broadening but less severe (legitimately * for global services).

## agent-77-kms-alias-key-collection-wildcards

- Policy summary: kms:Decrypt + GenerateDataKey on arn:aws:kms:us-east-1:123:alias/* — every alias in region.
- Det score: 6
- Real risk: 7
- Why scorer misses it: The IAM principal-collection wildcard branch in _is_strict_wildcard is hard-coded to service=="iam". Other services have analogous collection wildcards: kms:alias/*, kms:key/*, lambda:layer/*. _is_broad_resource catches them via the generic `endswith('/*') and count('/')<=1` branch, but _is_strict_wildcard doesn't — and the secret-bearing-reads / high-risk-on-strict-broad rule uses strict_wildcard. So kms:Decrypt on alias/* misses the 7-floor that fires on kms:Decrypt on Resource:'*'.
- Suggested scorer fix: extend the strict-wildcard collection check to non-IAM services. Either a per-service map of known collection prefixes, or generalize: any resource_spec ending in `/<single-glob-segment>` (no further slashes) counts as collection-level wildcard.

## agent-78-verified-permissions-authz-bypass

- Policy summary: verifiedpermissions:CreatePolicy + UpdatePolicy + PutSchema + CreatePolicyStore + CreateIdentitySource on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: Amazon Verified Permissions is the application-layer authorization service (Cedar policies). Mutating Cedar policies = rewriting application authorization. Persists past JIT grant. Not in any list.
- Suggested scorer fix: add verifiedpermissions:CreatePolicy, verifiedpermissions:UpdatePolicy, verifiedpermissions:PutSchema, verifiedpermissions:CreatePolicyStore, verifiedpermissions:CreateIdentitySource to _HIGH_IMPACT_MUTATION_ACTIONS (floor 5) or a new authorization-plane-mutation set.

## agent-79-appflow-saas-exfil

- Policy summary: appflow:CreateFlow + UpdateFlow + StartFlow + CreateConnectorProfile on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: AppFlow pipes SaaS data (Salesforce/Slack/ServiceNow) into AWS destinations. Single-call persistent-exfil setup, same primitive as logs:PutSubscriptionFilter (already in _CROSS_ACCOUNT_EXFIL_ACTIONS). Not in any list.
- Suggested scorer fix: add appflow:CreateFlow, appflow:UpdateFlow, appflow:StartFlow, appflow:CreateConnectorProfile to _CROSS_ACCOUNT_EXFIL_ACTIONS.

## agent-80-opensearch-serverless-data-access

- Policy summary: aoss:CreateAccessPolicy + UpdateAccessPolicy + CreateSecurityPolicy on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: OpenSearch Serverless (`aoss:` prefix) handles authorization through data-access policies separate from IAM. CreateAccessPolicy grants persistent search/data access to any IAM principal. Same semantics as resource-policy mutation in other services.
- Suggested scorer fix: add aoss:CreateAccessPolicy, aoss:UpdateAccessPolicy, aoss:CreateSecurityPolicy to _CROSS_ACCOUNT_EXFIL_ACTIONS.

## agent-81-ecr-public-supply-chain

- Policy summary: ecr-public:PutImage + UploadLayerPart + SetRepositoryPolicy on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: `ecr:` and `ecr-public:` are TWO services. The ECR catastrophic / high-impact coverage doesn't cover ECR Public. ecr-public:PutImage publishes containers to public.ecr.aws under the company name — brand-impersonation supply-chain attack.
- Suggested scorer fix: add ecr-public:PutImage, ecr-public:UploadLayerPart, ecr-public:SetRepositoryPolicy to _HIGH_IMPACT_MUTATION_ACTIONS or _CROSS_ACCOUNT_EXFIL_ACTIONS. Also consider adding service-alias handling: ecr / ecr-public should share treatment.

## agent-82-synthetics-canary-rce

- Policy summary: synthetics:CreateCanary + UpdateCanary + iam:PassRole on canary-execution-role.
- Det score: 6
- Real risk: 8
- Why scorer misses it: CloudWatch Synthetics canaries are caller-supplied Node/Python scripts running on Lambda-like workers as the canary execution role. Identical primitive to lambda:CreateFunction. Composition rule misses it.
- Suggested scorer fix: add synthetics:CreateCanary, synthetics:UpdateCanary to _CODE_EXECUTION_PRIMITIVES.

## agent-83-direct-connect-network-bridge

- Policy summary: directconnect:CreateInterconnect + AllocatePrivateVirtualInterface + AcceptPrivateVirtualInterface on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: Direct Connect actions create network paths from external data centers into VPCs. Single-call persistent network bridge, identical primitive to ec2:CreateVpcPeeringConnection (which IS in _CROSS_ACCOUNT_EXFIL_ACTIONS).
- Suggested scorer fix: add directconnect:CreateInterconnect, directconnect:AllocatePrivateVirtualInterface, directconnect:AcceptPrivateVirtualInterface, directconnect:CreateBGPPeer, directconnect:UpdateVirtualInterfaceAttributes to _CROSS_ACCOUNT_EXFIL_ACTIONS. Sibling actions to consider: ec2:CreateVpnConnection, ec2:CreateClientVpnEndpoint (Site-to-Site VPN / Client VPN).

## agent-84-iam-tag-abac-bypass

- Policy summary: iam:TagRole + UntagRole + TagUser + UntagUser on Resource:*.
- Det score: 6
- Real risk: 8
- Why scorer misses it: Many orgs use ABAC (attribute-based access control) where IAM policies authorize via aws:ResourceTag/aws:PrincipalTag. Mutating tags = mutating authorization. Account-wide TagRole = ABAC-authorization bypass primitive. Tagging is IAM access level "Tagging" so floor-via-state-changing-write fires only at level 6.
- Suggested scorer fix: add iam:TagRole, iam:UntagRole, iam:TagUser, iam:UntagUser to _HIGH_IMPACT_MUTATION_ACTIONS (floor 5). For broader visibility, emit a factor noting "ABAC bypass primitive if org uses aws:ResourceTag conditions" whenever any *TagResource* action appears on Resource:*. Same applies to ec2:CreateTags, s3:PutBucketTagging, kms:TagResource, tag:TagResources (the cross-service tagging API).

## agent-85-s3-object-lambda-service-alias

- Policy summary: s3-object-lambda:GetObject on Resource:* in a read-only request.
- Det score: 4 (cross-resource-read floor)
- Real risk: 7
- Why scorer misses it: AWS has SIBLING service prefixes for S3-object operations: s3:, s3-object-lambda:, s3-outposts:. _SECRET_BEARING_READS lists `s3:GetObject` but not `s3-object-lambda:GetObject` or `s3-outposts:GetObject` — all three read the same object data. Object Lambda is particularly important because the same prefix is also a content-rewrite primitive (s3-object-lambda:WriteGetObjectResponse).
- Suggested scorer fix: add `s3-object-lambda:GetObject`, `s3-outposts:GetObject` to _SECRET_BEARING_READS. Better: introduce a service-alias canonicalization step that maps s3-object-lambda/s3-outposts → s3 for rule lookup. Same problem may exist for other "service variants" (rds vs rds-data; logs vs cloudwatch).


# ===========================================================
# Round 3 — cross-cutting scorer recommendations
# ===========================================================
# Continuing the round-2 letter naming (A-G already used).

# H. NotResource handling for non-wildcard actions. The NotResource branch
#    only fires when at least one Action is wildcarded — completely
#    bypassing non-wildcard actions. NotResource semantically means
#    "every resource EXCEPT" — broader than Resource:'*' minus a few.
#    When NotResource is set and Resource is absent/empty, treat the
#    effective resource set as broad and run ALL the resource-broad
#    floors (destructive-on-broad, high-risk-on-broad, sensitive-on-broad,
#    catastrophic-on-broad, PassRole-broad). Closes agent-62 and 63.

# I. _CATASTROPHIC_ACTIONS additions. (a) IAM group/user policy-attachment
#    siblings of AttachRolePolicy; (b) Roles Anywhere (X.509 federation);
#    (c) cognito-identity (identity-pool role binding); (d) Lake Formation
#    grants; (e) iam:CreateUser, iam:CreateRole (principal creation); (f)
#    ssm:StartSession, ssm:SendCommand (account-wide interactive shell on
#    Resource:*). Closes agent-64, 65, 66, 68, 71, 75. Specifically:
#      iam:AttachUserPolicy, iam:PutUserPolicy, iam:AttachGroupPolicy,
#      iam:PutGroupPolicy, iam:AddUserToGroup, iam:CreateUser, iam:CreateRole,
#      rolesanywhere:CreateTrustAnchor, rolesanywhere:UpdateTrustAnchor,
#      rolesanywhere:EnableTrustAnchor, rolesanywhere:CreateProfile,
#      cognito-identity:CreateIdentityPool, cognito-identity:UpdateIdentityPool,
#      cognito-identity:SetIdentityPoolRoles, lakeformation:GrantPermissions,
#      lakeformation:PutDataLakeSettings (+ when ssm:StartSession or
#      ssm:SendCommand is on Resource:*, floor 9).

# J. _CODE_EXECUTION_PRIMITIVES additions. New compute primitives missed
#    by rounds 1+2: lambda:PublishLayerVersion, lambda:UpdateFunction-
#    Configuration, lambda:AddLayerVersionPermission, glue:CreateDevEndpoint,
#    glue:UpdateDevEndpoint, glue:CreateSession, glue:RunStatement,
#    airflow:CreateEnvironment, airflow:UpdateEnvironment,
#    servicecatalog:CreateProduct, servicecatalog:UpdateProduct,
#    servicecatalog:ProvisionProduct, servicecatalog:AssociatePrincipal-
#    WithPortfolio, synthetics:CreateCanary, synthetics:UpdateCanary.
#    Closes agent-67, 69, 70, 72, 82.

# K. _CROSS_ACCOUNT_EXFIL_ACTIONS / _HIGH_IMPACT_MUTATION_ACTIONS additions.
#    appflow:* (SaaS data piping), aoss:* access policies (OpenSearch
#    Serverless data access), ecr-public:* (public registry supply chain),
#    directconnect:* (network bridge), verifiedpermissions:* (app authz
#    bypass), iam:TagRole/UntagRole/TagUser/UntagUser (ABAC bypass).
#    Closes agent-78, 79, 80, 81, 83, 84.

# L. Action-name wildcard matching catastrophic actions. When an action
#    contains an action-name glob (`iam:Create*`, `iam:*AccessKey*`),
#    check if the glob would match ANY entry in _CATASTROPHIC_ACTIONS_LC
#    via fnmatch. If yes, floor at 9 instead of the generic 7 (sensitive-
#    service-inside-wildcard floor). Same logic for _HIGH_IMPACT and
#    _CODE_EXECUTION_PRIMITIVES — `lambda:*` matches the code-exec
#    primitives and should escalate the score. Closes agent-73.

# M. Residual case-insensitivity gaps. The round-2 sweep lowered set
#    lookups but missed literal-string compares: `if action == "iam:PassRole":`
#    at line ~1110 of review.py. Fix to `if action.lower() == "iam:passrole":`.
#    Audit all other literal-action compares. Closes agent-74.

# N. ARN account-segment + region-segment wildcard handling. _is_broad_-
#    resource and _is_strict_wildcard inspect resource_spec (parts[5]) but
#    not parts[4] (account-id) or parts[3] (region). An ARN like
#    `arn:aws:lambda:us-east-1:*:function:foo` matches that resource in
#    EVERY account in the partition — should be treated as broad. Closes
#    agent-76. Strict-wildcard collection check should also extend beyond
#    IAM to other services (kms:alias/*, kms:key/*, lambda:layer/*).
#    Closes agent-77.

# O. Service-alias canonicalization. Some AWS services have multiple
#    prefixes for the same underlying capability:
#      s3 / s3-object-lambda / s3-outposts (object operations)
#      ecr / ecr-public (container images)
#      rds / rds-data (database)
#      logs / cloudwatch (observability)
#    A canonicalization step that maps these to a single "logical service"
#    before rule lookup would prevent the entire alias-bypass class.
#    Cheapest fix: just add the alias variants to every relevant set.
#    Closes agent-85.


# ===========================================================
# Round 4 — WHITE-BOX
# ===========================================================
# Code-level review with full visibility into review.py / blacklist.py /
# routes/score.py. Findings here exploit BUGS (off-by-one, case-sensitive
# checks, missing rule paths) rather than just expanding action sets.
# Each finding cites the specific code location (file + line).


## agent-86-question-mark-wildcard-bypass

- Policy summary: `iam:?ttachRolePolicy`, `iam:?reateAccessKey`, `iam:?utRolePolicy` on Resource: `*`.
- Det score: 6 (sensitive-service-on-broad floor)
- Real risk: 9 (catastrophic — AWS evaluates `?` as a single-char wildcard, so these are equivalent to the literal catastrophic forms)
- Why scorer misses it: review.py treats `?` as a literal character throughout. Every wildcard-detection check uses `"*" in action` (lines 901, 1136, 1144, 1159, 1215, 1481, 1500). `?` is invisible to the scorer.
  - Action-name-glob rule (line 1215) and catastrophic-fnmatch rule (line 1234) only fire if `*` is present.
  - Set-membership checks (`action.lower() in <SET>_LC`, lines 920/1291/1317/1436/1456/1663) require a literal match — `?` prevents that.
  - Service-wildcard rule (line 1159) only checks `"*" in service`.
- Suggested fix: introduce a single helper `_has_wildcard(s) -> bool: return "*" in s or "?" in s`, use it everywhere a literal-wildcard test currently uses `"*" in"`; for set-membership lookups, when the attacker's action has any wildcard, fnmatch-expand it against the target set instead of checking literal membership.

## agent-87-destructive-verb-case-sensitive

- Policy summary: `s3:deletebucket`, `s3:deleteobject`, `ec2:terminateinstances`, `rds:deletedbinstance`, `dynamodb:deletetable` on Resource: `*`.
- Det score: 4
- Real risk: 8
- Why scorer misses it: AWS IAM is case-insensitive on action names, but the destructive-verb-prefix list at lines 1263-1266 (action-name-glob branch) and 1357-1360 (destructive-on-broad floor) compares `action_name.startswith(("Delete", "Destroy", ...))` with canonical-capitalized prefixes. Lowercase input never matches. Round 2's case-insensitivity sweep updated set-membership lookups (`*_LC`) but did NOT touch these literal `startswith` calls.
- Suggested fix: lowercase both sides — `action_name.lower().startswith(("delete", "destroy", ...))`. Apply at lines 1267 AND 1361.

## agent-88-code-exec-composition-wildcard

- Policy summary: `lambda:Create*` + `lambda:Update*` + `lambda:InvokeFunction` on Resource: `*`, plus `iam:PassRole` on a narrow admin-role ARN.
- Det score: 6
- Real risk: 9 (RCE-as-admin via composition)
- Why scorer misses it: `has_code_exec = bool(action_names_lc & _CODE_EXECUTION_PRIMITIVES_LC)` (line 1556) is a literal set-intersection. `lambda:Create*` is not in the primitive set; the wildcard doesn't fnmatch-expand. The standalone `code-execution primitive on broad resource` rule (line 1567) has the same issue — uses `_canonical_action(a) in _CODE_EXECUTION_PRIMITIVES_LC`.
- Suggested fix: add a helper `_action_covers_any(attacker_action, target_set_lc)` that fnmatch-expands attacker's wildcards (`*` or `?`) against the target set, OR checks literal membership when no wildcard. Use it in both composition rules + the standalone code-exec-on-broad floor.

## agent-89-iam-recon-composition-wildcard

- Policy summary: Statement 1: `iam:List*` + `iam:Get*` on Resource: `*`. Statement 2: `sts:AssumeRole` on `arn:aws:iam::*:role/*`.
- Det score: 7 (sensitive-service-on-broad + cross-resource)
- Real risk: 9 (lateral movement primitive)
- Why scorer misses it: `iam_recon_actions_lc` (line 1615) is an exact-name frozenset. `iam:list*` and `iam:get*` are not in it; the wildcards don't fnmatch-expand. Same root cause as agent-88, different composition rule.
- Suggested fix: same fnmatch-expansion helper applied to the recon composition check at line 1619.

## agent-90-notresource-policy-level-composition

- Policy summary: Statement 1: iam-recon actions on Resource: `*`. Statement 2: `sts:AssumeRole` with `NotResource: ["arn:aws:iam::123456789012:role/already-using"]` (effectively any role except one).
- Det score: 7
- Real risk: 9 (same lateral movement as agent-30 but via NotResource)
- Why scorer misses it: The per-statement loop's `NotResource → wildcard_resource = True` override (lines 1132-1134) does NOT propagate to the policy-level `all_actions` collection (lines 1539-1547). That collection computes `inclusive_broad` / `strict_broad` from `_as_list(stmt.get("Resource"))` only — NotResource is invisible to it. So `has_assume_broad` (line 1620) sees `inclusive=False` and the composition rule fails. Variables overwritten classic of round-3 NotResource fix — fixed it for per-statement but not policy-level.
- Suggested fix: when collecting `all_actions`, also apply NotResource promotion:

    not_resources_in_stmt = _as_list(stmt.get("NotResource"))
    if not_resources_in_stmt and not any(
        r == "*" or r.endswith(":*") for r in not_resources_in_stmt
    ):
        inclusive_broad = True
        strict_broad = True

  Apply at line 1545 (inside the per-statement collection loop).

## agent-91-additional-destructive-verbs

- Policy summary: `ec2:DetachVolume`, `ec2:DetachNetworkInterface`, `ec2:DisassociateIamInstanceProfile`, `ec2:DisassociateAddress`, `autoscaling:SuspendProcesses` on Resource: `*`.
- Det score: 6 (state-changing-on-broad)
- Real risk: 7 (account-wide service disruption — detach disks/NICs/instance profiles from every running instance, freeze ASGs)
- Why scorer misses it: The destructive-verb prefix list (lines 1263-1266 / 1357-1360) is missing `Detach`, `Disassociate`, `Suspend`, `Abort`, `Purge`, `Remove`, `Kill`, `Eject`. AWS uses all of these for genuinely destructive operations.
- Suggested fix: extend the prefix tuple. Same change applies in both locations (or extract to a module-level constant `_DESTRUCTIVE_VERB_PREFIXES`).

## agent-92-narrow-write-floor-order-dependent

- Policy summary: `[ec2:CreateTags, ec2:CreateVpc]` on a narrow VPC ARN.
- Det score: 2 (Tagging floor)
- Expected score: 3 (Write floor — should apply because CreateVpc is in the list)
- Why scorer misses it: The narrow-write-floor loop at lines 1479-1491 breaks after the FIRST action whose level matches Write/Tagging. Tagging hits first because of action ordering, floors at 2, and breaks before ever evaluating CreateVpc. Reverse the order (`[ec2:CreateVpc, ec2:CreateTags]`) and the score becomes 3. This is an order-dependent off-by-one that violates determinism.
- Suggested fix: remove the early break OR track the max level across the whole action list:

    level_floor = 0
    for action in actions:
        if action == "*" or ":" not in action or "*" in action:
            continue
        level = _action_level(action)
        if level in ("Write", "Permissions management"):
            level_floor = max(level_floor, 3)
        elif level == "Tagging":
            level_floor = max(level_floor, 2)
    if level_floor:
        score = max(score, level_floor)

## agent-93-blacklist-question-mark-bypass

- Policy summary: `iam:?reateAccessKey` on Resource: `*` against a deployment with `ban-create-access-key` blacklist rule installed.
- Blacklist verdict: MISS (real AWS: would grant `iam:CreateAccessKey`).
- Scorer verdict: 6 (sensitive-service-on-broad floor; missing the catastrophic floor due to agent-86).
- Real risk: 9.
- Why blacklist misses it: blacklist.py line 155 uses `fnmatch.fnmatchcase(action_lc, rule.pattern.lower())`. fnmatch is asymmetric: wildcards in the PATTERN are wildcards, but wildcards in the SUBJECT (action) are literal. Rule pattern `iam:CreateAccessKey` has literal `c`; action `iam:?reateAccessKey` has literal `?`; they don't match. AWS, in contrast, treats action's `?` as a wildcard at policy evaluation.
- Suggested fix: when the action contains glob meta-characters, ALSO check the reverse direction — fnmatch the rule's pattern as subject against the action as pattern. Or normalize via canonical-action expansion first.
- Note: also tested whitespace bypasses — leading/trailing space, tab — all bypass. AWS rejects whitespace in action names, but blacklist should reject too (don't rely on AWS rejecting).

## agent-94-secret-bearing-notresource-bypass

- Policy summary: `ec2:GetConsoleOutput`, `ec2:GetPasswordData`, `ssm:GetCommandInvocation` with `NotResource: ["arn:aws:ec2:us-east-1:123456789012:instance/i-known-empty"]` (read instance internals on every instance except one).
- Det score: 6
- Real risk: 7
- Why scorer misses it: Same NotResource policy-level oversight as agent-90. The secret-bearing-reads composition (line 1644) requires `strict` from `all_actions` collection. Policy-level `strict_broad` is computed without NotResource override.
- Suggested fix: same as agent-90 (apply NotResource promotion in the policy-level all_actions collection).

## agent-95-notaction-narrow-allows-catastrophic

- Policy summary: `NotAction: ["sts:AssumeRole"]` on `Resource: arn:aws:iam::123456789012:role/some-app-role` (every IAM API on this role except AssumeRole — includes AttachRolePolicy = full admin attach).
- Det score: 7 (NotAction-on-narrow floor, hard-coded line 977)
- Real risk: 9
- Why scorer misses it: NotAction means the Action key is absent. The per-action loop (lines 1143-1334) iterates `actions = _as_list(stmt.get("Action"))`, which is `[]`. The catastrophic-action floor at line 1455-1468 never fires because there are no actions to check. The hard-coded floor 7 doesn't consider what the implicit-allow set covers.
- Suggested fix: when NotAction is set, compute the implicit-allow as `(_CATASTROPHIC_ACTIONS_LC | _HIGH_IMPACT_MUTATION_ACTIONS_LC | _CODE_EXECUTION_PRIMITIVES_LC | ...) - not_actions_lc`. If `_CATASTROPHIC_ACTIONS_LC - not_actions_lc` is non-empty, floor at 9 regardless of resource scope (consistent with the catastrophic floor's "narrow doesn't help" semantics).


# ===========================================================
# Round 4 — cross-cutting scorer recommendations
# ===========================================================

# P. Architectural: wildcard handling. The scorer treats `*` and `?` very
#    differently — `*` triggers every wildcard rule, `?` is invisible.
#    AWS IAM treats them equivalently (single-char vs multi-char glob).
#    A single helper `_has_wildcard()` plus consistent use of fnmatch
#    when the attacker action has any wildcard would close the entire
#    class of agent-86-style bypasses. Pattern: never test `"*" in s`;
#    always test `_has_wildcard(s)`. Always fnmatch-expand against the
#    target set when wildcards are present; never set-intersect.

# Q. Architectural: NotResource policy-level promotion. The round-3
#    NotResource fix was applied per-statement (lines 1132-1134) but
#    not per-policy. The policy-level pass at line 1539-1547 ignores
#    NotResource. The fix is mechanically simple but easy to miss
#    again when adding new composition rules — extract a shared helper
#    `_statement_resource_signal(stmt) -> (resources, inclusive_broad,
#    strict_broad)` and call it from BOTH the per-statement loop and
#    the policy-level collection.

# R. Architectural: case-insensitivity. The round-2 sweep covered set-
#    membership lookups but missed two other patterns: `.startswith(
#    canonical-prefix)` (destructive verbs, agent-87) and `extra_*` admin
#    context (case-sensitive, see remarks in scorer trace at
#    `effective_sensitive`). Rule: anything that compares user-supplied
#    text against AWS surface should lowercase BOTH sides.

# S. Architectural: rule ordering / break statements. agent-92 — early
#    `break` after first level match makes the narrow-write floor order-
#    dependent. As a class: never `break` inside a loop that's
#    computing a `max(...)` floor. Compute the floor across the entire
#    list, then apply once.

# T. Architectural: NotAction's implicit-allow set. NotAction grants
#    everything-except. The current code floors at 7 (narrow) or 9
#    (broad) without considering WHAT it actually grants. Better:
#    enumerate the implicit-catastrophic set
#    (`_CATASTROPHIC_ACTIONS_LC - not_actions_lc`) and run the
#    catastrophic-floor rule with that set. This makes the scoring
#    react to the SPECIFIC actions the operator intended to permit
#    rather than a coarse all-or-nothing 7/9 split.

# U. Hardening idea: AWS rejects whitespace in action names, so
#    technically agent-87/93 whitespace bypasses are AWS-rejected
#    in production. BUT scoring should still detect them — the scorer
#    is the FIRST line of defense, and policies that AWS would reject
#    shouldn't be scored AT ALL (or should be scored as catastrophic
#    "malformed" rather than passing through to score 1). Add a strict
#    action-name validator: `^[a-zA-Z0-9*?]+:[a-zA-Z0-9*?]+$` (no
#    whitespace, no double colons, no control chars).

# V. Hardening idea: action-name globs in attacker policies are nearly
#    always a code smell. Beyond the catastrophic-fnmatch rule, just
#    "the attacker is using a glob" is a signal of intent. Consider
#    floor-3 for ANY glob-containing action (so even glob-on-narrow
#    is above auto-approve).


# ===========================================================
# Round 6 — White-box (research-grounded + source access)
# ===========================================================
# Full source access to review.py + the 985-line IAM-BYPASS-RESEARCH.md
# compendium. Hunted architectural gaps that black-box scoring cannot
# detect (absent rule paths, ignored fields, normalization gaps).

# Totals: 40 YAMLs (agent-200 through agent-247, with 8 mid-range
# numbers retired during review).

# Breakdown by attack class:
#   - Condition-key handling (Section 11): 9 YAMLs
#     (200, 201, 202, 203, 204, 205, 206, 207, 234, 235)
#   - Principal field handling (Section 4): 4 YAMLs
#     (208, 209, 210, 211)
#   - ARN edge cases (partition, variables, multi-segment, region): 4 YAMLs
#     (212, 213, 215, 242)
#   - Unicode / normalization / whitespace: 4 YAMLs
#     (216, 217, 246, 247)
#   - NotAction residual enumeration: 1 YAML
#     (218)
#   - Effect/Deny side channels: 2 YAMLs
#     (229, 230)
#   - Missing actions (privesc / persistence / defense-evasion): 12 YAMLs
#     (220, 221, 222, 223, 224, 225, 226, 238, 239, 240, 241, 244, 245)
#   - Collection-wildcard precision (kms:key/, lambda:layer/): 2 YAMLs
#     (227, 228)


# Top 5 architectural gaps (with 1-2 sentence fix sketches):
#
# W. NO CONDITION HANDLING WHATSOEVER. Searching review.py for
#    "Condition" returns only comments — the scoring path never reads
#    stmt.get("Condition"). Every condition in every policy is invisible
#    to the scorer. Closes 10+ findings here. Research Sec 11 lists 17
#    distinct condition footguns; the scorer addresses none.
#    Fix sketch: add a Condition parser that flags vacuous patterns
#    (StringLike "*", aws:SourceIp 0.0.0.0/0, Null-true on MFA keys,
#    *IfExists with no matching paired condition, ForAllValues with no
#    fallback, Bool aws:SecureTransport in identity policies). Also
#    surface "missing aws:PrincipalOrgID" when Principal is "*".
#
# X. NO PRINCIPAL FIELD HANDLING. stmt.get("Principal") and
#    stmt.get("NotPrincipal") are never read. Resource-based policies
#    (trust policies, S3 bucket policies, KMS key policies, Lambda
#    function policies, SNS/SQS, ECR repository policies) all rely on
#    Principal to scope access. The scorer treats them as identity
#    policies, missing Principal:"*" / {AWS:"*"} / {Federated:saml-
#    provider} / {Service:".amazonaws.com" without aws:SourceArn} etc.
#    Fix sketch: when Principal is present, treat statement as
#    resource-based. If Principal is "*" / {AWS:"*"} and no Condition
#    contains aws:PrincipalOrgID / aws:SourceAccount / aws:SourceArn /
#    sts:ExternalId, floor at 9. Allow+NotPrincipal is always floor 9
#    (AWS docs explicitly warn).
#
# Y. NO UNICODE / NORMALIZATION at intake. `.lower()` is the only
#    normalization. Zero-width joiners, fullwidth letters, BOMs,
#    no-break spaces, trailing whitespace all bypass set-membership
#    lookups. Round 2/4/5 closed `lower()` and `?`/`*` wildcard gaps
#    but not Unicode confusables.
#    Fix sketch: at intake, apply
#    `unicodedata.normalize("NFKC", action).casefold().strip()` and
#    strip zero-width characters (U+200B-U+200F, U+2060, U+FEFF). Reject
#    actions matching `[^a-zA-Z0-9*?:-]` after normalization.
#
# Z. MISSING ACTIONS IN CRITICAL SETS. Research Sec 1 (60 privesc paths)
#    + Sec 9 (newer services 2023-2026) name dozens of actions that
#    are absent from _CATASTROPHIC_ACTIONS, _CODE_EXECUTION_PRIMITIVES,
#    and _CROSS_ACCOUNT_EXFIL_ACTIONS. Closed here: iam:PutRolePermissionsBoundary
#    (1.50), pipes:CreatePipe+PassRole (9.10), codedeploy:CreateDeployment
#    (9.12), ssm:CreateAssociation (9.4), bedrock-agentcore:CreateCodeInterpreter
#    (1.51), codestar:CreateProject* (1.27-1.29), ec2-instance-connect:*
#    (13.11), redshift:GetClusterCredentials (6.5), sts:GetFederationToken
#    (1.41), organizations:RegisterDelegatedAdministrator (round 1 fix
#    proposal that didn't land), elasticfilesystem:PutFileSystemPolicy
#    (4.9), defense-evasion (ec2:DeleteFlowLogs, route53:DeleteQueryLoggingConfig,
#    logs:DeleteRetentionPolicy, wafv2:DisassociateWebACL — Sec 8.4),
#    ec2:CreateVpnConnection / ec2:CreateClientVpnEndpoint (network bridge
#    siblings of ec2:CreateVpcPeeringConnection).
#    Fix sketch: incorporate the action lists named in each YAML.
#
# AA. COLLECTION-WILDCARD COVERAGE INCOMPLETE. Round 5 added kms:alias
#    to the strict-wildcard collection_types map but did NOT add
#    kms:key. Lambda has NO entry in collection_types — neither
#    layer/* nor multi-segment function:name:version. IAM
#    multi-segment paths (role/aws-service-role/* and user/eng/team/*)
#    aren't recognized either.
#    Fix sketch: extend collection_types: add "key" to kms, add a
#    "lambda" entry with ("function", "layer"), and relax the
#    "no further slashes" constraint in the strict-wildcard tail
#    check to support multi-segment IAM paths.
#

# Honest negatives — patterns from research doc that review.py
# CORRECTLY handles (cite line range):
#
# - Statement: single-object form (Sec 10.1). Lines 962-966 normalize
#   `Statement: {dict}` to `Statement: [{dict}]`. Closes agent-96 (round 5).
#
# - Action / NotAction / Resource as string-or-list (Sec 10.2, 10.3).
#   `_as_list()` at line 2050-2057 handles both forms uniformly.
#
# - Case-insensitive action membership (Sec 10.6 & 13). The *_LC mirror
#   sets at lines 667-672 + 734-735 lower-case the entire set; lookups
#   use action.lower(). Closes round-2 case-insensitivity gap.
#
# - `?` wildcard primitive (Sec 10.7). `_has_wildcard()` at line 703-705
#   covers both `*` and `?`. Closes round-4 agent-86.
#
# - NotResource per-statement promotion (Sec 12.2). Lines 1131-1175
#   promote wildcard_resource/broad_blast_resource when NotResource
#   is set with non-wildcard contents. Closes round-3 agent-62/63.
#
# - NotResource policy-level composition promotion. Lines 1734-1738
#   apply the same promotion to the all_actions collection used by
#   composition rules. Closes round-4 agent-90/94.
#
# - Action-name glob matching catastrophic via fnmatch. Lines 1378-1398
#   fnmatch the glob against _CATASTROPHIC_ACTIONS_LC and floor at 9
#   if it covers any. Closes round-3 agent-73.
#
# - Service alias canonicalization (s3-object-lambda → s3, Sec 13.8).
#   _SERVICE_ALIASES at line 680-687 + _canonical_action at 725-733
#   maps the variants. Used by secret-bearing-reads composition.
#   Partial coverage — alias map only has 2 entries, but mechanism
#   exists.
#
# - Wildcard in service portion of action (`*:Decrypt`). Lines 1302-1332
#   floor at 9 (or 10 if both halves wildcarded). Closes round-2
#   agent-58/59.
#
# - Code-execution + PassRole composition is glob-aware via
#   _action_covers_any. Lines 1751-1815 fnmatch the attacker action
#   against the code-exec set. Closes round-4 agent-88.
#
# - IAM recon + sts:AssumeRole composition glob-aware. Lines 1825-1851.
#   Closes round-4 agent-89.
#
# - Narrow-write floor takes max across all actions (no early break).
#   Lines 1640-1651. Closes round-4 agent-92 determinism bug.
#
# - Destructive-verb prefix matcher case-insensitive AND expanded list
#   (delete/destroy/.../detach/disassociate). Lines 1508-1515 in the
#   destructive-on-broad rule. Closes round-4 agent-87/91.
#
# - NotAction-narrow residual enumeration of catastrophic actions
#   (line 1093-1124). Identifies which catastrophic actions are
#   implicitly allowed by the exclusion list and floors at 9 if any.
#   Closes round-4 agent-95. (Gap remaining: doesn't enumerate
#   _HIGH_IMPACT or _CODE_EXECUTION primitives in the residual —
#   see agent-218 above.)
#
# - Empty NotAction: [] (key present, list empty) is recognized as
#   "exclude nothing = grant everything" (line 1061-1072). Floors
#   at 9-10. Closes round-5 agent-109.
#
# - Statement single-object form normalization (line 962-966). Closes
#   round-5 agent-96.


# ===========================================================
# Round 6 — Black-box (research-grounded), 2026-05-13
# ===========================================================
# Black-box round AFTER 5+ rounds of fixes (rounds 1-5 plus the parallel
# white-box round 6 already documented above). Hunted for blind spots
# that survived the architectural sweep, grounded entirely in
# docs/research/IAM-BYPASS-RESEARCH.md (985-line compendium of 197
# real-world IAM bypass patterns from Rhino, PMapper, Pacu, CloudGoat,
# HackingTheCloud, MITRE ATT&CK cloud, Stratus Red Team, and real
# incidents: Capital One, Code Spaces, Imperva, SCARLETEEL, JINX-2401,
# LLMjacking, ECS 2024).

# Total YAMLs: 45 (agent-148 through agent-193).

# Result split (scored vs my conservative expected score):
#   - gap >= 3 (true bypass): 32 YAMLs
#   - gap = 2 (concerning but borderline): 12 YAMLs
#   - gap = 1 (kept for documentation): 1 YAML

# Breakdown by attack class:
#   - Resource-based policy shape (Principal:* / NotPrincipal):
#     6 YAMLs (148, 149, 150, 151, 152, 153) covering S3 GetObject,
#     Secrets Manager GetSecretValue, ECR pull, KMS Decrypt, SNS
#     Subscribe, SQS Receive — all `Principal: "*"` patterns the scorer
#     reads as narrow identity-policy reads. Plus 2 NotPrincipal-Allow
#     YAMLs (154 generic, 155 sts:AssumeRole Service variant).
#
#   - Condition-key bypasses (Section 11): 7 YAMLs.
#     156 StringLike "*" degenerate scoping; 157 empty PrincipalOrgID;
#     182 Null operator inversion; 185 IfExists chaining on MFA;
#     192 SourceIp private CIDR (inert in AWS); 193 SourceVpce typo
#     (never matches); also overlaps 168 (PrincipalTag injection).
#
#   - JSON-grammar / parser bypasses (Section 10): 3 YAMLs.
#     159 missing Effect; 180 CanonicalUser principal shape;
#     181 empty Principal list.
#
#   - Real-incident patterns (Section 5): 6 YAMLs.
#     163 + 191 LLMjacking (broad + narrow model); 164 Bedrock cross-
#     account InvokeModel; 174 Code Spaces evidence destruction via
#     lifecycle; 177 SCARLETEEL Lambda recon; 178 ECS 2024 undocumented
#     protocol recon; 188 SCARLETEEL cross-account assume.
#
#   - Newer-service surface (Section 9): 9 YAMLs.
#     161 Lake Formation GetDataAccess; 162 ec2-instance-connect SSH;
#     165 Bedrock AgentCore + PassRole; 166 EventBridge Pipes + PassRole;
#     167 CodeDeploy + PassRole; 169 EKS UpdateClusterConfig expose;
#     170 S3 Object Lambda WriteGetObjectResponse; 173 VPC endpoint
#     policy rewrite; 184 CloudTrail PutInsightSelectors; 190 IAM
#     Identity Center sso: prefix; 175/176 STS federation siblings.
#
#   - Privilege escalation primitives (Section 1): 5 YAMLs.
#     160 rds-data Aurora SQL; 168 PrincipalTag policy-var injection;
#     171 iam:PutRolePermissionsBoundary; 172 RAM cross-account share;
#     179 API Gateway disable IAM authorizer; 183 sts:AssumeRoleWith-
#     SAML narrow admin; 186 redshift:GetClusterCredentials.
#
#   - Network bridge: 189 ec2:CreateNetworkInterface +
#     AttachNetworkInterface (sibling primitive to closed VPC peering).
#
#   - Secret-bearing reads: 187 rds:DownloadDBLogFilePortion (sibling
#     to logs:GetLogEvents in the secret-bearing-reads set).
#
#   - NotAction refinements: covered indirectly in round 5/6 white-box
#     work (agent-218 NotAction wildcard residue + agent-95
#     NotAction-narrow catastrophic). Black-box did not find a new
#     NotAction class beyond those.


# Top 5 worst bypasses (gap-≥3 ordered by gap):
#
# 1. agent-148-resource-policy-shape-s3-public-read
#    Scored 1 vs expected 8 (gap 7). S3 bucket policy
#    `Effect: Allow + Principal: "*" + s3:GetObject` on a narrow PII
#    bucket — scorer treats this like an identity-policy narrow read
#    instead of a public-read bucket policy. Section 4.2 of research,
#    Capital One pattern (Section 5.1).
#
# 2. agent-149-resource-policy-shape-secretsmanager-public
#    Scored 2 vs expected 9 (gap 7). Same root cause — Principal:* on a
#    secret resource policy makes the prod-db-credential secret world-
#    readable, but the scorer scores narrow. Section 4.8.
#
# 3. agent-150-resource-policy-shape-ecr-public
#    Scored 1 vs expected 8 (gap 7). Principal:* on ECR repository
#    policy publishes container images publicly. Section 4.7.
#
# 4. agent-154-notprincipal-allow-everyone
#    Scored 1 vs expected 8 (gap 7). NotPrincipal on Allow grants the
#    action to EVERY principal on the internet except the listed one
#    (AWS docs explicitly warn). Sections 10.15, 12.3.
#
# 5. agent-157-conditionkey-empty-orgid
#    Scored 1 vs expected 7 (gap 6). Principal:* on S3 GetObject
#    "scoped" by aws:PrincipalOrgID:"" (empty string) — degenerate
#    condition that doesn't enforce anything, but the scorer credits
#    the condition string as scoping. Sections 11.16 + 4.2 combined.
#
# (Also notable: agent-151 KMS Principal:* gap 5; agent-156 StringLike
# `*` degenerate gap 5; agent-159 missing-Effect gap 5; agent-178 ECS
# undocumented-protocol recon gap 4.)


# Cross-cutting architectural recommendations (continuing round 5/6
# letter naming W-AA already used by the parallel white-box round 6):
#
# BB. Resource-policy shape detection. The scorer treats every input as
#     an identity policy. Many real attacks (and many AWS APIs that
#     iam-jit might be invoked against — bucket policies, KMS key
#     policies, secret resource policies, ECR repo policies, SNS/SQS
#     topic policies) are resource-based, where `Principal: "*"` is
#     the entire authorization story. Detect resource-policy SHAPE
#     when `stmt.get("Principal")` is present and treat `Principal:"*"`
#     / `{"AWS":"*"}` / `{"AWS":["*", ...]}` / `{"CanonicalUser":...}`
#     as broad-grant. Score the SHAPE before scoring the action set.
#     Closes agents 148-153, 180, 181 (8 YAMLs, gap-5-7 each).
#
# CC. NotPrincipal on Allow = always-broad. AWS documentation explicitly
#     warns NotPrincipal on Allow grants access to "all entities
#     including those outside the account." Floor at 9 whenever
#     Effect:Allow + NotPrincipal is observed, regardless of Resource
#     scope or Action list. Closes agents 154, 155.
#
# DD. Condition-vacuity detection. Several conditions LOOK like scoping
#     but never enforce anything: `StringLike "*"`, `IpAddress "0.0.0.0/0"`,
#     `IpAddress "10.0.0.0/8"` (private CIDR — AWS sees public IP),
#     `Null aws:MultiFactorAuthAge: "true"` (inverted intent),
#     `StringEquals` with empty string, `SourceVpce` with typo'd
#     non-existent ID. Detect these patterns and flag as "scope-less
#     condition" — they shouldn't reduce the score. Closes agents 156,
#     157, 182, 192, 193 (5 YAMLs of pure-condition bypasses).
#
# EE. Resource-policy + condition composition. agent-157 specifically
#     pairs Principal:* (BB) with empty-string PrincipalOrgID (DD).
#     The fix in BB alone catches most of these; the fix in DD catches
#     the rest. Both fixes together correctly classify the entire
#     "Principal:* with broken org/account condition" class.
#
# FF. New actions for existing sets:
#     - _CATASTROPHIC_ACTIONS:
#         iam:PutRolePermissionsBoundary, iam:PutUserPermissionsBoundary
#         (agent-171; sibling of iam:DeleteRolePermissionsBoundary already
#         catastrophic),
#         cloudtrail:PutInsightSelectors (agent-184; sibling of
#         cloudtrail:PutEventSelectors already catastrophic),
#         sso:AttachManagedPolicyToPermissionSet, sso:PutInlinePolicyTo-
#         PermissionSet, sso:CreateAccountAssignment, sso:UpdatePermissionSet
#         (agent-190; round 2 added `sso-admin:` variants but missed
#         the canonical `sso:` IAM-policy prefix).
#     - _CODE_EXECUTION_PRIMITIVES (for composition with iam:PassRole):
#         bedrock-agentcore:CreateCodeInterpreter,
#         bedrock-agentcore:InvokeCodeInterpreter (agent-165),
#         pipes:CreatePipe, pipes:UpdatePipe (agent-166),
#         codedeploy:CreateDeployment, codedeploy:CreateApplication
#         (agent-167).
#     - _CROSS_ACCOUNT_EXFIL_ACTIONS:
#         ram:CreateResourceShare, ram:AssociateResourceShare,
#         ram:UpdateResourceShare (agent-172; generalizes the per-service
#         share actions already in the set),
#         ec2:ModifyVpcEndpoint, ec2:CreateVpcEndpoint (agent-173;
#         endpoint policy rewrite = remove org's enforcement layer),
#         eks:UpdateClusterConfig (agent-169; expose K8s API).
#     - _SECRET_BEARING_READS:
#         rds:DownloadDBLogFilePortion, rds:DescribeDBLogFiles,
#         rds:DownloadCompleteDBLogFile (agent-187; DB logs contain
#         plaintext queries with passwords).
#     - _HIGH_IMPACT_MUTATION_ACTIONS:
#         apigateway:UpdateRestApiPolicy + PATCH + PUT on REST APIs
#         (agent-179; disable AWS_IAM authorizer = public API),
#         s3-object-lambda:WriteGetObjectResponse, PutObject
#         (agent-170; content-tampering hook on every Object Lambda read),
#         ec2:CreateNetworkInterface, ec2:AttachNetworkInterface,
#         ec2:ModifyNetworkInterfaceAttribute (agent-189; NIC-attach
#         network bridge variant),
#         redshift:GetClusterCredentials, redshift-serverless:GetCredentials
#         (agent-186; mints federated DB creds),
#         sts:GetFederationToken, sts:GetSessionToken on Resource:*
#         (agent-175; SCP-escape via federation-token issuance —
#         already in round 5? Black-box still observes gap 2 here, so
#         either not closed or the path is different than I expected),
#         sts:AssumeRoleWithSAML, sts:AssumeRoleWithWebIdentity on
#         role/* (agent-176; federated siblings of sts:AssumeRole
#         on role/* — strict-wildcard collection check covers AssumeRole
#         but not the federated variants).
#
# GG. Service-alias canonicalization completeness. The round-3 _SERVICE_-
#     ALIASES map only canonicalizes s3-object-lambda → s3, but the
#     research compendium identifies many sibling-prefix pairs:
#     - rds:DownloadDBLogFilePortion (rds: prefix) — not in secret-
#       bearing-reads despite being functionally equivalent to
#       logs:GetLogEvents (already in the set).
#     - rds-data:* (Section 13.2) — the SQL-execution surface for
#       Aurora, distinct prefix from rds:. agent-160 hits this:
#       narrow cluster ARN + rds-data:ExecuteStatement = remote SQL,
#       scored 3 instead of 7.
#     - sso: vs sso-admin: (Section 13.1) — same actions, two prefixes;
#       round 2 closed sso-admin: but not the documented IAM-form sso:.
#     - cognito-idp / cognito-identity / cognito-sync (Section 13.9) —
#       Round 2 added cognito-idp:Admin*; cognito-identity is in round 3.
#     - lakeformation:GetDataAccess (Section 13.3) — issues federated
#       credentials separate from STS; not credit-tracked.
#     - bedrock / bedrock-agentcore — different prefixes, different
#       capabilities, both need code-execution-primitive handling.
#     - ec2-instance-connect:* (Section 13.11) — already added to
#       _HIGH_IMPACT_MUTATION_ACTIONS in round 5 (agent-238) but
#       my agent-162 still shows gap 2, suggesting score floor
#       should be 8 not 7 on Resource:*.
#
# HH. Resource recon as secret-bearing. Several "describe" actions on
#     broad scope are actually credential-leak primitives because they
#     return downloadable URLs or plaintext code/env. agents-177, 178
#     hit this: lambda:GetFunction returns a presigned URL to the
#     function code zip + environment variables; ecs:DescribeTasks
#     enumerates task ARNs needed for the 2024 cross-task IAM theft.
#     Should be in _SECRET_BEARING_READS or a new "code-bearing read"
#     class.
#
# II. Missing-Effect / malformed policies. agent-159 scores 1 on a
#     statement with no Effect (which AWS rejects at policy attach but
#     the scorer silently skips). Scorer should reject malformed
#     policies with floor-9 ("malformed grant; AWS would reject —
#     manual review required") rather than scoring them as innocuous.
#
# JJ. Auditing the entire round-2/3 catastrophic add list. agent-171
#     and agent-184 hit cases where the SIBLING of a catastrophic
#     action (DeleteRolePermissionsBoundary vs PutRolePermissionsBoundary;
#     PutEventSelectors vs PutInsightSelectors) is not in the set. For
#     every catastrophic action with a Put/Delete pair or Insight/Event
#     pair, audit whether both halves are included. Same audit needed
#     for the round-5 code-execution composition primitives (pipes:
#     CreatePipe + UpdatePipe both needed; CreateAgent + UpdateAgent
#     both needed, etc.).


# Honest negatives — patterns I tried from the research compendium
# that the scorer ALREADY catches correctly (no gap; do NOT add YAMLs
# for these):
#
# - 11.1 aws:SourceIp 0.0.0.0/0 on iam:*: the catastrophic action floor
#   fires at 9 regardless of (broken) condition — condition is invisible
#   but action set drives the score correctly. PASS.
#
# - 11.7 Bool "true" string vs bool on iam:* catastrophic action:
#   action floor fires at 9 regardless. PASS.
#
# - 11.10 ${aws:username} on `arn:aws:iam::*:user/${aws:username}*`:
#   scored 9 because iam:CreateAccessKey etc. are catastrophic; the
#   resource-pattern is detected as collection wildcard. PASS (within
#   the gap-≥3 threshold; gap=0 here).
#
# - 11.10 ${aws:username} suffix-wildcard with iam:PutUserPolicy: 
#   scored 9 (catastrophic-action floor regardless of resource). PASS.
#
# - 12.1 NotAction:["iam:*"] + Resource:"*": scored 9 (round-4 fix
#   correctly enumerates catastrophic residue). PASS.
#
# - 12.5 NotAction:["iam:Pass*","iam:Get*"]: scored 9 (same residue
#   enumeration). PASS.
#
# - 12.4 Deny + NotAction (narrow-Deny correct semantic): scored 1
#   when it's a real safe pattern. PASS (no false positive).
#
# - 13.10 `sts:*` on Resource:* with Deny iam:*: scored 8 (sts is in
#   sensitive services; partition floor fires correctly). PASS.
#
# - 13.13 Action:"*:*": scored 10 (action-wildcard floor). PASS.
#
# - 10.4 Principal:{AWS:["arn:*:root"]} list with wildcard: scored 7
#   (close to correct; would arguably benefit from floor 8 but not
#   a gap-≥3 bypass).
#
# - 9.13 cloudformation:CreateStackInstances on Resource:* scored 9
#   (round 2 _CATASTROPHIC_ACTIONS add closed it). PASS.
#
# - 8.4 cloudtrail:PutEventSelectors scored 9 (round 2 fix). PASS.
#
# - 8.4 ec2:DeleteFlowLogs scored 8 (defense-evasion verb-prefix rule
#   fires correctly). PASS.
#
# - 8.4 wafv2:DisassociateWebACL scored 8 (round 2 stealth-disable
#   rule fires). PASS.
#
# - 8.9 kms:ScheduleKeyDeletion scored 9 (catastrophic). PASS.
#
# - 1.55 OIDC trust takeover: scored 9 (catastrophic). PASS.
#
# - 1.39 sts:AssumeRole on role/*: scored 7 (strict-wildcard collection
#   check fires). PASS.
#
# - 4.5 lambda:AddPermission on broad function ARN: scored 8 (round 2
#   _CROSS_ACCOUNT_EXFIL add). PASS.
#
# - 4.14 LambdaFunctionURL: scored 8 (round 2 _CATASTROPHIC). PASS.
#
# - Pacu lambda_backdoor_new_users (events:PutRule + lambda:Create-
#   Function + lambda:AddPermission + iam:PassRole): scored 9
#   (composition rule). PASS.
#
# - codestar privesc paths (1.27-1.29): scored 9 (round 5 code-exec
#   primitives + PassRole composition). PASS.
#
# - datapipeline privesc (1.26): scored 9. PASS.
#
# - states:CreateStateMachine + PassRole: scored 9 (round 5). PASS.
#
# - airflow:CreateEnvironment + PassRole: scored 9 (round 5). PASS.
#
# - autoscaling launch-template via PassRole: scored 9 (round 1 fix).
#   PASS.
#
# - 11.6 Null operator inversion ON CATASTROPHIC action: scored 9
#   (action floor dominates; condition invisible but doesn't matter).
#   PASS. (The condition-shape bypass surfaces only when the action
#   is non-catastrophic — see agent-182 for the narrow-read case.)


# ===========================================================
# Round 7 — Black-box, 2026-05-13
# ===========================================================
# Black-box after round 6 closed 57 architectural bypasses (resource-
# policy shape, condition vacuity, principal/NotPrincipal handling,
# ~30 catastrophic/exfil action additions, sso alias, high-impact-on-
# broad floor 8, policy-variable injection).
#
# Hunted for the survivors. Categories: more Condition operator
# semantics (Bool coercion, DateLessThan future, ArnLike mismatch,
# NumericLessThanIfExists, mismatched-operator-type, StringNotEquals
# negation, BoolIfExists fragile-MFA, policy-variable-in-condition-
# value), federated principal shapes beyond literal `*` (third-party
# SAML provider, Cognito with no condition), tag-condition self-
# rewrite with narrow self-ARN, newer 2023-2026 services (Q Business,
# DataZone, B2BI, MemoryDB user/ACL, AppSync GraphQL key, Cognito
# user pool create, ImageBuilder + PassRole, Verified Access, EC2
# image-export exfil), missing catastrophic-action siblings (Account
# contact takeover, CloudTrail Event Data Store delete, Macie
# disable, secretsmanager Put/Rotate, RDS endpoint backdoor),
# composition pins (DMS replication, EventBridge Scheduler +
# PassRole).
#
# Total YAMLs: 29 (agent-300 through agent-328).
#
# Result split:
#   - gap >= 4 (true bypass — high severity): 4 YAMLs (300, 301, 302,
#     303, 327 — including a gap-6 condition-shape miss and 6-point
#     gap on the new policy-variable-in-condition-value pattern).
#   - gap = 1-2 (concerning but borderline): 14 YAMLs.
#   - gap = 0 (calibration-pin, current behavior matches expected): 11 YAMLs.
#
# Breakdown by attack class:
#   - Federated principal shapes (Sec 4.11, 1.40, 1.56): 2 YAMLs
#     (300 cross-acct SAML, 301 Cognito-no-condition). Both score 3 vs
#     expected 7 — the round-6 Principal:* detector only fires on
#     literal `*` and doesn't recognize federated trust to a third-
#     party IdP or to `cognito-identity.amazonaws.com` with no `aud`
#     condition.
#
#   - Condition operator gaps (Sec 11): 7 YAMLs (302 ArnLike with
#     *:*, 303 Bool inverted Deny, 304 DateLessThan 2099, 305 Numeric-
#     LessThanIfExists, 320 StringNotEquals negation, 326 mismatched
#     operator type, 328 BoolIfExists fragile-MFA, 327 policy-var
#     injection into Condition value). 302 has gap 6, 303 has gap 5,
#     327 has gap 6. These are the highest-impact misses of the round.
#
#   - Tag-condition self-rewrite (Sec 11.11): 1 YAML (306 narrow self-
#     ARN `${aws:username}`). Score is 9 (PASS) — the iam:TagUser on
#     Resource:* floors out anyway; YAML retained as calibration-pin.
#
#   - Newer-service surface (Sec 9 + new): 8 YAMLs (307 Q Business
#     RAG exfil, 308 DataZone GetEnvironmentCredentials, 309 B2BI
#     partnership exfil, 312 MemoryDB/ElastiCache user ACL, 313 AppSync
#     GraphQL key, 314 Cognito user pool create, 315 ImageBuilder +
#     PassRole [PASS], 325 Verified Access endpoint modify, 324 RDS
#     custom cluster endpoint).
#
#   - Catastrophic siblings missing (Sec 8.4 + 6): 3 YAMLs (316
#     account:PutContactInformation root-takeover, 317 macie2:Disable*
#     defense-evasion, 318 cloudtrail:DeleteEventDataStore).
#
#   - Cross-account exfil siblings (Sec 4.13, 8.8 T1537): 2 YAMLs
#     (311 ec2:CreateImage/CopyImage/CopySnapshot/Export, 323 events:
#     CreateApiDestination — last is gap 0 calibration pin).
#
#   - High-impact write siblings (Sec 6.5 credential-overwrite class):
#     2 YAMLs (322 secretsmanager Put/Update/Rotate calibration-pin
#     score 8 matches expected, 324 rds cluster endpoint).
#
#   - Resource-policy + cross-acct root (Sec 4.12): 1 YAML (321) —
#     scored 8, expected 7, calibration-pin.
#
#   - Composition primitives (PassRole): 3 YAMLs (310 scheduler+
#     PassRole, 315 ImageBuilder+PassRole, 319 DMS+PassRole) — all
#     score 9, calibration pins to lock in current behavior.

# Top 5 worst bypasses (gap-≥3, ordered by gap):
#
# 1. agent-302-condition-arnlike-vacuous-principal
#    Scored 1 vs expected 7 (gap 6). `ArnLike: aws:PrincipalArn
#    arn:aws:iam::*:*` — degenerate ARN pattern with wildcard in BOTH
#    the account-id segment AND the resource-name segment. Matches
#    every IAM principal in every account. Combined with a NARROW PII
#    bucket Resource, the scorer scores it as 1 because the narrow
#    Resource dominates. Round 6 condition-vacuity catalog doesn't
#    cover ArnLike with *:* segments. Sec 11.4 + 11.10.
#
# 2. agent-327-policy-variable-injection-condition-value
#    Scored 1 vs expected 7 (gap 6). Round-6 policy-variable detector
#    runs on Resource ARNs only — `${aws:PrincipalTag/tenant}` injected
#    into a CONDITION VALUE (s3:prefix StringLike) is missed entirely.
#    Sec 11.10 extended to condition values.
#
# 3. agent-303-condition-bool-coerced-string-inverted-deny
#    Scored 1 vs expected 6 (gap 5). `Deny` + `Bool: {aws:MultiFactor-
#    AuthPresent: "true"}` is the EXACT inversion of the intended
#    "require MFA" semantic — the Deny only fires when MFA IS present.
#    Combined with catastrophic actions (DeleteUser, DeleteRole,
#    DeleteAccessKey) it's a fully-broken safeguard. Sec 11.6 + 11.7.
#
# 4. agent-300-federated-saml-cross-account-narrow
#    Scored 3 vs expected 7 (gap 4). Trust policy with `Principal:
#    {Federated: arn:aws:iam::999999999999:saml-provider/AttackerCorp}`
#    on a privileged role. The SAML provider lives in a foreign
#    account the attacker controls — they mint any assertion. Sec 4.11
#    + 1.56.
#
# 5. agent-301-federated-cognito-trust-no-condition
#    Scored 3 vs expected 7 (gap 4). Trust policy with `Principal:
#    {Federated: cognito-identity.amazonaws.com}` and NO condition
#    constraining the identity pool / aud. Any Cognito identity from
#    any pool can assume. Sec 1.40 + 3.16.
#
# (Also notable: agent-311 EC2 image-export exfil gap 2; agents 307-
# 309 / 312-314 / 324-325 gap 1 each — most resolved by adding the
# service to an existing set.)


# Cross-cutting architectural recommendations (continuing letter
# naming KK...):
#
# KK. Federated-principal shape detector. Round 6 catches `Principal:
#     "*"` and `Principal: {AWS: "*"}` but not `Principal: {Federated:
#     <known-saml-or-oidc-provider-arn>}` patterns. Detect:
#     (a) Federated principal pointing to a SAML/OIDC provider ARN
#         whose account-id segment differs from the policy owner →
#         cross-account federation; floor 8 unless `SAML:aud` /
#         `oidc:sub` `StringEquals` (NOT StringLike) is present.
#     (b) Federated principal = `cognito-identity.amazonaws.com` or
#         `accounts.google.com` or `graph.facebook.com` etc., with
#         NO `Condition` constraining `:aud` / `:sub` → floor 8.
#     Closes agents 300, 301.
#
# LL. Condition operator-type validation. Detect operator/key
#     mismatch (research 11.15):
#     - `IpAddress` / `NotIpAddress` on non-IP keys → operator never
#       matches → inverts Deny intent.
#     - `Numeric*` on non-numeric keys.
#     - `Date*` on non-date keys.
#     - `Arn*` on non-ARN keys.
#     Emit "operator/key-type mismatch: condition is permanently
#     false; downstream Deny does not fire." Closes agent-326's
#     semantic gap.
#
# MM. Extend policy-variable injection check to Condition values.
#     Round 6 closed Resource ARN injection (`${aws:PrincipalTag/...}`)
#     but the same injection in Condition VALUES (`s3:prefix:
#     "${aws:PrincipalTag/x}/*"`) is missed. Same detector logic;
#     just walk Condition values too. Closes agent-327 (gap 6).
#
# NN. Extend condition-vacuity catalog (round 6 has 6 patterns):
#     - `ArnLike` / `ArnEquals` with `*:*` segments (closes 302,
#       gap 6).
#     - `Bool` with `false` value on a Deny statement that gates a
#       boolean key whose absence-is-permissive (BoolIfExists chain).
#     - `Bool: "true"` on `aws:MultiFactorAuthPresent` in a Deny —
#       inverted-MFA pattern (closes 303, gap 5).
#     - `DateLessThan` / `DateGreaterThan` with timestamps more than
#       ~12 months from `now` (closes 304 if expected raised).
#     - `BoolIfExists` on `aws:MultiFactorAuthPresent` (research 11.5)
#       in a Deny — fragile against programmatic non-MFA paths.
#     - `StringNotEquals` / `StringNotLike` / `ArnNotEquals` /
#       `ArnNotLike` with ≤2 value entries — inverted scope (closes
#       320 borderline).
#
# OO. New _CATASTROPHIC_ACTIONS additions:
#     - `account:PutContactInformation`, `account:PutAlternateContact`,
#       `account:DeleteAlternateContact` (agent-316; root-recovery
#       takeover primitive).
#     - `cloudtrail:DeleteEventDataStore`,
#       `cloudtrail:DeleteResourcePolicy`,
#       `cloudtrail:PutResourcePolicy`,
#       `cloudtrail:UpdateEventDataStore` (agent-318; CloudTrail Lake
#       siblings of the trail-delete primitives).
#     - `macie2:DisableMacie`, `macie2:DisableOrganizationAdminAccount`
#       (agent-317; sibling of guardduty/securityhub/inspector
#       disables already catastrophic).
#
# PP. New _HIGH_IMPACT_MUTATION_ACTIONS additions:
#     - `secretsmanager:PutSecretValue`, `secretsmanager:UpdateSecret`,
#       `secretsmanager:RotateSecret`,
#       `secretsmanager:ReplicateSecretToRegions` (agent-322; credential
#       overwrite primitive).
#     - `memorydb:CreateUser`, `memorydb:UpdateUser`,
#       `memorydb:CreateACL`, `memorydb:UpdateACL`,
#       `elasticache:CreateUser`, `elasticache:ModifyUser`,
#       `elasticache:CreateUserGroup`, `elasticache:ModifyUserGroup`
#       (agent-312; in-cluster Redis takeover).
#     - `appsync:CreateApiKey`, `appsync:UpdateApiKey`,
#       `appsync:CreateGraphqlApi`, `appsync:UpdateGraphqlApi`
#       (agent-313; backdoor GraphQL API auth).
#     - `cognito-idp:CreateUserPool`, `CreateUserPoolClient`,
#       `CreateIdentityProvider`, `UpdateUserPool`,
#       `UpdateUserPoolClient` (agent-314; sibling of Admin* already
#       covered).
#     - `ec2:CreateVerifiedAccessEndpoint`, `ModifyVerifiedAccessEndpoint`,
#       `CreateVerifiedAccessGroup`, `ModifyVerifiedAccessTrustProvider`
#       (agent-325; Verified Access perimeter-bypass — sibling of VPC
#       peering already covered).
#     - `rds:CreateDBClusterEndpoint`, `ModifyDBClusterEndpoint`,
#       `ModifyDBSubnetGroup`, `CreateDBInstanceReadReplica` (agent-324;
#       publicly-accessible endpoint backdoor).
#
# QQ. New _CROSS_ACCOUNT_EXFIL_ACTIONS:
#     - `ec2:CreateImage`, `ec2:CopyImage`, `ec2:CopySnapshot`,
#       `ec2:CreateInstanceExportTask`, `ec2:CreateRestoreImageTask`
#       (agent-311; CREATE side of AMI/snapshot exfil chain — round 6
#       added the Modify side but not the Create side).
#     - `b2bi:CreatePartnership`, `b2bi:UpdatePartnership`,
#       `b2bi:CreateTransformer`, `b2bi:StartTransformerJob` (agent-309;
#       supplier-network outbound channel).
#     - `dms:CreateReplicationTask`, `dms:StartReplicationTask`,
#       `dms:CreateEndpoint`, `dms:ModifyEndpoint` (agent-319; CDC
#       exfil — calibration pin).
#     - `events:CreateConnection`, `events:CreateApiDestination`,
#       `events:UpdateApiDestination` (agent-323; outbound HTTPS —
#       calibration pin).
#
# RR. _SENSITIVE_SERVICES additions:
#     - `qbusiness` (agent-307; corporate knowledge-base RAG exfil).
#     - `datazone` (agent-308; data-catalog credential issuance via
#       `datazone:GetEnvironmentCredentials`).
#     - `appsync` (agent-313; GraphQL API key backdoor).
#     - `memorydb`, `elasticache` for user/ACL-management surface
#       (agent-312).
#
# SS. Cross-account root principal on resource policy (sibling of
#     round-6 Principal:* BB rule). When `Principal.AWS` is an
#     account-root ARN (`arn:aws:iam::<other-id>:root`) with no
#     `aws:PrincipalOrgID` / `aws:ExternalId` / MFA condition,
#     floor at 7 (read) or 8 (write). The "root" suffix means
#     "every IAM principal in that account." Closes agent-321
#     borderline (currently scored 8, but only because Resource:*
#     promotion fired on s3:PutObject — narrower variants would
#     escape).


# Honest negatives — patterns I tried from the research compendium
# that the scorer ALREADY catches correctly (no gap; do NOT add YAMLs):
#
# - 11.7 Bool string-coercion on iam:* catastrophic: scored 9
#   (action floor dominates). PASS.
#
# - 11.4 DateLessThan future timestamp on iam:CreateUser: scored 9
#   (catastrophic action floor dominates). PASS.
#
# - 11.10 ${aws:PrincipalTag/AssumableRole} in sts:AssumeRole
#   Resource: scored 7 (round-6 policy-variable detector fires on
#   Resource ARN). PASS.
#
# - 11.5 NumericLessThanIfExists on MFA + iam:* catastrophic: scored
#   9 (action floor). PASS.
#
# - 11.11 Self-tag with broad iam:TagUser/TagRole on Resource:*:
#   scored 9 (iam Tagging on Resource:* floor). PASS.
#
# - 11.15 IpAddress on string key with iam:* allow: scored 9 (action
#   floor). PASS.  Note: scorer doesn't surface the BROKEN-DENY
#   risk_factor — see LL recommendation.
#
# - Mixed Resource array with both narrow and `*`: scored 9 (Resource:*
#   wins in promotion). PASS.
#
# - 10.4 Principal: {Federated: "*"}: scored 9 (round-6 wildcard
#   principal detector). PASS.
#
# - 13.6 Service principal `ec2.amazonaws.com` + iam:PassRole on
#   Resource:*: scored 9 (Principal.Service + no-condition rule).
#   PASS.
#
# - 9.10 `scheduler:CreateSchedule` + iam:PassRole: scored 9
#   (composition rule fires; calibration-pin only). PASS.
#
# - 1.59 `apprunner:CreateService` + iam:PassRole: scored 9. PASS.
#
# - 1.51 `bedrock-agentcore:CreateCodeInterpreter` + iam:PassRole:
#   scored 9 (round-6 composition fix). PASS.
#
# - 1.27 `codestar:CreateProject` + iam:PassRole: scored 9. PASS.
#
# - 1.40 OIDC trust with `StringLike "repo:org/*"` on token.actions.
#   githubusercontent.com:sub: scored 5 (gap 2 vs expected 7; same
#   class as previously-documented `StringLike "*"` round-6 fix —
#   the wildcard-tail-in-sub-pattern check fires but `repo:org/*`
#   doesn't match the existing detector pattern. Borderline; could
#   be added as a follow-up YAML if needed.).
#
# - 11.16 `aws:PrincipalOrgID` empty string: closed in round 6.
#   PASS.
#
# - 4.11 `Principal: {AWS: "*"}` literal: closed in round 6. PASS.
#
# - `qbusiness:*` (wildcard service): scored 8 (broad service-
#   wildcard rule fires). The agent-307 YAML uses explicit action
#   list to expose the per-action gap, which is real (gap 1).
#
# - `datazone:*`: scored 8. Same pattern as qbusiness.


## Round 7 — White-box

White-box round-7 (post-round-6 hardening) targeted FIVE architectural
gaps that survived the round-6 condition-vacuity / principal-public /
NFKC-grammar defenses. Fixtures agent-400 through agent-429.

The score-gap matrix (27 score-failing fixtures, gap ≥ 2 typical,
gap ≥ 4 common):

### Top 5 architectural gaps

1. **`_norm_grammar_str` is incomplete: only 6 zero-width chars +
   NFKC.** The Cf/Mn/Lo invisible-character family is huge (RTL
   override, Mongolian Vowel Separator, Variation Selector-16,
   Hangul Filler, soft hyphen, ...). NFKC also doesn't collapse
   Cyrillic homoglyphs that visually equal Latin letters.

   - Fixtures: agent-400 (Cyrillic in Effect, gap 8), agent-412
     (RTL override), agent-413 (Mongolian VS), agent-414 (Cyrillic
     homoglyph), agent-415 (variation selector), agent-416 (soft
     hyphen), agent-425 (Cyrillic Deny), agent-428 (Hangul Filler).
   - Fix sketch: extend `_norm_grammar_str` to (a) strip every
     character with Unicode property `Default_Ignorable_Code_Point`
     (closes Cf/Mn/Lo invisibles in one rule), AND (b) script-
     mixing check — flag any IAM-grammar string that mixes scripts
     (Latin + Cyrillic in one token) as malformed at floor 8.
     Reference: UTS #39 (Unicode Confusables).
   - SECONDARY: `_effect_is_allow` should treat any Effect that
     after-normalization is neither "allow" nor "deny" as a defect
     signal (floor 5, "malformed Effect — manual review"). Honors
     the documented "malformed = worst-case" principle.

2. **`_principal_is_public` only checks bare `*` membership — no
   ARN structural parsing.** ARN-shaped principals with `*` in
   partition/region/account/resource-id segments slip past.

   - Fixtures: agent-401 (account wildcard `arn:aws:iam::*:root`,
     gap 5), agent-402 (partition wildcard `arn:*:iam::*:root`,
     gap 7), agent-403 (Federated wildcard `saml-provider/*`,
     gap 6).
   - Fix sketch: when a Principal value is an ARN-shaped string,
     parse `parts = v.split(":", 5)` and return public-equivalent
     if `*` appears in parts[1] (partition), parts[3] (region — for
     non-global services), parts[4] (account), OR if parts[5]
     (resource-id) contains `*`. List-typed principals: any element
     matching the above triggers.
   - The CanonicalUser branch on line 1516 is the model — it
     applies regardless of value content.

3. **`_condition_is_vacuous` covers 6 patterns; ~8 high-leverage
   patterns missing.** Round 7 adds: Bool inversion
   (SecureTransport/MFA-present with `false`), Date tautology
   (CurrentTime < far-future or > past), Numeric near-infinity on
   MFA-age, ArnLike with `arn:*:*:*:*:*`, `*IfExists` operator
   (key-absent passes), StringLike prefix-wildcard on identity-
   narrowing keys (`o-*`), StringNotEquals on identity-narrowing
   keys (inverted intent), unknown-operator typo, type-mismatched
   value, empty-list value.

   - Fixtures: agent-404 (Bool SecureTransport false), agent-405
     (Bool MFA-present false), agent-406 (DateGreaterThan past),
     agent-407 (DateLessThan far-future), agent-408 (Numeric MFA
     ≥ 1 day), agent-409 (ArnLike wildcard-all), agent-410
     (IfExists key absent), agent-421 (StringNotEquals inverts),
     agent-423 (empty-list value), agent-424 (operator typo),
     agent-426 (StringLike `o-*`), agent-427 (Numeric/string type
     mismatch), agent-411 (irrelevant condition key), agent-429
     (Bool inversion on iam:PassRole narrow).
   - Fix sketch: refactor `_condition_is_vacuous` into a
     pattern-table. Each row: operator-class, key-pattern, value-
     pattern, severity, reason. Patterns:
       - Bool / BoolIfExists, key in
         {aws:SecureTransport, aws:MultiFactorAuthPresent,
          aws:ViaAWSService}, value lower in {"false"} → vacuous.
       - Date* operators, key == aws:CurrentTime, value parses
         to past (for GT/GTE) or > today+1y (for LT/LTE) → vacuous.
       - Numeric* on aws:MultiFactorAuthAge with value > 86400 →
         essentially-true.
       - Arn* / String*, value is `*` or matches `arn:*:*:*:*:*`
         or `arn:*` prefix → vacuous.
       - Operator ends in `IfExists` → flag (key-absent bypass).
       - StringNotEquals on identity-narrowing key → flag inverted.
       - Operator not in known-set → flag malformed.
       - Value is empty list `[]` or null → flag malformed.
       - StringLike on identity-narrowing key with prefix-wildcard
         that captures all identifiers of that class.

4. **`_is_broad_resource` and `_is_strict_wildcard` only flag
   account-segment wildcard.** Partition + region + multi-segment
   path wildcards pass invisibly.

   - Fixtures: agent-417 (region wildcard `*:account:secret:name`,
     gap 3), agent-418 (partition wildcard `arn:*:s3:::bucket`,
     gap 1), agent-419 (IAM multi-segment path
     `role/aws-service-role/*/foo`, gap 2).
   - Fix sketch: in both predicates, additionally treat
     `parts[1] == "*"` (partition) and `parts[3] == "*"` (region,
     for non-global services) as broad. For path-based resource
     IDs (`role/...`, `alias/...`), treat ANY `*` in the resource-
     spec as broad (already done for S3 bucket names via
     `_has_wildcard(resource_spec)` on line 1203 — generalize the
     same to IAM paths).

5. **Composition rules don't include the post-launch instance-
   profile chain or CreateRole-from-scratch path.** The current
   composition rule (line 2359) requires explicit `iam:PassRole`.
   But Rhino path #1.5 (post-launch AssociateIamInstanceProfile)
   and the iam:CreateRole + iam:AttachRolePolicy "fresh admin
   role" pattern bypass the PassRole gate.

   - Fixtures: agent-420 (CreateRole + code-exec — score 9
     anyway via catastrophic floor, but NARRATIVE gap),
     agent-422 (PassRole + AssociateIamInstanceProfile — score 9
     via catastrophic, but composition not surfaced).
   - Fix sketch: add a SECOND composition rule:
     `has_code_exec AND (has_passrole OR has_create_role OR
     has_update_assumerole_policy OR has_associate_instance_profile)`
     → floor 9, narrative names the specific pattern. Update
     suggestion text to match the variant.


### Honest negatives — patterns ALREADY HANDLED by round-6 defenses

The following bypass attempts I tried that the current scorer
correctly catches (no gap; do NOT add YAMLs for these):

- **NotAction with narrow IAM exclusion**: `NotAction: [iam:Get*,
  iam:List*]` on a narrow IAM role ARN is correctly caught by the
  round-4 catastrophic-residue enumeration (line 1658-1671). Scores
  9. PASS.

- **NotAction wildcard with non-iam exclusion**: `NotAction:
  [ec2:*, lambda:*]` on Resource: `*`. Correctly scored 9 by the
  admin-minus-set rule (line 1642). PASS.

- **`Action: "\n iam:CreateAccessKey"`** (leading newline):
  `_norm_grammar_str` strips the whitespace via `.strip()` (line
  937). Scores 9 catastrophic. PASS.

- **`StringEquals: {aws:PrincipalAccount: "*"}`**: bare `*`
  caught by Pattern 1 (line 1010). PASS.

- **`Principal: {Service: "*"}`**: `*` value caught by
  `_principal_is_public` dict-values check (line 1088). PASS.

- **`Principal: {CanonicalUser: "<long hex>"}`**: caught by
  explicit CanonicalUser branch (line 1516). PASS at floor 5.

- **`Effect: "Ａｌｌｏｗ"` (fullwidth)**: NFKC collapses
  fullwidth Roman to ASCII; correctly scores Allow. Combined with
  `Action: *` scores 10. PASS.

- **Empty `NotAction: []`**: caught by line 1616-1626. Scores 10
  on broad resource. PASS.

- **`Action: ['\niam:CreateAccessKey']`**: whitespace strip works
  inside list elements. PASS.

- **Service prefix typo (e.g. lowercase `sso:` vs `sso-admin:`)**:
  `_SERVICE_ALIASES` (line 844) collapses sso→sso-admin. PASS.

- **Statement: bare dict (not list)**: line 1361-1362 normalizes.
  PASS.

- **Missing Effect (implicit Allow)**: line 1112-1113 treats None
  as Allow. PASS at the correct catastrophic floor for `Action: *`.

- **`Condition: {Null: {aws:PrincipalOrgID: "true"}}`** (round-6
  Null inversion): Pattern 2 catches. PASS.

- **`Bool: {aws:SecureTransport: "true"}` (correct intent)**:
  scored at face value; no false positive. PASS — the round-7 fix
  must avoid false-flagging the correctly-intended form.

- **NotPrincipal on Allow**: caught at line 1475-1492, scores 9.
  PASS.

- **`Principal: {AWS: []}` (empty)**: caught at line 1497-1509,
  scores 5. PASS.

- **`Principal: {Service: "lambda.amazonaws.com"}` no Condition**:
  caught at line 1530-1544, scores 6. PASS.

- **Policy-variable injection on attacker tag**
  (`${aws:PrincipalTag/...}`): caught at line 1553-1572, scores 7.
  PASS.

- **`Action: "*:*"`**: line 1861, scores 10. PASS.

- **`Action: "*:Create*"`**: line 1874, scores 9. PASS.

- **`iam:Create*` glob**: catastrophic-residue check at line 1934
  fires, scores 9. PASS.

- **CodeExec + PassRole composition**: line 2359 fires, scores 9.
  PASS.

- **IAM recon + broad sts:AssumeRole**: line 2391 fires, scores 9.
  PASS.

- **`Principal: "*"` bare string**: line 1084, scores 9. PASS.

- **`s3:DeleteBucket` on Resource: `*`** (destructive verb):
  line 2071, scores 8. PASS.

- **Cross-account-exfil action (lambda:AddPermission, etc.)**:
  line 2453, scores 8. PASS.

- **CodeExec primitive alone on strict-broad** (no PassRole):
  line 2336, scores 8. PASS.

- **Secret-bearing-read on strict-broad** (lambda:GetFunction,
  ecs:DescribeTasks, rds:DownloadDBLogFile*): line 2429, scores 7.
  PASS.


## Round 8 — Black-box

Black-box after round 7 closed ~120 architectural bypasses (Cyrillic/
Greek/Hangul homoglyphs in Effect, Cf/Mn category strip for all
invisibles, Principal ARN with wildcard partition/account/saml,
broad-resource partition/region wildcards, ArnLike vacuous detection,
Bool inverted SecureTransport, Numeric type-mismatched, DateLessThan
far-future, policy-var in Condition VALUE, newer-service action
additions for Q Business / DataZone / B2BI / MemoryDB / AppSync /
Cognito IDP / Account API / Macie / CloudTrail EventDataStore / RDS
endpoint / Verified Access).

Total YAMLs: 30 (agent-500 through agent-529).

Result split:
  - gap >= 4 (true bypass — high severity): 7 YAMLs (500, 501, 502,
    503, 504, 505, 521).
  - gap = 3 (clear bypass): 7 YAMLs (508, 509, 510, 511, 512, 513,
    514, 515 — code-exec composition siblings, bedrock subprefixes,
    identitystore).
  - gap = 1-2 (concerning but borderline): 12 YAMLs (516, 518, 519,
    520, 522, 523, 524, 525, 526, 527, 528).
  - gap = 0 (calibration-pin, current behavior matches expected):
    3 YAMLs (506, 517, 529).

Breakdown by attack class:

  - Condition-value cross-account leakage (NEW class — Sec 11.4/11.10
    extended): 4 YAMLs (504 PrincipalAccount foreign on sts:AssumeRole
    gap 5, 506 PrincipalAccount foreign on KMS gap 0 [calibration],
    507 SourceAccount foreign on Lambda gap 4, 503 PrincipalOrgID
    foreign gap 5). The scorer's narrow-Resource heuristic dominates
    even when the Condition VALUE points to a foreign account ID —
    the cross-account direction is invisible.

  - Wildcard-PrincipalArn in Condition (NEW — Sec 11.10 extended):
    2 YAMLs (500 arn:aws:iam::*:role/* gap 6, 501 arn:aws:iam::*:root
    gap 6). These are the SAME class as round-7's ArnLike *:* fix but
    expressed via `StringLike aws:PrincipalArn` operator. Round-7
    vacuous-condition catalog doesn't cover.

  - CloudTrail-bucket attack patterns (Sec 8.4 T1562.008): 2 YAMLs
    (502 s3:PutBucketLifecycleConfiguration on cloudtrail-logs-*
    bucket gap 6, 505 cloudtrail:PutResourcePolicy narrow gap 5).
    The narrow Resource (one bucket / one event-data-store)
    suppresses the catastrophic-floor handling.

  - Composition-rule siblings (Sec 1.x code-exec + PassRole): 4
    YAMLs (512 amplify gap 3, 513 dms gap 3, 514 codepipeline gap 3,
    515 emr-serverless gap 3). All four services should be in
    `_CODE_EXECUTION_PRIMITIVES` per round-7 NN pattern but slipped
    through.

  - Service-aliasing — bedrock subprefixes (Sec 13.x): 3 YAMLs (508
    bedrock-agent-runtime gap 3, 509 bedrock-agent control plane
    gap 4, 510 bedrock-runtime gap 3). Round-7 added `bedrock`
    proper as sensitive but missed `-agent`, `-agent-runtime`,
    `-runtime` siblings.

  - Identity Center sibling miss (Sec 1.52): 1 YAML (511
    identitystore:CreateGroupMembership gap 3). `sso-admin:` admin
    actions are catastrophic (round 2); `identitystore:` admin
    actions on the same blast surface are not.

  - StringNotEquals inverted Deny (Sec 11): 1 YAML (521 gap 1).
    Borderline — current score 8, expected 9, the "inverted deny
    + paired allow" semantic isn't surfaced as catastrophic.

  - Newer-service catastrophic/high-impact additions: 7 YAMLs
    (516 iam:Pass* glob gap 1, 518 omics gap 1, 519 iotsitewise
    gap 1, 520 ssm-incidents gap 1, 522 mq gap 1, 525 kms:ReEncrypt
    gap 1, 528 iot:CreatePolicy gap 1) — all gap 1; small additions
    to existing high-impact sets.

  - Cross-account exfil siblings (Sec 4.13 / 8.8 T1537): 1 YAML
    (523 ec2:CopyImage gap 1). Round-7 QQ recommendation documented
    but didn't implement the EC2 Create-side exfil additions.

  - DNS hijack primitive: 1 YAML (527 route53:ChangeResourceRecordSets
    narrow zone gap 2). High-impact-mutation handles Resource:* but
    narrow zone ARN escapes — DNS rewrite on ONE zone is still full
    MITM.

  - Supply-chain / IAM tagging siblings: 2 YAMLs (524 iam:TagPolicy/
    TagSAML/TagOIDC gap 1, 526 codeartifact:AssociateExternalConnection
    gap 1).

  - Calibration pins (gap 0): 3 YAMLs (506, 517, 529).

# Top 5 worst bypasses (gap-≥4, ordered by gap):

# 1. agent-500-principal-arn-stringlike-any-role
#    Scored 1 vs expected 7 (gap 6). `StringLike aws:PrincipalArn
#    arn:aws:iam::*:role/*` on a narrow KMS key — condition matches
#    any IAM role in any AWS account. Round-7 ArnLike *:* vacuity
#    catalog doesn't extend to StringLike on PrincipalArn. The narrow
#    Resource ARN dominates scoring. Sec 11.10.

# 2. agent-501-principal-arn-stringlike-any-root
#    Scored 1 vs expected 7 (gap 6). Same class as agent-500 — value
#    `arn:aws:iam::*:root` literally matches the root principal of
#    every AWS account in the world. Sec 11.10 + 4.12.

# 3. agent-502-cloudtrail-bucket-lifecycle-attack
#    Scored 1 vs expected 7 (gap 6). `s3:PutBucketLifecycleConfiguration`
#    on `aws-cloudtrail-logs-*` bucket — installing a 1-day lifecycle
#    silently deletes every audit log. The narrow Resource (one bucket)
#    looks scoped but the consequence is total evidence destruction.
#    Sec 8.4 T1562.008.

# 4. agent-503-principal-org-id-foreign
#    Scored 1 vs expected 6 (gap 5). `aws:PrincipalOrgID = o-attackerorg`
#    — structurally valid org ID, scorer can't verify it matches the
#    deploying account's org. Should flag for manual review at
#    floor 6 minimum. Sec 11.16.

# 5. agent-504-principal-account-foreign-sts-assumerole
#    Scored 3 vs expected 7 (gap 4). Trust policy on a privileged role
#    with `aws:PrincipalAccount = 999999999999` (a different account
#    than the resource owner). Cross-account assumption with no
#    ExternalID / MFA / SAML aud. Same class as agent-300/agent-301
#    federated principal but expressed via PrincipalAccount condition.
#    Sec 4.11 + 11.4.

# (Also notable: agent-505 CloudTrail PutResourcePolicy narrow gap 5,
# agent-506 KMS PrincipalAccount-foreign gap 4, agent-509
# bedrock-agent control-plane gap 4.)


# Cross-cutting architectural recommendations (continuing letter
# naming TT...):

# TT. Cross-account direction detector. Sibling of round-7 KK
#     (federated principal shape). When a Condition VALUE is an
#     account ID (`aws:PrincipalAccount` / `aws:SourceAccount` /
#     `aws:ResourceAccount`) that DIFFERS from the Resource ARN
#     account-id segment, surface "cross-account grant" as a
#     primary risk factor and floor at 7 (read) / 8 (write). For
#     `sts:AssumeRole` trust policies, floor at 8 unless MFA /
#     ExternalID / SAML aud condition is also present. Closes
#     agents 504 (gap 5), 506 (gap 4), 507 (gap 4).

# UU. Wildcard-PrincipalArn condition detector. The round-7 ArnLike
#     *:* fix (NN) catches ArnLike operator; extend to
#     `StringLike aws:PrincipalArn` / `StringEquals aws:PrincipalArn`
#     / `StringLike aws:SourceArn`. Detect:
#       - account-id segment is `*`
#       - resource-id segment ends in `*` (e.g. `role/*`, `user/*`,
#         `:root`)
#       - partition segment is `*`
#     All three → vacuous principal → floor 7.
#     Closes agents 500 (gap 6), 501 (gap 6).

# VV. PrincipalOrgID validation. Without account context the scorer
#     can't verify the org-ID, but should surface "PrincipalOrgID
#     unverified — manual review" at floor 6 for data-bearing reads
#     on broad resources, AND treat `aws:PrincipalOrgID` empty-string
#     / `*` / `o-*` (wildcard org ID) as vacuous. Closes agent 503
#     (gap 5) borderline.

# WW. CloudTrail-bucket-target detector. When a Resource ARN matches
#     `aws-cloudtrail-logs-*` / `cloudtrail-*` / org-trail prefix,
#     and the Action is any of `s3:PutBucketLifecycleConfiguration`,
#     `s3:PutLifecycleConfiguration`, `s3:DeleteBucketPolicy`,
#     `s3:PutBucketPolicy`, `s3:DeleteObject`, surface "CloudTrail
#     bucket target — log destruction primitive" and floor at 7.
#     Closes agent 502 (gap 6).
#     Also: add `s3:PutBucketLifecycleConfiguration` and
#     `s3:PutLifecycleConfiguration` to
#     `_HIGH_IMPACT_MUTATION_ACTIONS` independent of bucket name —
#     lifecycle on ANY bucket is content-destructive at scale.

# XX. CloudTrail PutResourcePolicy on narrow ARN. Sibling of XX —
#     `cloudtrail:PutResourcePolicy` already in `_CATASTROPHIC_ACTIONS`
#     per round-7 OO but the narrow-Resource handling doesn't floor.
#     Audit `_CATASTROPHIC_ACTIONS` enforcement on narrow Resource
#     ARNs — should fire regardless of Resource specificity for any
#     "Put/Delete-ResourcePolicy" action.

# YY. Code-execution-primitives audit (round-5 NN extension):
#     - `amplify:CreateApp`, `amplify:UpdateApp`, `amplify:CreateBranch`,
#       `amplify:CreateDeployment`, `amplify:CreateBackendEnvironment`
#     - `codepipeline:CreatePipeline`, `codepipeline:UpdatePipeline`,
#       `codepipeline:StartPipelineExecution`,
#       `codepipeline:PutActionRevision`
#     - `dms:CreateReplicationTask`, `dms:StartReplicationTask`,
#       `dms:CreateEndpoint`, `dms:ModifyEndpoint`,
#       `dms:StartDataMigration`
#     - `emr-serverless:CreateApplication`,
#       `emr-serverless:StartJobRun`, `emr-serverless:UpdateApplication`,
#       `emr-containers:StartJobRun`, `emr-containers:CreateManagedEndpoint`
#     - `omics:CreateWorkflow`, `omics:StartRun`
#     - `bedrock-agent:CreateAgent`, `bedrock-agent:UpdateAgent`,
#       `bedrock-agent:CreateAgentActionGroup`,
#       `bedrock-agent:AssociateAgentKnowledgeBase`,
#       `bedrock-agent:PrepareAgent`
#     Each forms a `+iam:PassRole` composition that should floor 9.
#     Closes agents 512, 513, 514, 515, 518, 509.

# ZZ. Bedrock service-prefix alias expansion. Round-7 added
#     `bedrock` and `bedrock-agentcore` to `_SENSITIVE_SERVICES`;
#     also add:
#     - `bedrock-runtime` (runtime InvokeModel surface)
#     - `bedrock-agent` (control-plane Agent definitions)
#     - `bedrock-agent-runtime` (Agent invocation + RAG retrieval)
#     Add the per-action high-risk-read members:
#     - `bedrock-runtime:InvokeModel`,
#       `bedrock-runtime:InvokeModelWithResponseStream`,
#       `bedrock-runtime:Converse`,
#       `bedrock-runtime:ConverseStream`
#     - `bedrock-agent-runtime:InvokeAgent`,
#       `bedrock-agent-runtime:Retrieve`,
#       `bedrock-agent-runtime:RetrieveAndGenerate`
#     Closes agents 508, 509, 510.

# AAA. Identity Center identitystore-prefix catastrophics. Round-2
#      added `sso-admin:Attach*ToPermissionSet` etc. as catastrophic;
#      mirror to:
#      - `identitystore:CreateGroupMembership`,
#        `identitystore:DeleteGroupMembership`,
#        `identitystore:UpdateGroupMembership`
#      - `identitystore:CreateUser`, `identitystore:UpdateUser`,
#        `identitystore:DeleteUser`
#      - `identitystore:CreateGroup`, `identitystore:UpdateGroup`,
#        `identitystore:DeleteGroup`
#      Org-wide IdP modification is admin-equivalent.
#      Closes agent 511 (gap 3).

# BBB. StringNotEquals inverted-Deny + paired-Allow detector. When
#      a Deny statement uses `StringNotEquals` / `StringNotLike` /
#      `ArnNotEquals` on an identity-narrowing key (`aws:userid`,
#      `aws:PrincipalArn`, `aws:username`) AND the same policy
#      contains an Allow for the same actions, treat the Deny as
#      INVERTED into a whitelist for the named exception → floor 9.
#      Sibling of round-7 LL but Allow+Deny-paired detection.
#      Closes agent 521 (gap 1, narrative).

# CCC. Newer-service _HIGH_IMPACT_MUTATION_ACTIONS additions:
#      - `omics:CreateWorkflow`, `omics:StartRun`
#      - `iotsitewise:CreateAccessPolicy`,
#        `iotsitewise:UpdateAccessPolicy`,
#        `iotsitewise:DeleteAccessPolicy`
#      - `ssm-incidents:CreateResponsePlan`,
#        `ssm-incidents:UpdateResponsePlan`,
#        `ssm-incidents:CreateReplicationSet`
#      - `mq:CreateUser`, `mq:UpdateUser`, `mq:CreateBroker`,
#        `mq:UpdateBrokerConfiguration`
#      - `kms:ReEncryptFrom`, `kms:ReEncryptTo`,
#        `kms:ImportKeyMaterial`, `kms:DeleteImportedKeyMaterial`
#      - `iot:CreatePolicy`, `iot:AttachPolicy`,
#        `iot:CreatePolicyVersion`, `iot:SetDefaultPolicyVersion`
#      - `codeartifact:AssociateExternalConnection`,
#        `codeartifact:DisassociateExternalConnection`,
#        `codeartifact:PutPackageOriginConfiguration`
#      Closes agents 518, 519, 520, 522, 525, 526, 528.

# DDD. `iam:Pass*` glob → composition-rule semantics. The glob
#      `iam:Pass*` matches `iam:PassRole`. When this glob appears
#      AND the same policy has a code-execution-primitive, the
#      composition floor should still fire (9). Currently the
#      glob-resolution catalog catches `iam:Pass*` as
#      "wildcard-in-sensitive-service" (factor 7) but doesn't apply
#      the composition rule. Closes agent 516.

# EEE. Cross-account-exfil EC2 Create-side (round-7 QQ already
#      documented):
#      - `ec2:CreateImage`, `ec2:CopyImage`, `ec2:CopySnapshot`,
#        `ec2:CreateInstanceExportTask`,
#        `ec2:CreateRestoreImageTask`,
#        `ec2:CreateStoreImageTask`
#      Closes agent 523.

# FFF. Route53 narrow-zone DNS-hijack semantic.
#      `route53:ChangeResourceRecordSets` on a narrow zone ARN
#      should floor at 7 — DNS-level MITM on a single production
#      zone is still full traffic hijack. Current high-impact-
#      mutation handling only floors on Resource:*. Sibling treatment:
#      `route53domains:TransferDomain`,
#      `route53domains:UpdateDomainNameservers`. Closes agent 527.

# GGG. IAM Tag-action sibling audit. Round-2 caught `iam:TagUser` /
#      `iam:TagRole` as catastrophic; round-7 confirmed `iam:Tag*`
#      glob fires. Audit explicit listings of the tag siblings:
#      - `iam:TagPolicy`, `iam:UntagPolicy`
#      - `iam:TagOpenIDConnectProvider`,
#        `iam:UntagOpenIDConnectProvider`
#      - `iam:TagSAMLProvider`, `iam:UntagSAMLProvider`
#      - `iam:TagInstanceProfile`, `iam:UntagInstanceProfile`
#      - `iam:TagServerCertificate`, `iam:UntagServerCertificate`
#      All should be in `_CATASTROPHIC_ACTIONS` (or the iam-tagging
#      high-floor set). Closes agent 524.


# Honest negatives — patterns I tried from the research compendium
# that the scorer ALREADY catches correctly (no gap; do NOT add
# YAMLs):
#
# - 3a/3b/3e/3f. `iam:Update*`, `iam:Add*`, `iam:*Login*`,
#   `iam:Put*Policy` glob: round-4 catastrophic-residue enumeration
#   fires, scores 9. PASS.
#
# - 5a. lambda:CreateFunction + iam:AttachRolePolicy narrow: scores
#   9 via the catastrophic `iam:AttachRolePolicy` floor. PASS
#   (composition not surfaced as a named pattern but score is right).
#
# - H1/H2. iam:DeleteRolePermissionsBoundary /
#   iam:PutRolePermissionsBoundary on Resource:* — catastrophic
#   floor fires at 9. PASS.
#
# - AC11. lambda:UpdateFunctionCode regional-wildcard + PassRole
#   composition: scores 9. PASS. (Calibration pin agent-517.)
#
# - AC22. iam:UpdateSAMLProvider on narrow ARN — catastrophic floor
#   fires at 9. PASS.
#
# - AC30. organizations:CreatePolicy + AttachPolicy: scores 9. PASS.
#
# - AC17. accessanalyzer:DeleteAnalyzer on Resource:*: scores 8.
#   PASS.
#
# - AC29. route53:ChangeResourceRecordSets on Resource:*: scores 8.
#   PASS.
#
# - T2/T3. inspector2:Disable*, detective:DeleteGraph: catastrophic
#   floors fire at 9 / 8. PASS.
#
# - V1 reconfirmed: identitystore:CreateGroupMembership scores 6 —
#   PASS the test BUT a clear gap (pinned as agent-511).
#
# - 7b. Wiz JINX-2401 chain (iam:CreateUser + CreatePolicy +
#   CreateAccessKey + CreateLoginProfile + bedrock:InvokeModel):
#   scores 9. PASS at the standard catastrophic-IAM floor; the
#   "10" expectation reflects the multi-stage chain narrative but
#   isn't a score gap.
#
# - 4a. kms:CallerAccount foreign + kms:Decrypt on Resource:*:
#   scores 7 (correct — broad-on-sensitive). PASS.
#
# - C1. aws:PrincipalAccount foreign + secretsmanager:GetSecretValue
#   narrow with `prod-*` resource ID: scores 7 — the resource glob
#   triggers broad-secret-bearing-read floor independent of
#   PrincipalAccount semantics. PASS at the round-7 floor.
#
# - 1a/1b. Top-level PermissionsBoundary key / session-policy shape
#   with `Action:* Resource:*`: scores 10 catastrophic regardless
#   of out-of-band keys. PASS.
#
# - sts:AssumeRole on arn:aws:iam::*:role/* (recon + assume
#   composition): caught by round-1 strict-wildcard collection +
#   round-7 composition fix, scores 9. PASS.
#
# - 8a. CloudTrail bucket + PutBucketPolicy/DeleteBucketPolicy on
#   `aws-cloudtrail-logs-*`: scores 9 — DeleteBucketPolicy /
#   PutBucketPolicy already catastrophic. PASS.  (The 502 fixture
#   uses *Lifecycle*Configuration specifically, which is the
#   gap.)


## Round 8 — White-box

White-box probe after round 7 closed the Cyrillic/Greek/Hangul Effect
homoglyphs, broad-resource partition/region wildcards, ArnLike *:*
vacuity, Bool inverted SecureTransport, Numeric type-mismatched
operator, DateLessThan far-future, policy-var-in-Condition-value, and
extensive newer-service action additions. Rounds 6+7 also fixed
Effect-as-list / Effect-empty-string defenses (lines 1366-1380).

Reading review.py end-to-end, three architectural gaps stand out:

1.  **Trust-policy semantics are absent.** The per-statement loop
    has no notion of "this is a trust policy" — a statement whose
    Action is `sts:AssumeRole*` and whose Principal is set is treated
    as an identity-policy statement. `_principal_is_public` only
    fires on bare `*` or wildcard-ARN partition/account, so cross-
    account ARN principals (`arn:aws:iam::999...:root`,
    `arn:aws:iam::999...:user/attacker`, etc.) score 3 ("scoped or
    limited; no broad patterns"). This is the BIGGEST round-8 gap by
    severity — it makes the scorer blind to the textbook Pacu
    `iam__backdoor_assume_role` primitive.
    Fixtures: agent-600 (cross-account root, gap 4), agent-601
    (cross-account user, gap 4), agent-602 (same-account root, gap
    2), agent-623 (wildcard role path, gap 4), agent-626 (root +
    broken ExternalId, gap 3), agent-640 (Service + cross-account
    AWS root mixed, gap 1).

2.  **`_is_strict_wild_top`'s single-bucket exemption is service-
    agnostic.** Line 2503: `r.startswith("arn:") and r.endswith("/*")
    and r.count("/") == 1` makes ANY `service/*` shape look
    narrow ("the one bucket case"). This is correct for S3, where
    `arn:aws:s3:::bucket/*` IS a single bucket. For DynamoDB,
    Kinesis, DAX, OpenSearch, etc., `service/*` means "every resource
    of that collection type" — full account-wide blast.
    Fixtures: agent-606 (dynamodb table/*, gap 5), agent-608
    (kinesis stream/*, gap 5), agent-609 (dax cache/*, gap 4),
    agent-607 (kms key/*, gap 1 — partial).

3.  **Trust-policy + federated-principal handling is asymmetric.**
    The Service-principal-without-Condition rule (line 1761) fires
    for Service-typed principals but not for Federated-typed; the
    `_condition_is_vacuous` Pattern 6 fires on `StringLike :sub :aud`
    but not `StringEquals` with literal `*`.
    Fixtures: agent-603 (Cognito Federated no-condition, gap 3),
    agent-604 (Google :aud StringEquals=*, gap 2), agent-605
    (Service wildcard suffix `*.amazonaws.com`, gap 2).

4.  **Crash-class bugs on malformed Statement lists.** `_deterministic`
    (line 1700) iterates `policy["Statement"]` without per-element
    type validation. `Statement: [null]` or `Statement: ["string"]`
    cause `AttributeError: 'NoneType'/'str' object has no attribute
    'get'` to bubble up. In any callsite that wraps `analyze_policy`
    in a try/except + falls back to score 1 (a common defensive
    pattern), this is a SCORE-1 BYPASS for a deliberately malformed
    policy that includes an Allow * Action elsewhere in the list.
    Fixtures: agent-636 (Statement [null] crash), agent-637 (string
    entry crashes before reaching Allow *, gap 4-9 depending on
    framework wrapping).

5.  **No catalog awareness of "data-bearing read" outside the
    `_SECRET_BEARING_READS` set.** `cloudformation:GetTemplate*`
    (templates often hold inline secrets), `cognito-identity:
    GetCredentialsForIdentity` (mints creds), `identitystore:
    CreateUser`/`CreateGroupMembership` (org-wide IdP write),
    `sts:TagSession` (ABAC bypass primitive), `iam:CreateService
    LinkedRole` (mints managed-policy principals) all score 4-6
    — below catastrophic. The catalog is missing roughly a dozen
    high-blast actions whose `_SENSITIVE_SERVICES` route caps the
    floor at 6.
    Fixtures: agent-615 (gap 2), agent-616 (gap 2), agent-617
    (gap 2), agent-619 (gap 1), agent-621 (gap 1).


Total YAMLs: 36 (agent-600 through agent-641; 610/611/624 omitted
because rounds 6/7 already implemented the fix; verified during
probe).

Result split:
  - gap >= 4 (true bypass — high severity): 11 YAMLs (600, 601,
    603, 606, 608, 609, 612, 623, 633, 638, 626).
  - gap = 2-3 (clear bypass): 10 YAMLs (602, 604, 605, 613, 615,
    616, 617, 622, 626, 634, 635).
  - gap = 1 (concerning, smaller): 12 YAMLs (607, 614, 618, 619,
    620, 621, 625, 627, 628, 629, 640, 641).
  - calibration / overlap / crash: 4 YAMLs (630 decoy-Deny
    narrative-only no score gap, 631 double-colon currently OK,
    636/637 crash class).

Breakdown by attack class:

  - Trust-policy semantics (THIS IS THE BIG ONE): 8 YAMLs (600,
    601, 602, 603, 604, 623, 626, 640) — all variations on
    "Principal is set, Action is sts:AssumeRole*, scorer ignores
    the trust-policy form." Cumulative gap across these fixtures
    is 21 score-points — by far the largest single architectural
    gap in this round.

  - Service-agnostic `_is_strict_wild_top` exemption (the
    `service/*` narrow-vs-broad confusion): 3 YAMLs (606, 608,
    609) — gap 14 cumulative.

  - Malformed Effect/Statement/Action defects: 5 YAMLs (612, 633,
    636, 637, 638). The "malformed = worst-case" principle from
    line 1101 is applied to MISSING Effect but not to malformed
    Statement, empty Action, or non-string Action.

  - Action catalog additions (sensitive-but-not-catastrophic):
    7 YAMLs (615, 616, 617, 618, 619, 620, 621).

  - Resource-shape edge cases: 6 YAMLs (607 KMS key/*, 622 empty
    account ARN, 625 Resource-as-dict, 627 s3express, 628 sns
    topic /*, 635 empty partition).

  - Condition vacuity extensions: 2 YAMLs (614 StringEquals
    literal `*`, 632 vacuous-condition on narrow PassRole — should
    elevate the floor).

  - Version/2008-grammar awareness: 2 YAMLs (613 Version 2008
    explicitly, 639 Version omitted).

  - PassRole narrow-with-admin-arn heuristic: 1 YAML (641).

  - SCP / wildcard NotAction sibling: 1 YAML (634 EC2 mass recon
    — metadata-listing skip too aggressive).

  - Defense-evasion sibling (sso-admin Delete/Detach not
    catastrophic): 1 YAML (620).


# Top 5 architectural gaps (proposed fixes, 1-2 sentences each):

# HHH. Trust-policy detector. When a statement has Principal set
#      AND Action covers `sts:AssumeRole*`, evaluate principal-by-
#      principal: any AWS-ARN value with a foreign account ID (or
#      `:root` in own account) → floor 7; Service with `*` in name
#      → floor 8; Federated without Condition → floor 6; condition
#      vacuity is per-condition (already detected) but the floor
#      should add — not max — when the trust-policy shape is
#      recognized. Closes agents 600-605, 623, 626, 640 (gap
#      cumulative ~21).

# III. Service-aware single-bucket exemption. Limit the
#      `arn:...:bucket/*` narrow-treatment in `_is_strict_wild_top`
#      (line 2503) to services where that shape ACTUALLY means "one
#      bucket": currently only S3. For dynamodb, kinesis, dax,
#      opensearch, sns, sqs, glue, codebuild, etc., `service/*` is
#      a COLLECTION wildcard. Add an allow-list of single-bucket-
#      semantic services (just `s3` for now) and treat all others
#      as strict broad. Closes agents 606, 608, 609 (gap ~14).

# JJJ. Malformed-shape worst-case rule extension. Apply the
#      existing "malformed = worst-case" principle (line 1101
#      docstring) consistently across Statement-list per-element
#      validation (skip non-dict entries + flag), `_as_list` non-
#      str/non-list values (return malformed-sentinel), and Effect/
#      Action/Resource type-handling failures. Closes agents 612,
#      633, 636, 637, 638 (gap ~22 cumulative). The crash class
#      (636/637) is the most urgent — a callsite wrapping
#      analyze_policy in try/except + score-1 fallback turns these
#      into TOTAL bypasses.

# KKK. Sensitive-service-narrow-write catalog. Expand
#      `_HIGH_IMPACT_MUTATION_ACTIONS` with the post-round-7 finds:
#      `cloudformation:GetTemplate`, `cloudformation:GetTemplate
#      Summary`, `cloudformation:GetStackPolicy` (to `_SECRET_BEARING_
#      READS`), `cognito-identity:GetCredentialsForIdentity`,
#      `cognito-identity:GetOpenIdTokenForDeveloperIdentity`,
#      `identitystore:CreateUser`, `identitystore:CreateGroupMembership`,
#      `identitystore:CreateGroup` (to `_CATASTROPHIC_ACTIONS`),
#      `sts:TagSession`, `sts:SetSourceIdentity` (to `_HIGH_RISK_
#      ACTIONS`), `iam:CreateServiceLinkedRole`,
#      `lambda:InvokeFunctionUrl`, `sns:Publish`,
#      `appstream:CreateImageBuilder`/`CreateFleet`/`CreateStreamingURL`,
#      `sso-admin:Delete*` and `sso-admin:Detach*`. Closes agents
#      615-621, 628, 629 (gap ~12 cumulative).

# LLL. ARN-segment empty-detection + Version-aware variable-
#      interpolation warnings. Treat empty partition/region/account
#      segments (parts[1/3/4] in ("", "*")) consistently in
#      `_is_broad_resource` and `_is_strict_wild_top`. When `Version`
#      is missing or `2008-10-17`, flag any Resource containing
#      `${aws:...}` as defect-class (the variables don't expand).
#      Closes agents 622, 635, 613, 639.


# Honest negatives — round-8 attempts the scorer CORRECTLY catches:
#
# - **Sid 'AllowEverything' on Effect=Deny**: scored 1 (correct —
#   Deny statement, no Allow → no risk). The `Sid` field has no
#   semantic impact on scoring; "reassuring Sid" + Action:* still
#   scores 10 from the Action handling (line 2082). PASS.
#   Sid is metadata-only; the scorer correctly ignores it. No fix
#   needed.
#
# - **Top-level Condition outside Statement**: scored 9 (Allow
#   catastrophic still fires). The stray top-level Condition is
#   ignored by AWS too. PASS.
#
# - **Effect Deny + Allow catastrophic, Deny on vacuous condition**:
#   scored 9 (the Allow catastrophic is caught). The "decoy Deny"
#   gap is narrative — the scorer DOESN'T surface "this Deny will
#   never fire" but the score is right because the Allow alone is
#   catastrophic-floor-9. PASS on score; narrative gap documented
#   as agent-630.
#
# - **Action `*:Decrypt` (wildcard service)**: scored 9 (round-7
#   wildcard-service handling at line 2105). PASS.
#
# - **NotAction `iam:Delete*` on broad resource**: scored 9 — the
#   admin-minus-set rule fires. PASS.
#
# - **Effect 'aLLOw' (mixed case)**: scored 10 — `_norm_grammar_str`
#   + `.lower()` handles. PASS.
#
# - **NotAction `iam:*` on narrow IAM resource**: scored 9 — the
#   catastrophic-residue enumeration (line 1890) catches the
#   non-excluded catastrophic actions. PASS.
#
# - **PassRole + StringEquals aws:RequestedRegion='us-east-*'
#   on Resource:* (broken condition, PassRole broad)**: scored 9
#   — PassRole-broad floor 9 fires regardless of broken condition.
#   PASS at the action floor. (The condition-vacuity narrative
#   gap is documented as agent-614 / agent-632 for the narrow
#   variants.)
#
# - **bedrock:InvokeModel on cross-account ARN**: scored 8 (cross-
#   account-exfil tier from round 6). PASS.
#
# - **NotPrincipal empty `{AWS: []}` on Allow**: scored 9 by line
#   1711. PASS.
#
# - **iam:PassRole + RAM CreateResourceShare composition**: scored
#   9 (RAM is in cross-account-exfil already). PASS.
#
# - **PassRole + ExternalId condition (legitimate use)**: PASS —
#   the scorer doesn't false-flag this benign pattern.
#
# - **`iam:*Network*` infix wildcard**: scored 8 (sensitive-service
#   wildcard action). PASS. (No catastrophic-residue match for
#   this specific glob.)
#
# - **Federated principal arn:aws:iam::*:saml-provider/foo**:
#   scored 9 by line 1316 (round-7 fix). PASS.
#
# - **Effect ['Allow'] list-of-one + Effect '' empty string +
#   Effect ['Allow','Deny'] list**: all handled by lines 1366-1380
#   (round-8 fix already in tree). PASS. (My initial YAMLs for
#   these — agent-610/611/624 — deleted after verification showed
#   the fixes were merged.)



## Round 9 — White-box (convergence verification)

Convergence verdict: **NOT fully converged.** Architectural gaps
remain, though they are now narrower and split between strict
bypasses, malformed-shape defects, action-catalog completeness,
factor-quality (rather than score-quality), and false-narrowing
condition patterns. 15 architectural gap fixtures, 2 awareness-
gap documents.

New architectural gaps found: YES (15 score-affecting +
2 factor-quality awareness fixtures).

# Top 5 architectural gaps (by gap size and reach):

1. **`Statement` keyed by Sid (object-of-statements grammar form,
   not list-of-statements) — silent score-1 bypass for full
   admin.** Round-5 agent-96 closed the single-`{dict}` form but
   not the multi-Sid-keyed-`{Sid1: {...}, Sid2: {...}}` form.
   The single-dict branch at line 1681 wraps the WHOLE container
   in a list, but the container itself has no Effect/Action keys
   so all per-statement rules skip it. Final score: 1 for
   `Statement: {Wild: {Effect:Allow, Action:*, Resource:*}}`.
   Fixture: agent-800 (gap 6 — and 9 if the deployer
   auto-normalizes to list).

2. **Cc-category (control-character) action-name bypass.** TAB
   (U+0009), VT (U+000B), CR (U+000D), and NUL (U+0000) embedded
   in an action string survive `_norm_grammar_str` because that
   function strips Cf+Mn but NOT Cc. `Action: "iam:\tPassRole"`
   scores 6 (sensitive-service floor), missing the PassRole-broad
   floor of 9. This is the round-7-Cf-strip sibling, one Unicode
   category over. Fixtures: agent-815 (VT), 816 (CR), 817 (NUL).
   Gap = 3 each; cumulative 9. Round 7 agent-714 already documents
   tab black-box; agent-815/816/817 catalog VT/CR/NUL.

3. **PassRole narrow-to-known-admin-role-name.** Round-8 agent-641
   caught `PassRole + list with admin ARN buried`; this round's
   agent-805 is the simpler `PassRole + single ARN naming
   OrganizationAccountAccessRole` form. The role name is known-
   admin by AWS-Organizations convention, but the scorer sees only
   "narrow PassRole" → score 4. Real risk: 7. Gap = 3.
   Proposed fix: known-admin-role-name pattern set
   (`OrganizationAccountAccessRole`, `*Administrator*`,
   `AWSReservedSSO_Admin*`, `*SuperUser*`).

4. **Trust-policy detector misses bare 12-digit account-IDs.**
   `Principal: {AWS: "999988887777"}` is semantically equivalent
   to `Principal: {AWS: "arn:aws:iam::999988887777:root"}` per
   AWS docs, but the literal-arn check at line 1970 gates on
   `v.startswith("arn:")`. Bare account IDs slip past the
   trust-policy floor 7. Fixture: agent-820 (gap 1, but the
   bypass class is broader than the size suggests — every
   cross-account-share resource policy in the world).

5. **Path-normalization shadow: double-slash bucket/path is
   treated narrow.** `arn:aws:s3:::prod-data//*` scores 3
   (narrow). AWS's resource-evaluator collapses consecutive
   slashes in S3 keys; the scorer should too. Fixture: agent-801
   (gap 2). Small but adversarially exploitable: any submission
   form that doesn't normalize ARN paths lets an attacker
   submit `prod-data//*` and pass the scorer while AWS treats
   it as `prod-data/*`.

# Honorable mentions (gap 1-3, real but smaller):

- **API Gateway leading-slash collection wildcard** (agent-802):
  `arn:aws:apigateway:us-east-1::/apikeys/*` not recognized as
  broad because `collection_types` map lacks `apigateway` AND
  the leading-`/` form makes the partition split off an empty
  first-segment. Gap = 2.

- **IAM-recon + narrow AssumeRole composition** (agent-804):
  the recon-broad + AssumeRole-narrow combo is still lateral-
  movement (recon's job is to FIND which narrow role to target),
  but the composition rule at line 2853 gates on
  `has_assume_broad`. Gap = 1.

- **AutoScaling CreateAutoScalingGroup** (agent-806): PMapper
  autoscaling_edges primitive — bind existing LC with privileged
  profile. Missing from `_HIGH_IMPACT_MUTATION_ACTIONS`. Gap = 1.

- **Pacu cross-account recon** (agent-807): `ec2:DescribeSnapshots`,
  `ec2:DescribeVpcPeeringConnections`,
  `ec2:DescribeVpnConnections` are pure cross-account recon
  primitives. No `_CROSS_ACCOUNT_RECON_ACTIONS` set exists.
  Gap = 2.

- **Resource policy missing Principal** (agent-808): AA validation
  finding MISSING_PRINCIPAL. The scorer doesn't distinguish
  resource-policy from identity-policy. Gap = 3 (contingent on
  schema knowing the policy type).

- **Invalid ARN region typo** (agent-809): AA validation finding
  INVALID_ARN_REGION. No real-region whitelist in
  `_is_broad_resource`. Gap = 3.

- **GitHub OIDC StringLike `repo:org/*`** (agent-811): Sysdig
  research class; the federated-floor at 7 fires but the more
  specific "OIDC sub claim with `*`" isn't detected as the
  fork-takeover class. Gap = 1.

- **Pacu organizations-enum + AssumeRole** (agent-819): the
  iam-recon-actions_lc set at line 2839 is IAM-only; doesn't
  catch `organizations:ListAccounts` + `sts:AssumeRole *`
  composition (which is the EXACT Pacu org-assume primitive).
  Gap = 1.

# Awareness-gap documents (score correct, factor-quality
# lacking):

- **PermissionsBoundary field ignored** (agent-803): scorer
  doesn't surface that the policy includes a boundary. Score
  correctly 10 because identity policy IS admin; the qualitative
  signal for human review is missing. Architectural fix: when
  `policy.get("PermissionsBoundary")` is set, append a factor
  noting the boundary's presence and the intersection semantic.

- **`aws:ViaAWSService` false-narrowing condition** (agent-810):
  scorer correctly fires PassRole-broad floor 9 regardless of
  condition. The qualitative gap is the MISSING factor — a
  reviewer seeing the condition assumes it narrows; the scorer's
  factor list should explain why ViaAWSService is a principal-
  class assertion, NOT resource scope. Architectural fix:
  extend `_condition_is_vacuous` with a `false_narrowing` class.

# Honest negatives — patterns I probed that ALREADY pass:

- **Wildcard service `**:**`**: scored 10 (Action:* path). PASS.
- **Resource as dict** (`{some-key: arn:aws:iam::*:role/admin}`):
  scored 10 — the dict-typed Resource on Action:* hits the
  Action:* worst-case path. PASS.
- **Action array containing a dict entry**: scored 9 — the
  `_as_list` filter handles non-string entries gracefully. PASS.
- **Principal CanonicalUser: "*"**: scored 9 — the
  `_principal_is_public` check (round 7 fix) handles `*` in
  CanonicalUser. PASS.
- **Principal AWS: "*"** vs `{AWS: "*"}`: both scored 9. PASS.
- **Code-execution-primitive alone on Resource:* (no PassRole)**
  (`lambda:CreateFunction Resource:*`): scored 8 (round-5
  floor 8 path fires). PASS.
- **Specific PassRole + RunInstances/RunTask in separate
  statements**: scored 9 (the PassRole+code-exec composition
  rule fires across statements). PASS.
- **Capital One pattern** (`s3:ListAllMyBuckets + s3:List* +
  s3:GetObject Resource:*`): scored 7 (one tier below
  expected 8 — secret-bearing-reads catches but s3:* family
  expected to be higher). gap = 1, smaller than top-5.
- **Code Spaces destructive chain** (ec2/s3 deletes): scored 8
  (one tier below expected 9). gap = 1, smaller than top-5.
- **Lambda + EventBridge persistence**: scored 8 (one tier
  below expected 9). gap = 1, smaller than top-5.
- **`StringEquals aws:PrincipalAccount: "*"`** (vacuous-by-`*`):
  scored 9. PASS — round-8 vacuity catalog catches this.
- **`aws:CalledVia` chain**: scored 10 (the catastrophic-action
  Action:* fires regardless). PASS.
- **Resource ARN with 3 colons (`arn:aws:s3:*`)**: scored 9
  (Action `s3:*` fires the catastrophic-residue path). PASS.
- **Malformed ARN with extra colons (`arn:aws:iam::::::*`)**:
  scored 9 (Action `iam:*` fires catastrophic-residue). PASS.

# Score-zone calibration (auto-approve threshold drift):

Five Capital-One / Code-Spaces / Pacu-persistence patterns
score 7-8 where I expected 8-9 (research-corpus expectations
match my expectations). These are calibration drift — the
floor-tier for "destructive chain" and "persistence chain"
could each move up by 1 tier without false-positive cost.
This is gap-1 territory and below the top-5 cutoff; not
written as fixtures.

Of the 16 still-failing research patterns from the harness:
- 5 are calibration-drift gap-1 (research-01-42, -02-3, -02-5
  organizations, -02-6, -05-1, -05-2, -07-1, -09-5, -10-7,
  -11-3, -13-14): these correspond to my agent-806, 819, 811,
  and the cluster of gap-1 high-impact-action catalog gaps.
- 3 are pure-recon defect class (-02-5 vpc, -02-7 ebs):
  agent-807 documents.
- 2 are AA-validation-finding class (-07-5 invalid region,
  -07-5 missing principal): agent-808, 809 document.
- 1 is question-mark glob in Action (research-10-7): TODO — I
  did NOT write a fixture; this is a sister to round-6
  `_has_wildcard` handling that didn't propagate to action
  catalog lookups. The fix is in `_canonical_action` or
  `_action_covers_any` to treat `?` the same as `*` for
  service prefix matching.
- 1 is deny-NotAction normalization (-12-4): research expects
  Deny+NotAction:[CreateUser] to score 2 (not 1, the current
  result); this is a metadata-quality gap, not a security gap,
  and below the "write a fixture" bar.

# Convergence verdict — full:

The scorer is **substantially converged** on the core
architectural surface. The remaining gaps are:

- **Grammar-shape completeness** (alternative `Statement`
  forms, control-char chars in actions, double-slash
  normalization) — narrow but exploitable.
- **Catalog completeness** (specific actions: known-admin role
  names, autoscaling create primitives, organizations
  enumeration, cross-account recon classes).
- **Trust-policy precision** (bare account ID, GitHub-OIDC
  sub-wildcard specifically).
- **Factor-quality** (PermissionsBoundary surfaced,
  ViaAWSService explained, condition false-narrowing class).

None of the round-9 gaps are catastrophic-floor bypasses on
the same scale as round-7's Cyrillic-Effect (gap 8). The
largest single round-9 gap is agent-800 (Statement-keyed-by-
Sid, gap 6) — a fundamental grammar-shape miss but with low
real-world incidence (AWS itself rejects the form). The next
tier — agent-805/815/816/817 each gap 3 — represents the
true remaining surface: action-catalog completeness for
known-admin-role-names and control-character normalization.

The loop has not formally converged because new gaps were
found, but the slope of gap-finding per round-of-effort is
clearly diminishing. Round 8 was the last big-bypass round
(trust-policy: gap 21 cumulative across 8 fixtures). Round
9's cumulative score-gap across 15 fixtures is ~37 points
(avg 2.5/fixture vs round 8's 4.7/fixture). One more round
should reach the asymptote where every remaining gap is
gap-1 calibration drift.

Total round-9 YAMLs: 17 (agent-800 through agent-820,
gaps 801-802-804-805-806-807-808-809-811-815-816-817-819-820
are score-affecting; 803 and 810 are factor-quality
awareness documents; 812/813/814/818 were drafted but
deleted because they duplicated existing round-9 black-box
fixtures or were redundant after broader-collection-types
testing showed the generic bucket/* fallback already catches
most service-collection forms).

Breakdown:
  - gap ≥ 5: 1 fixture (800).
  - gap = 3-4: 6 fixtures (805, 808, 809, 815, 816, 817).
  - gap = 1-2: 8 fixtures (801, 802, 804, 806, 807, 811, 819, 820).
  - factor-quality awareness: 2 fixtures (803, 810).



## Round 9 — Black-box (net-new patterns)

Black-box probe focused on net-new attack surface NOT covered by the
research compendium (mid-2025 cutoff): newer services (Aurora DSQL,
Bedrock Guardrails, VPC Lattice, Greengrass v2, MGN, Network Manager,
DataSync, Connect), cross-account resource-policy edges, encoding
bypasses, trust-policy ExternalId vacuity, and cross-statement
compositions.

Total YAMLs: 32 (agent-700 through agent-731).

Result split (relative to expected score_min):
  - gap ≥ 5 (severe bypass): 3 fixtures (705 score 1 vs 7, 711 score 1
    vs 7, 709 score 2 vs 7).
  - gap = 3-4: 12 fixtures (700, 701, 702, 703, 704, 706, 707, 708,
    710, 712, 718, 731).
  - gap = 1-2: 11 fixtures (713, 714, 715, 716, 719, 720, 721, 722,
    723, 724).
  - calibration-pin / PASS: 6 fixtures (717, 725, 726, 727, 728, 729,
    730).

Total gap units: 76.

# TOP 5 WORST GAPS (gap-≥4, ordered by severity):

# 1. agent-705-resource-policy-cross-account-kinesis
#    Scored 1 vs expected 7 (gap 6). Kinesis stream resource-based
#    policy (2023 GA) granting `kinesis:GetRecords` to a foreign
#    account on a narrow stream ARN. Attacker reads every record
#    streaming through the prod stream. Worst-case score (1) because
#    Kinesis read-actions aren't in the data-bearing-read catalog.

# 2. agent-711-encoding-bypass-url-encoded-action
#    Scored 1 vs expected 7 (gap 6). `iam%3APassRole` — URL-encoded
#    colon. The action-string parser doesn't normalize URL-encoded
#    bytes; the scorer sees an unknown action and scores 1. Any
#    toolchain that decodes the policy before applying it (some CDK
#    transforms, custom Terraform providers) will get the catastrophic
#    `iam:PassRole` Resource:*. Pure evasion.

# 3. agent-709-resource-policy-cross-account-glue-catalog
#    Scored 2 vs expected 7 (gap 5). Glue catalog cross-account read.
#    Same gap class as 700-708 (resource-policy cross-account
#    direction not detected). Glue catalog grants are the gateway
#    for cross-account data lakes.

# 4. agent-712-encoding-bypass-double-url-encoded-action
#    Scored 1 vs expected 6 (gap 5). `iam%253APassRole` — double-
#    encoded. Single decode produces `iam%3APassRole`; the scorer
#    sees unknown action and scores 1. Same class as 711.

# 5. agent-700-resource-policy-cross-account-sns (and siblings
#    701/702/703/704/706/707/708/731)
#    Scored 3 vs expected 7 (gap 4 each). Same architectural gap:
#    SNS, SQS, EFS, KMS, DynamoDB (new 2024 RBP), EventBridge,
#    Lambda, Step Functions, IoT MQTT — all cross-account resource-
#    policy statements scored as benign narrow-resource statements
#    because the scorer doesn't differentiate Principal-keyed
#    statements as resource-policies and doesn't detect cross-
#    account direction in Principal AWS ARNs.

# Breakdown by attack class:

#  - Resource-policy cross-account class (NEW for round 9): 11
#    fixtures (700-710 minus 705 + 731). All score 1-3 vs expected
#    6-7. Single dominant architectural gap. Sibling treatment
#    needed across all services that support resource-based policies.

#  - Encoding/normalization bypass (extending round-7 Unicode-strip):
#    4 fixtures (711, 712, 713, 714). URL-encoded colon, double
#    URL-encoded colon, NBSP in action, TAB in action. All bypass
#    action-catalog matching. Round-7 closed invisible Cf/Mn in
#    Effect; ACTION strings need parallel normalization + a
#    "non-canonical action format" suspicion flag.

#  - Trust-policy ExternalId vacuity (extending round-8 trust-policy
#    detection): 3 fixtures (715-717). The scorer detects ExternalId
#    condition EXISTENCE but not VALUE. Vacuous values (`*`,
#    `""`, short numeric lists) defeat confused-deputy protection.

#  - Newer-service catastrophic actions (post-research-doc cutoff):
#    5 fixtures (718 dsql, 720 connect lambda, 721 mgn, 722
#    networkmanager, 726 vpc-lattice — passing — and 727 bedrock
#    guardrails — passing). Aurora DSQL, Connect, MGN, NetworkManager
#    are the gaps; vpc-lattice + bedrock-guardrails are now correctly
#    scored 8 (calibration pin worth recording).

#  - Cross-statement composition siblings (extending round-7 YY):
#    3 fixtures (719 datasync-only, 723 greengrass+passrole, 724
#    datasync+passrole). DataSync and Greengrass v2 are missing from
#    _CODE_EXECUTION_PRIMITIVES / exfil-composition catalogs.

#  - Calibration pins (PASS): 6 fixtures (717, 725, 726, 727, 728,
#    729, 730).


# Cross-cutting architectural recommendations (continuing letter
# naming HHH...):

# HHH. **Resource-policy cross-account direction detector**
#      (HIGHEST IMPACT — single change closes 11 fixtures).
#      When a statement has a `Principal` key with `Principal.AWS`
#      containing an ARN, and the ARN's account-id segment differs
#      from the Resource ARN's account-id segment, treat the
#      statement as a RESOURCE-POLICY CROSS-ACCOUNT GRANT and apply
#      a separate scoring path:
#        - read-only actions (Get*, List*, Describe*, Receive,
#          Subscribe, ESHttpGet, Decrypt, GetRecords, etc.): floor 7
#        - write actions (Put*, Send, Publish, Update, Delete, Mount
#          with Write, ClientWrite, ClientRootAccess, PublishPackage):
#          floor 8
#        - admin-shape (PutBucketPolicy, PutKeyPolicy, PutAuthPolicy,
#          CreateGuardrail-on-cross-acct): floor 9 (already mostly
#          covered)
#      Also detect Principal: "*" / Principal.AWS: "*" as
#      public-grant (already covered for some, not all).
#      Specifically also handle Principal as bare 12-digit account
#      ID (not in ARN form) — sometimes accepted in resource
#      policies.
#      Closes agents 700, 701, 702, 703, 704, 705, 706, 707, 708,
#      709, 710, 731 (12 fixtures, gap range 3-6).

# III. **Action-string normalization + non-canonical flag**.
#      Extend the round-7 Unicode-invisible strip to all action
#      strings, AND treat the following as non-canonical evasion
#      attempts (floor at 6-8 depending on what the verb-stem looks
#      like after normalization):
#        - URL-encoded characters (`%XX` sequences) in action
#        - Double-URL-encoded (`%25XX`)
#        - Non-breaking space (U+00A0) inside action
#        - TAB / CR / LF inside action
#        - More than one colon in action (e.g. `iam::PassRole`)
#        - Zero-width joiner / non-joiner (U+200C/U+200D) inside
#          action
#      Normalize to canonical `service:Action` form; THEN re-evaluate
#      against the action catalog. If normalization changed the
#      string, ALSO floor at 6+ regardless (the author shouldn't
#      need encoding — its presence is evasion-signal).
#      Closes agents 711, 712, 713, 714.

# JJJ. **ExternalId VALUE validation in trust policies**. Round-8
#      added trust-policy cross-account detection (600-602). Extend
#      to validate the ExternalId VALUE:
#        - `*` or `*` patterns via StringLike → vacuous
#        - empty string `""` → vacuous
#        - lists of < 5 short numeric/alphabetic tokens (entropy
#          test: shortest token < 8 chars, all-digits or all-lowercase
#          → likely brute-forceable)
#        - policy-variable values (`${aws:username}` etc.) in
#          StringEquals → broken (these don't substitute properly
#          in trust-policy evaluation per AWS docs)
#      When any of the above, treat trust-policy as having NO
#      ExternalId protection → floor 8 (same as cross-acct + no
#      Condition).
#      Closes agents 715, 716 (borderline 717).

# KKK. **Newer-service catastrophic/high-impact additions
#      (post-mid-2025)**:
#        - **Aurora DSQL**: `dsql:DbConnectAdmin` (treat like
#          `rds-db:connect` admin → floor 6 narrow / 8 broad);
#          `dsql:DeleteCluster`, `dsql:UpdateCluster` (high-impact
#          mutation).
#        - **Amazon Connect**: `connect:AssociateLambdaFunction`
#          (code-execution primitive in contact flow → floor 7);
#          `connect:UpdateContactFlow`, `connect:CreateInstance`,
#          `connect:UpdateUserSecurityProfiles` (high-impact).
#        - **MGN**: `mgn:StartCutover`, `mgn:FinalizeCutover`,
#          `mgn:StartTest`, `mgn:UpdateSourceServerReplicationType`
#          (server-takeover primitives → floor 7).
#        - **Network Manager**: `networkmanager:RegisterTransitGateway`,
#          `networkmanager:CreateConnection`,
#          `networkmanager:UpdateConnection`,
#          `networkmanager:CreateConnectPeer` (network-pivot →
#          floor 7).
#        - **Bedrock Guardrails**: `bedrock:DeleteGuardrail`,
#          `bedrock:UpdateGuardrail` (LLM safety bypass — already
#          scoring 8, PIN).
#        - **VPC Lattice**: `vpc-lattice:PutAuthPolicy`,
#          `vpc-lattice:DeleteAuthPolicy` (service-mesh authz
#          override — already scoring 8, PIN).
#      Closes agents 718, 720, 721, 722.

# LLL. **DataSync and Greengrass v2 in code-exec/exfil composition
#      catalog** (extending round-7 NN and round-8 YY):
#        - `datasync:CreateLocationS3`,
#          `datasync:CreateLocationObjectStorage`,
#          `datasync:CreateTask`,
#          `datasync:StartTaskExecution`
#          + iam:PassRole → floor 9 (exfil composition)
#        - `greengrass:CreateDeployment`,
#          `greengrass:CreateComponentVersion`
#          + iam:PassRole → floor 9 (fleet RCE)
#      Standalone (no PassRole): floor 7 because the actions still
#      do per-call passing implicitly via the assumed service role.
#      Closes agents 719, 723, 724.

# MMM. **Cross-partition Principal sanity check**. When Principal
#      AWS ARN's partition (`aws-cn`, `aws-us-gov`, `aws-iso`,
#      `aws-iso-b`, `aws-iso-c`) differs from Resource ARN's
#      partition (typically `aws`), the grant is structurally
#      impossible — AWS partitions don't trust across boundaries.
#      Treat as "ambiguous — manual review" with a CLARIFYING
#      factor + floor 5 (not 1, not 9). The scorer currently
#      treats `aws-cn:iam::999...:root` as a valid cross-account
#      principal and may over-score.
#      Loose fix — closes agent 728 (currently over-scoring 9 vs
#      expected ~5).


# Honest negatives — patterns I tested in probe rounds 1-5 that the
# scorer ALREADY catches correctly (no YAML written):
#
# - **lambda:CreateFunction + lambda:AddPermission narrow** (no
#   PassRole): scored 8. PASS via existing lambda:AddPermission
#   handling.
#
# - **Permission-boundary key at top level + Action:* Resource:**:
#   scored 10 (round-2/round-8 catastrophic-floor on Action:*).
#   PASS regardless of the misnamed PermissionsBoundary key.
#
# - **Aurora DSQL `dsql:DeleteCluster` Resource:** broad scope:
#   scored 8 (sensitive-service-style residue). PASS borderline.
#
# - **mgn:* Resource:** full broad: scored 8 (likely from `mgn:*`
#   glob recognition). PASS borderline.
#
# - **bedrock:DeleteGuardrail / UpdateGuardrail Resource:** scored
#   8 — round-7/8 bedrock sensitive-service treatment covers it.
#   PASS borderline (still net-new info).
#
# - **vpc-lattice:PutAuthPolicy Resource:** scored 8. PASS borderline.
#
# - **Principal: "*" on s3 / lambda invoke**: scored 9.
#   PASS via existing Principal:* detection.
#
# - **API Gateway resource policy cross-account narrow**: scored 6
#   (not great but above the catastrophic-need threshold). Borderline
#   PASS — could be 7 but the resource-glob `/*` triggers some
#   collection-broad heuristic.
#
# - **iam:Pa+ZWJ+ssRole (zero-width joiner)**: scored 9. PASS via
#   round-7 invisible-Cf strip extended to ACTION strings.
#
# - **NBSP+iam:PassRole (leading NBSP) / iam:PassRole+trailing-NBSP**:
#   scored 9. PASS — leading/trailing whitespace strip works; the
#   gap (agents 713/714) is whitespace INSIDE the action.
#
# - **aws:CalledVia / CalledViaLast = cloudformation.amazonaws.com
#   on iam:PassRole Resource:**: scored 9. PASS via existing iam:*
#   wildcard / Resource:* catastrophic floor; CalledVia condition
#   doesn't suppress.
#
# - **Lake Formation cross-account GetDataAccess**: scored 8.
#   PASS via existing lakeformation handler (round-2/agent-71).
#
# - **CWLogs cross-account PutSubscriptionFilter**: scored 8.
#   PASS via existing logs-subscription handling. (The destination
#   form, agent-710, is the gap.)
#
# - **CloudFormation StackSet CreateStackInstances / CreateStackSet**:
#   scored 9. PASS via existing cfn-stackset detection (round-2).
#
# - **Step Functions resource policy** + sufficiently broad actions:
#   confirmed gap (708).
#
# - **iam:DeleteRolePermissionsBoundary narrow**: scored 9.
#   PASS via existing iam-catastrophic floor.
#
# - **CreateRole with bogus PermissionsBoundary condition value
#   (boundary = AdministratorAccess)**: scored 9. PASS via
#   iam:CreateRole catastrophic floor; the condition is essentially
#   ignored as expected.
#
# - **Composition: iam-recon + sts:AssumeRole on dev-* glob**:
#   scored 9. PASS (round-7 composition rule). Pinned as 725.
#
# - **`aws:ViaAWSService = true` on broad iam:PassRole**: scored 9.
#   The condition is a RESTRICTION (only service calls), but the
#   scorer correctly doesn't trust it to lower below the iam:PassRole
#   broad-resource floor. PASS.
#
# - **JSON-stringified PrincipalArn condition value (`[\"...\"]`)**:
#   scored 9. PASS — the bogus literal string doesn't match any
#   real principal in evaluation; the broad Action s3:* dominates.
#
# - **`aws:TokenIssueTime` numeric vacuous-large value**: scored 9.
#   PASS — Numeric operator handling closes this.


## Round 10 — White-box (final convergence audit)

Total fixtures written: 11 (agent-1000 through agent-1010).
Test result split (vs expected score_min):

  - FAIL (gap-confirming): 5 fixtures
      agent-1000 dict-shaped Action          gap 4 (scored 1, exp ≥5; true risk ≥9 → effective gap 8)
      agent-1001 missing Resource + PassRole gap 3 (scored 4, exp ≥7; true risk ≥9 → effective gap 5)
      agent-1002 CFN Fn::Sub Action          gap 4 (scored 1, exp ≥5; true risk ≥9 → effective gap 8)
      agent-1003 dict-shaped Resource        gap 1 (scored 4, exp ≥5; true risk ≥7 → effective gap 3)
      agent-1004 JSON-stringified Action     gap 1 (scored 4, exp ≥5; true risk ≥9 → effective gap 5)

  - PASS (already-converged or factor-quality): 6 fixtures
      agent-1005 null in Action list         PASS (scored 9; round-8 list filter handles)
      agent-1006 tuple-form Statement        PASS in YAML — only fails when policy is built
                                             with Python tuple (YAML can't express tuples;
                                             documented as harness-level bypass)
      agent-1007 top-level list policy       PASS in YAML — only fails when policy is built
                                             as a top-level list (YAML loader auto-wraps in
                                             dict; documented as crash-or-bypass at API edge)
      agent-1008 PermissionsBoundary ignored PASS (scored 10; boundary suppression deliberately
                                             not honored — over-score is acceptable, missing
                                             FACTOR is the calibration drift)
      agent-1009 Principal.AWS nested dict   PASS (scored 7 via broad-Resource fallback; the
                                             cross-account signal is lost but not score-affecting)
      agent-1010 Condition list-form         PASS (scored 9; vacuity-detector ignores list-
                                             form but per-action floor dominates)

# CONVERGENCE CERTIFICATION: PARTIAL.
#
# max_gap of round-10 fixtures = 4 (vs the corpus expected_min); but the
# TRUE-RISK gap (against the worst-case interpretation of what the policy
# encodes) reaches 8 for agent-1000 and agent-1002 (dict-shaped Action).
#
# Per the certification rubric in the audit prompt:
#   YES if max_gap ≤ 2 across all findings
#   PARTIAL if max_gap = 3-4 on isolated edges
#   NO if max_gap ≥ 5 (still has bypass surface)
#
# On the CORPUS-EXPECTED basis: max_gap = 4 → PARTIAL.
# On the TRUE-RISK basis (worst-case): max_gap = 8 → NO.
#
# The honest answer is PARTIAL: the scorer is converged on every CATEGORY
# of attack pattern (single-statement narrow, narrow-Resource, glob-action,
# wildcard-account-segment, Unicode-homoglyph, vacuous-condition, NotAction/
# NotResource composition, code-execution-via-PassRole composition,
# trust-policy cross-account, resource-policy cross-account-direction)
# BUT a single TYPE-GUARD class — non-string Action / non-string Resource —
# remains a complete bypass at the `_as_list` level.
#
# The defect is ARCHITECTURAL (one rule needs to change — `_as_list` and
# `_norm_grammar_str` to handle dict/int/bool/None gracefully and surface
# a malformed-shape factor) but is NARROW (one helper function, ~10 lines).
# Round-stoppable with a single PR.
#
# Cumulative architectural surface:
#   - gap ≥ 5 (true-risk basis): 2 fixtures (agent-1000, 1002 — dict-shape
#     Action, both same defect class)
#   - gap = 3-4 (true-risk basis): 3 fixtures (agent-1001 missing-Resource,
#     agent-1003 dict-Resource, agent-1004 JSON-stringified Action)
#   - gap = 1-2 (calibration / factor-quality only): 3 fixtures (agent-1005,
#     1008, 1009)
#   - PASS-pin (defensive regression guards): 3 fixtures (agent-1006, 1007,
#     1010)


# TOP 3 ARCHITECTURAL RECOMMENDATIONS (round 10):
#
# NNN. **Type-guard `_as_list`: detect non-string Action/Resource and
#      flag at floor 5+**. The single highest-leverage fix in round 10.
#      Closes agents 1000, 1002, 1003, 1004 (4 fixtures, true-risk gap
#      range 3-8). Specifically:
#
#         def _as_list(value):
#             if value is None: return []
#             if isinstance(value, str): ...
#             if isinstance(value, list): ...
#             # NEW: anything else (dict, int, bool, etc.) is malformed.
#             # Return a sentinel that the caller can detect AND surface
#             # a 'malformed Action/Resource shape' factor at floor 5+.
#             return ["__MALFORMED_SHAPE__"]
#
#      Plus a per-statement check after Action/Resource normalization
#      that fires the malformed-shape factor when the sentinel is
#      present, similar to the existing "malformed Statement entries"
#      flag at line 1860-1875.
#
#      Optional refinement for dict-shaped Action: if the dict's KEYS
#      look like IAM action strings (contain `:` and match the
#      service:Action shape), iterate the keys as the implied action
#      list — closer to what the author likely intended. CFN's
#      `Fn::Sub` / `Ref` / `Fn::Join` dicts should be detected and
#      either resolved heuristically or flagged at floor 7.
#
# OOO. **Missing-Resource = implicit Resource:* for identity-policy
#      statements**. When a statement has Action but no Resource AND
#      no Principal (i.e. it's clearly an identity-policy statement,
#      not a trust-policy), treat the Resource as `*` for the broad-
#      resource rules. Round-6 "malformed = worst-case" principle
#      already applies to missing Effect; this is the symmetric case
#      for missing Resource.
#
#      Detection: `Resource not in stmt` AND `NotResource not in stmt`
#      AND `Principal not in stmt`. The triple-absent rule avoids
#      false-positive on trust policies (which legitimately omit
#      Resource).
#
#      Closes agent-1001 (gap 3 vs corpus, true gap 5).
#
# PPP. **Top-level policy type guard + tuple acceptance**. Two related
#      bugs:
#        1. Top-level `Statement` accepted as `list` OR `dict`, but
#           not `tuple`. Some Python-side callers pass tuples (CDK
#           synth output, certain test harnesses). Replace
#           `isinstance(statements, list)` with `isinstance(statements,
#           (list, tuple))` at line 1772.
#        2. Top-level POLICY assumed to be a dict; raises
#           AttributeError when called with a list. Add an
#           `if not isinstance(policy, dict)` guard at the top of
#           `_deterministic` returning (5, [malformed], []).
#
#      Closes agents 1006, 1007. Pure defensive — the practical
#      bypass requires a specific upstream caller shape, but round-6
#      "malformed = worst-case" principle applies and the unhandled
#      exception is a defect regardless.


# HONEST NEGATIVES — patterns the scorer correctly handles (round 10):
#
# - **Action: ["*", "iam:CreateAccessKey"]** (wildcard alongside
#   specific) scored 10. PASS — the `"*" in actions` early-return at
#   line 2496 short-circuits to score 10 regardless of order.
#
# - **Action: ["iam:CreateAccessKey", "*"]** (reversed) scored 10.
#   PASS — same path, list contains "*".
#
# - **Effect: "Allоw" (Cyrillic single homoglyph)** scored 10. PASS via
#   round-7 _LATIN_HOMOGLYPHS map.
#
# - **Effect: "Аllоw" (multiple Cyrillic chars mixed)** scored 10.
#   PASS — same path, multiple homoglyphs all map.
#
# - **Effect: ["Allow"] (list form)** scored 10. PASS via round-8
#   eff[0] coercion at line 1478-1482.
#
# - **Effect: ["Deny", "Allow"] (list with Deny first)** scored 1.
#   PASS — first-element is Deny, statement skipped. Correct AWS
#   semantics.
#
# - **Effect: 1 (integer)** scored 10. PASS — non-string Effect
#   treated as conservative worst-case per line 1483-1485.
#
# - **Effect: {value: "Allow"} (dict)** scored 10. PASS — same
#   conservative path.
#
# - **Effect: " Allow " with NBSP padding** scored 10. PASS via
#   `_norm_grammar_str` strip.
#
# - **Effect: "ALLOW" (uppercase)** scored 10. PASS via lowercase
#   normalization at line 1486.
#
# - **Effect missing entirely + Action:* + Resource:*** scored 10.
#   PASS via line 1474-1475 (None Effect treated as Allow).
#
# - **Composition: iam:CreateRole (narrow) + iam:PassRole (narrow)**
#   scored 9. PASS — the catastrophic floor on CreateRole fires
#   regardless of resource scope.
#
# - **Composition: iam:CreateRole + iam:AttachRolePolicy (no PassRole)
#   on narrow role/dev-***  scored 9. PASS — CreateRole's catastrophic
#   floor dominates.
#
# - **Composition: ec2:RunInstances narrow + iam:PassRole narrow on
#   admin role** scored 9. PASS — has_code_exec + pass_role_any
#   composition fires regardless of resource scope.
#
# - **Composition: lambda:CreateFunction narrow + iam:PassRole on
#   admin role** scored 9. PASS — same code-exec composition rule.
#
# - **Statement: bare-dict with Sid** scored 10. PASS — round-9 fix
#   distinguishes single-stmt-with-Sid from sid-keyed-object form
#   correctly.
#
# - **Statement: {AdminSid: {admin}}** (single sid-keyed) scored 10.
#   PASS via round-9 agent-800 fix (sid-keyed normalization).
#
# - **Statement: {0: {admin}}** (integer-keyed dict) scored 10.
#   PASS — integer keys pass the sid-keyed detection heuristic.
#
# - **NotAction: [] + Action: harmless** scored 10. PASS via empty-
#   NotAction rule at line 2278-2289.
#
# - **Action + NotAction both present in one statement** scored 9.
#   PASS — NotAction rule fires AND broader-cross-resource rule
#   fires; max wins.
#
# - **Trust: Service:lambda no Condition (no Resource)** scored 6.
#   PASS — round-6 service-principal-no-condition rule at line
#   2043-2057 fires.
#
# - **Condition: StringEqualsIfExists with vacuous wildcard** scored 9.
#   PASS — broad iam:* dominates; the vacuous condition signal is
#   detected separately for narrow-action cases.
#
# - **Action with embedded newline `iam:Pass\nRole`** scored 9. PASS —
#   round-9 _norm_grammar_str Cc-category strip handles newline.
#
# - **Action with embedded tab `iam\t:PassRole`** scored 9. PASS via
#   same Cc-category strip.
#
# - **Resource: "" (empty string) + Action: iam:PassRole** scored 4.
#   THE narrow-PassRole floor fires (4); the missing-Resource gap
#   (agent-1001) is the score-affecting form; empty-string Resource
#   is below the bypass threshold for known-narrow-action floors.
#
# - **Statement: '' / None / 42 / "string" / frozenset** all scored 1
#   with "No statements in policy" factor. PASS — type guard works.
#
# - **iam:CreatePolicyVersion solo on Resource:*** scored 9. PASS via
#   _CATASTROPHIC_ACTIONS_LC catalog.
#
# - **Mixed-validity Action list [None, "iam:PassRole"]** scored 9.
#   PASS — _as_list filters None, the valid entry triggers floor 9.
#
# - **Statement: bare-dict with extra nested-dict ExtraField** scored
#   10. PASS — Statement-normalization treats it as single-dict (the
#   sid-keyed heuristic requires ALL values be dicts with policy
#   keys; extra field doesn't trigger sid-keyed-flatten).


# Convergence verdict — final round (10):
#
# The scorer has CONVERGED on every NAMED ATTACK CLASS from the
# research corpus (10 rounds × ~80 patterns per round = ~800 unique
# patterns enumerated, of which ~750 PASS).
#
# The remaining bypass surface is exclusively MALFORMED-INPUT shape:
#   - Non-string Action/Resource (dict, int, bool, None): 4 fixtures
#     blocking convergence at gap 3-8.
#   - Top-level tuple/list policy types: 2 fixtures, harness-level
#     edge cases.
#   - Calibration-quality factor surfacing (PermissionsBoundary,
#     nested-dict Principal): 3 fixtures, score-correct but factor-
#     incomplete.
#
# All remaining gaps point at TWO functions: `_as_list` (10 lines) and
# the top-of-`_deterministic` Statement normalization (15 lines). A
# single PR adding type guards + malformed-shape factor surfacing
# closes the round-10 surface entirely.
#
# Recommended action: implement NNN (type-guard `_as_list`) as the
# next-round fix. After that, the loop SHOULD reach the gap≤2
# asymptote per round 9's prediction.


## Round 10 — Black-box (convergence test)

Convergence-test round per round-9-BB instructions. Probed the eight
categories explicitly called out in round-9 honest-negatives (Aurora
DSQL, Connect AssociateLambdaFunction, MGN cutover, Network Manager,
Bedrock Guardrails, VPC Lattice authentication, DataSync agent-level,
GreenGrass v2) plus action-string encoding siblings and Principal-form
variants the prior round flagged but didn't write fixtures for.

Total YAMLs: 14 (agent-900 through agent-913).

Result split (relative to expected score_min):

  - gap = 6 (severe, 3 fixtures):
      agent-907 connect:UpdateContactFlow narrow:    1 vs 7
      agent-910 apigateway:UpdateApiKey narrow:      1 vs 7
      agent-912 appstream2:CreateImageBuilder:       1 vs 7

  - gap = 5 (2 fixtures):
      agent-904 leading-colon `:iam:PassRole`:        4 vs 9
      agent-906 HTML-entity `iam&#58;PassRole`:       1 vs 6

  - gap = 4 (7 fixtures):
      agent-900 bare-account-id Principal SNS:        3 vs 7
      agent-901 bare-account-id Principal KMS list:   3 vs 7
      agent-902 bare-account-id LIST Principal SQS:   3 vs 7
      agent-908 bedrock:UpdateGuardrail narrow:       3 vs 7
      agent-909 vpc-lattice:DeleteAuthPolicy narrow:  3 vs 7
      agent-911 glue:UpdateDevEndpoint narrow:        3 vs 7
      agent-913 sagemaker:CreatePresignedDomainUrl:   3 vs 7

  - gap = 3 (2 fixtures):
      agent-903 `iam::PassRole` multi-colon:          6 vs 9
      agent-905 `iam:PassRole:` trailing-colon:       6 vs 9

Total gap units: 62. Average gap: 4.4. Maximum gap: 6.

# TOP 3 worst gaps (severity-ranked):

# 1. agent-907 / 910 / 912 (all gap 6)
#    Single attack class: narrow-resource form of high-impact action
#    drops to score 1 because the verb isn't in any sensitive-services
#    / high-impact catalog despite being a documented code-execution
#    / data-exfil primitive. Three different services, same pattern:
#      - connect:UpdateContactFlow — IVR rewire → PII exfil
#      - apigateway:UpdateApiKey + GetApiKey — key compromise
#      - appstream2:CreateImageBuilder — Windows shell RCE
#    Each is a sensitive-service-action that needs catalog entry.

# 2. agent-906 (gap 5)
#    HTML-entity-encoded colon `iam&#58;PassRole`. Round-9-batch-2
#    closed URL-encoded colon (%3A, %253A) but the sibling HTML-entity
#    form is still wide open. Same evasion-signal class, different
#    decoder pipeline.

# 3. agent-904 (gap 5)
#    Leading-colon `:iam:PassRole`. The split-on-first-colon yields
#    service="" and action="iam:PassRole" — empty service-name shows
#    up in the factor text ("Resource: `*` for `` (broad...)"),
#    confirming the bug is in `_norm_grammar_str` not stripping
#    leading separator characters.

# Breakdown by attack class:

#  - **Bare 12-digit-account-id Principal form** (3 fixtures: 900,
#    901, 902). Round-9 HHH explicitly called this out as a needed
#    extension to the cross-account resource-policy detector but
#    the closure only landed the ARN form. The bare form is bypassed
#    entirely; the LIST-of-bare-ids form (902) scores even lower.

#  - **Action-string encoding/normalization siblings to round-9**
#    (4 fixtures: 903, 904, 905, 906). Multi-colon, leading-colon,
#    trailing-colon, HTML-entity. Round-9-batch-2 closed URL-encoded
#    colon (%3A, %253A); these are the literal-character and
#    HTML-entity decoder siblings. All belong to round-9 III's
#    "non-canonical action format" signal.

#  - **Narrow-resource form of newer-service catastrophic actions**
#    (4 fixtures: 908, 909, 910, 912). Round-9 verified the broad-
#    resource form scores correctly via service-residue floors;
#    the narrow form has no equivalent floor and drops to 1-3.

#  - **Narrow-resource form of established code-execution primitives**
#    (3 fixtures: 907, 911, 913). Same shape but on actions that
#    ARE in the code-exec catalog for the broad-resource case;
#    narrow-resource version is missing.

# Cross-cutting architectural recommendations (continuing letter
# naming RRR-UUU; the WB round-10 used NNN-PPP):

# RRR. **Bare-12-digit-account-id Principal recognition** (highest
#      ROI of this round — closes 3 fixtures: 900, 901, 902).
#      Extend round-9-batch-1's cross-account resource-policy
#      direction detector. When `Principal.AWS` is a string matching
#      regex `^[0-9]{12}$` (12 digits exactly), treat it as
#      semantically equivalent to `arn:aws:iam::<digits>:root`.
#      Compare against the account-id segment of the Resource ARN —
#      if different, surface "cross-account resource-policy grant"
#      and apply the same read/write floors as the ARN form.
#      Also: handle LIST values where every element is either an
#      ARN or a bare 12-digit account id. Mixed lists with at least
#      one foreign account → cross-account.

# SSS. **HTML-entity decode + literal-colon-runs normalization**
#      (closes 4 fixtures: 903, 904, 905, 906). Extend round-9-batch-2's
#      URL-decode pass in `_norm_grammar_str`:
#        - decode HTML entities for the three IAM-grammar characters
#          (colon, asterisk, slash): `&#58;` `&#42;` `&#47;` (decimal),
#          `&#x3A;` etc. (hex), and the named entities `&colon;`
#          `&ast;` `&sol;` if encountered.
#        - collapse runs of `:` to a single `:` (`re.sub(r":+", ":")`).
#        - strip leading `:` from action strings.
#        - strip trailing `:` from action strings (and whitespace).
#      Each transformation that changes the string should set a
#      "non-canonical action format" flag → floor 6 regardless of
#      resolved verb (evasion-signal). After normalization, re-run
#      catalog matching for the canonical verb-stem.

# TTT. **Narrow-resource form of newer-service sensitive actions**
#      (closes 4 fixtures: 908, 909, 910, 912). The round-9
#      service-residue floor only applies on Resource:* / collection-
#      wildcard forms. Extend the following actions to ALSO floor at
#      narrow-resource:
#        - `bedrock:UpdateGuardrail`, `bedrock:DeleteGuardrail`,
#          `bedrock:CreateGuardrailVersion`,
#          `bedrock:UpdateGuardrailVersion` → floor 7 narrow
#        - `vpc-lattice:PutAuthPolicy`, `vpc-lattice:DeleteAuthPolicy`,
#          `vpc-lattice:CreateAuthPolicy` → floor 7 narrow
#        - `apigateway:UpdateApiKey`, `apigateway:GetApiKey`,
#          `apigateway:ImportApiKeys` → floor 7 narrow
#        - `appstream2:CreateImageBuilder`,
#          `appstream2:StartImageBuilder`,
#          `appstream2:UpdateImageBuilder` → floor 7 narrow

# UUU. **Narrow-resource form of code-execution primitives**
#      (closes 3 fixtures: 907, 911, 913). Same shape as TTT but on
#      established code-exec actions whose broad-resource form is
#      already covered:
#        - `connect:UpdateContactFlow`, `connect:CreateContactFlow`,
#          `connect:UpdateUserSecurityProfile` → floor 7 narrow
#        - `glue:UpdateDevEndpoint`, `glue:CreateDevEndpoint` →
#          floor 7 narrow (PublicKey injection → SSH RCE)
#        - `sagemaker:CreatePresignedDomainUrl`,
#          `sagemaker:CreatePresignedNotebookInstanceUrl`,
#          `sagemaker:CreateApp` → floor 7 narrow (presigned URL
#          == credential-equivalent, bypasses MFA/IP-allow-lists)


# Honest negatives — probed but the scorer already handles correctly:
#
# - **dsql:DbConnectAdmin Resource:*** scored 6 (correct above
#   auto-approve threshold; could be 8 but PASS).
# - **dsql:DeleteCluster Resource:*** scored 8 — PASS.
# - **MGN StartCutover narrow** scored 3 — borderline gap 4 (not
#   written because lower-impact than contact-flow / image-builder).
# - **networkmanager:RegisterTransitGateway *** scored 6 — borderline
#   PASS (could be 8 for network-pivot but not catastrophic gap).
# - **datasync:CreateTask broad bundle** scored 6 — borderline PASS.
# - **greengrass:CreateDeployment narrow** scored 3 — borderline gap 4
#   (not written; lower marginal value).
# - **IAM:PassRole / Iam:PassRole (case variants of service)** scored
#   9. PASS — round-3 case-insensitive matching works across the
#   service prefix.
# - **Action: '*' / '**' / '*:*' / 'iam*' / 'iam?PassRole'** all
#   score 10. PASS via existing wildcard-catastrophic floor.
# - **U+200B zero-width-space inside iam:PassRole** scored 9. PASS
#   via round-7 invisible-char strip.
# - **U+FF1A fullwidth-colon `iam：PassRole`** scored 9. PASS — likely
#   via the Cf-strip catching the fullwidth char.
# - **Statement list with Sid in dict** scored 9. PASS via round-9.
# - **Missing Version field** scored 9. PASS.
# - **CanonicalUser Principal cross-account** scored 5. PASS borderline.
# - **Federated SAML cross-account no condition** scored 7. PASS.
# - **trust-policy ExternalId StringLike *** scored 7. PASS — round-9
#   ExternalId vacuity detection works.
# - **trust-policy ExternalId empty string** scored 7. PASS.
# - **trust-policy Principal Service: *** scored 9. PASS via Principal:*.
# - **bare-account-id principal s3:* cross-acct** scored 9 — PASS;
#   the broad s3:* action dominates regardless of Principal-form gap.
# - **inspector2:Disable *** scored 9. PASS.
# - **guardduty:DeleteMembers *** scored 8. PASS.
# - **sso:DeletePermissionSet *** scored 8. PASS.
# - **cognito-idp:AdminSetUserPassword *** scored 9. PASS.
# - **eks:AssociateAccessPolicy *** scored 9. PASS.


# ====================================================================
# CONVERGENCE VERDICT — Round 10 Black-box
# ====================================================================
#
# Verdict: **PARTIAL convergence.**
#
# Max gap this round: **6** (3 fixtures: agent-907 / 910 / 912).
# Round 9 BB closed with reported max_gap = 3 — this round finds 3
# fixtures at gap 6, all in the round-9-BB honest-negatives note
# ("scored 8, PASS borderline" for the BROAD-RESOURCE form). The
# narrow-resource form is the undiscovered surface. This is genuine
# net-new gap, not regression.
#
# The four recommendations (RRR/SSS/TTT/UUU) each close a small
# group of fixtures with a single mechanism — same shape as round-9's
# recommendations. The patterns are isolated edges (not big new
# attack-class discoveries like trust-policy in round 8), which is
# the convergence signal.
#
# Note from the WB peer: WB round 10 found max_gap = 4 on corpus
# basis (gap 8 on true-risk basis) with the malformed-input attack
# class as the remaining surface. The BB round finds max_gap = 6
# on the CATALOG surface (narrow-resource forms of well-known
# sensitive actions). Two orthogonal classes of remaining bypass:
#   - WB: shape/type bugs (`_as_list` doesn't handle dict/int/bool)
#   - BB: catalog gaps (narrow-resource forms + bare-account-id
#     Principal + HTML-entity-encoding)
#
# Both classes are CONVERGENT in shape — each remaining gap is
# closed by one of 4-7 distinct rules, not by an exhaustive
# pattern enumeration. The big-pattern-class discovery phase is
# DONE.
#
# Bottom line: **convergence is PARTIAL.** Same verdict as WB
# round 10. Big-pattern-class discovery is done; ~15-30 fixture/
# round backlog of narrow-resource catalog gaps, Principal-form
# siblings, and encoding-decoder siblings remains. Each closure
# is small (1-4 fixture impact). The recommended path forward is
# (a) one batch implementing RRR+SSS+TTT+UUU + WB's NNN+OOO+PPP,
# then (b) declare converged and accept any remaining isolated
# edges as known-limitation documentation.

