name: aws-managed-ROSAImageRegistryOperatorPolicy
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/service-role/ROSAImageRegistryOperatorPolicy\nAWS\
  \ description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic score\
  \ against future rule refactors. Current det_score=8; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Sid: ListBuckets
    Effect: Allow
    Action:
    - s3:ListBucket
    - s3:ListBucketMultipartUploads
    Resource: '*'
  - Sid: AllowSpecificBucketActions
    Effect: Allow
    Action:
    - s3:CreateBucket
    - s3:DeleteBucket
    - s3:GetBucketTagging
    - s3:GetBucketPublicAccessBlock
    - s3:GetEncryptionConfiguration
    - s3:GetLifecycleConfiguration
    - s3:GetBucketLocation
    - s3:PutBucketPublicAccessBlock
    - s3:PutBucketTagging
    - s3:PutEncryptionConfiguration
    - s3:PutLifecycleConfiguration
    Resource:
    - arn:aws:s3:::*-image-registry-${aws:RequestedRegion}-*
    - arn:aws:s3:::*-image-registry-${aws:RequestedRegion}?
    - arn:aws:s3:::*-image-registry-${aws:RequestedRegion}
  - Sid: AllowSpecificObjectActions
    Effect: Allow
    Action:
    - s3:AbortMultipartUpload
    - s3:DeleteObject
    - s3:GetObject
    - s3:ListMultipartUploadParts
    - s3:PutObject
    Resource:
    - arn:aws:s3:::*-image-registry-${aws:RequestedRegion}-*/*
    - arn:aws:s3:::*-image-registry-${aws:RequestedRegion}?/*
    - arn:aws:s3:::*-image-registry-${aws:RequestedRegion}/*
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 7
  score_max: 9
