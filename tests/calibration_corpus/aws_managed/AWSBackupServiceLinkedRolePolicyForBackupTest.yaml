name: aws-managed-AWSBackupServiceLinkedRolePolicyForBackupTest
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/aws-service-role/AWSBackupServiceLinkedRolePolicyForBackupTest\n\
  AWS description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic\
  \ score against future rule refactors. Current det_score=6; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Action:
    - elasticfilesystem:Backup
    - elasticfilesystem:DescribeTags
    Resource: arn:aws:elasticfilesystem:*:*:file-system/*
    Effect: Allow
    Condition:
      StringLike:
        aws:ResourceTag/aws:elasticfilesystem:default-backup: enabled
  - Action:
    - tag:GetResources
    Resource: '*'
    Effect: Allow
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 5
  score_max: 7
