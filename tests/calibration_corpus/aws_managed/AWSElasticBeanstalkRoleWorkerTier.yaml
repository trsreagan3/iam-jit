name: aws-managed-AWSElasticBeanstalkRoleWorkerTier
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/service-role/AWSElasticBeanstalkRoleWorkerTier\nAWS\
  \ description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic score\
  \ against future rule refactors. Current det_score=3; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Sid: AllowSQS
    Effect: Allow
    Action:
    - sqs:TagQueue
    - sqs:DeleteQueue
    - sqs:GetQueueAttributes
    - sqs:CreateQueue
    Resource: arn:aws:sqs:*:*:awseb-e-*
  - Sid: AllowDDB
    Effect: Allow
    Action:
    - dynamodb:CreateTable
    - dynamodb:TagResource
    - dynamodb:DescribeTable
    - dynamodb:DeleteTable
    Resource: arn:aws:dynamodb:*:*:table/awseb-e-*
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 2
  score_max: 4
