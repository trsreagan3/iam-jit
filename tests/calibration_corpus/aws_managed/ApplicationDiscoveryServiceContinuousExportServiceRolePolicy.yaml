name: aws-managed-ApplicationDiscoveryServiceContinuousExportServiceRolePolicy
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/aws-service-role/ApplicationDiscoveryServiceContinuousExportServiceRolePolicy\n\
  AWS description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic\
  \ score against future rule refactors. Current det_score=7; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Action:
    - glue:CreateDatabase
    - glue:UpdateDatabase
    - glue:CreateTable
    - glue:UpdateTable
    - firehose:CreateDeliveryStream
    - firehose:DescribeDeliveryStream
    - logs:CreateLogGroup
    Effect: Allow
    Resource: '*'
  - Action:
    - firehose:DeleteDeliveryStream
    - firehose:PutRecord
    - firehose:PutRecordBatch
    - firehose:UpdateDestination
    Effect: Allow
    Resource: arn:aws:firehose:*:*:deliverystream/aws-application-discovery-service*
  - Action:
    - s3:CreateBucket
    - s3:ListBucket
    - s3:PutBucketLogging
    - s3:PutEncryptionConfiguration
    Effect: Allow
    Resource: arn:aws:s3:::aws-application-discovery-service*
  - Action:
    - s3:GetObject
    Effect: Allow
    Resource: arn:aws:s3:::aws-application-discovery-service*/*
  - Action:
    - logs:CreateLogStream
    - logs:PutRetentionPolicy
    Effect: Allow
    Resource: arn:aws:logs:*:*:log-group:/aws/application-discovery-service/firehose*
  - Action:
    - iam:PassRole
    Effect: Allow
    Resource: arn:aws:iam::*:role/AWSApplicationDiscoveryServiceFirehose
    Condition:
      StringLike:
        iam:PassedToService: firehose.amazonaws.com
  - Action:
    - iam:PassRole
    Effect: Allow
    Resource: arn:aws:iam::*:role/service-role/AWSApplicationDiscoveryServiceFirehose
    Condition:
      StringLike:
        iam:PassedToService: firehose.amazonaws.com
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 6
  score_max: 8
