name: aws-managed-AWSQuickSetupSSMDeploymentS3BucketRolePolicy
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/AWSQuickSetupSSMDeploymentS3BucketRolePolicy\nAWS\
  \ description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic score\
  \ against future rule refactors. Current det_score=9; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Effect: Allow
    Action:
    - s3:CreateBucket
    - s3:DeleteBucket
    - s3:DeleteBucketPolicy
    - s3:PutBucketPublicAccessBlock
    - s3:ListBucket
    - s3:PutBucketPolicy
    - s3:PutEncryptionConfiguration
    - s3:PutBucketTagging
    - s3:PutLifecycleConfiguration
    - s3:PutBucketVersioning
    Condition:
      ForAnyValue:StringEquals:
        aws:CalledVia: cloudformation.amazonaws.com
      StringEquals:
        aws:ResourceAccount: ${aws:PrincipalAccount}
    Resource: arn:aws:s3:::do-not-delete-ssm-*
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 8
  score_max: 10
