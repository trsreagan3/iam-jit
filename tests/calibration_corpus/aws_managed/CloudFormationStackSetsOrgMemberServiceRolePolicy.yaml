name: aws-managed-CloudFormationStackSetsOrgMemberServiceRolePolicy
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/aws-service-role/CloudFormationStackSetsOrgMemberServiceRolePolicy\n\
  AWS description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic\
  \ score against future rule refactors. Current det_score=9; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Action:
    - iam:CreateRole
    - iam:DeleteRole
    - iam:GetRole
    Effect: Allow
    Resource:
    - arn:aws:iam::*:role/stacksets-exec-*
  - Action:
    - iam:DetachRolePolicy
    - iam:AttachRolePolicy
    Effect: Allow
    Resource:
    - arn:aws:iam::*:role/stacksets-exec-*
    Condition:
      StringEquals:
        iam:PolicyARN: arn:aws:iam::aws:policy/AdministratorAccess
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 8
  score_max: 10
