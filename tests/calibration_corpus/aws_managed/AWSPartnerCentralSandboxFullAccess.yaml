name: aws-managed-AWSPartnerCentralSandboxFullAccess
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/AWSPartnerCentralSandboxFullAccess\nAWS description:\
  \ (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic score against\
  \ future rule refactors. Current det_score=9; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Sid: AWSPartnerCentralSandboxAccess
    Effect: Allow
    Action:
    - partnercentral:*
    Resource: '*'
    Condition:
      StringEquals:
        partnercentral:Catalog: Sandbox
  - Sid: PartnerCentralAgentsSandboxSessionAccess
    Effect: Allow
    Action:
    - partnercentral:UseSession
    Resource: '*'
    Condition:
      StringEquals:
        partnercentral:Catalog: Sandbox
      Bool:
        aws:IsMcpServiceAction: 'true'
  - Sid: PassAWSPartnerCentralSnapshotJobRole
    Effect: Allow
    Action:
    - iam:PassRole
    Resource:
    - arn:aws:iam::*:role/*
    Condition:
      StringEquals:
        iam:PassedToService: resource-snapshot-job.partnercentral-selling.amazonaws.com
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 8
  score_max: 10
