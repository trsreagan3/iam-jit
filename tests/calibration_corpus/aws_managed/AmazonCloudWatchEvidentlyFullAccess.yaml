name: aws-managed-AmazonCloudWatchEvidentlyFullAccess
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/AmazonCloudWatchEvidentlyFullAccess\nAWS description:\
  \ (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic score against\
  \ future rule refactors. Current det_score=8; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Effect: Allow
    Action:
    - evidently:*
    Resource: '*'
  - Effect: Allow
    Action:
    - iam:ListRoles
    Resource: '*'
  - Effect: Allow
    Action:
    - iam:GetRole
    Resource:
    - arn:aws:iam::*:role/service-role/CloudWatchRUMEvidentlyRole-*
  - Effect: Allow
    Action:
    - s3:GetBucketLocation
    - s3:ListAllMyBuckets
    Resource: arn:aws:s3:::*
  - Effect: Allow
    Action:
    - cloudwatch:GetMetricData
    - cloudwatch:GetMetricStatistics
    - cloudwatch:DescribeAlarmHistory
    - cloudwatch:DescribeAlarmsForMetric
    - cloudwatch:ListTagsForResource
    Resource: '*'
  - Effect: Allow
    Action:
    - cloudwatch:DescribeAlarms
    - cloudwatch:TagResource
    - cloudwatch:UnTagResource
    Resource:
    - arn:aws:cloudwatch:*:*:alarm:*
  - Effect: Allow
    Action:
    - cloudtrail:LookupEvents
    Resource: '*'
  - Effect: Allow
    Action:
    - cloudwatch:PutMetricAlarm
    Resource:
    - arn:aws:cloudwatch:*:*:alarm:Evidently-Alarm-*
  - Effect: Allow
    Action:
    - sns:ListTopics
    Resource:
    - '*'
  - Effect: Allow
    Action:
    - sns:CreateTopic
    - sns:Subscribe
    - sns:ListSubscriptionsByTopic
    Resource:
    - arn:*:sns:*:*:Evidently-*
  - Effect: Allow
    Action:
    - logs:DescribeLogGroups
    Resource:
    - '*'
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 7
  score_max: 9
