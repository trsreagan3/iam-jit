name: aws-managed-EC2InstanceProfileForImageBuilderECRContainerBuilds
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/EC2InstanceProfileForImageBuilderECRContainerBuilds\n\
  AWS description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic\
  \ score against future rule refactors. Current det_score=7; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Effect: Allow
    Action:
    - imagebuilder:GetComponent
    - imagebuilder:GetContainerRecipe
    - ecr:GetAuthorizationToken
    - ecr:BatchGetImage
    - ecr:InitiateLayerUpload
    - ecr:UploadLayerPart
    - ecr:CompleteLayerUpload
    - ecr:BatchCheckLayerAvailability
    - ecr:GetDownloadUrlForLayer
    - ecr:PutImage
    Resource: '*'
  - Effect: Allow
    Action:
    - kms:Decrypt
    Resource: '*'
    Condition:
      ForAnyValue:StringEquals:
        kms:EncryptionContextKeys: aws:imagebuilder:arn
        aws:CalledVia:
        - imagebuilder.amazonaws.com
  - Effect: Allow
    Action:
    - s3:GetObject
    Resource: arn:aws:s3:::ec2imagebuilder*
  - Effect: Allow
    Action:
    - logs:CreateLogStream
    - logs:CreateLogGroup
    - logs:PutLogEvents
    Resource: arn:aws:logs:*:*:log-group:/aws/imagebuilder/*
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 6
  score_max: 8
