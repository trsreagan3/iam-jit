name: aws-managed-AWSSystemsManagerChangeManagementServicePolicy
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/aws-service-role/AWSSystemsManagerChangeManagementServicePolicy\n\
  AWS description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic\
  \ score against future rule refactors. Current det_score=9; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Effect: Allow
    Action:
    - ssm:CreateAssociation
    - ssm:DeleteAssociation
    - ssm:CreateOpsItem
    - ssm:GetOpsItem
    - ssm:UpdateOpsItem
    - ssm:StartAutomationExecution
    - ssm:StopAutomationExecution
    - ssm:GetAutomationExecution
    - ssm:GetCalendarState
    - ssm:GetDocument
    Resource:
    - '*'
  - Effect: Allow
    Action:
    - cloudwatch:DescribeAlarms
    Resource:
    - '*'
  - Effect: Allow
    Action:
    - sso:ListDirectoryAssociations
    Resource:
    - '*'
  - Effect: Allow
    Action:
    - sso-directory:DescribeUsers
    - sso-directory:IsMemberInGroup
    Resource:
    - '*'
  - Effect: Allow
    Action: iam:GetGroup
    Resource: '*'
  - Effect: Allow
    Action: iam:PassRole
    Resource: '*'
    Condition:
      StringEquals:
        iam:PassedToService:
        - ssm.amazonaws.com
  - Sid: AllowKmsAccessViaIdentityCenter
    Effect: Allow
    Action:
    - kms:Decrypt
    Resource: '*'
    Condition:
      ArnLike:
        kms:EncryptionContext:aws:sso:instance-arn: arn:*:sso:::instance/*
      StringLike:
        kms:ViaService: sso.*.amazonaws.com
  - Sid: AllowKmsAccessViaIdentityStore
    Effect: Allow
    Action:
    - kms:Decrypt
    Resource: '*'
    Condition:
      ArnLike:
        kms:EncryptionContext:aws:identitystore:identitystore-arn: arn:*:identitystore::*:identitystore/*
      StringLike:
        kms:ViaService: identitystore.*.amazonaws.com
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 8
  score_max: 10
