name: aws-managed-AWSApplicationAutoscalingWorkSpacesPoolPolicy
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/aws-service-role/AWSApplicationAutoscalingWorkSpacesPoolPolicy\n\
  AWS description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic\
  \ score against future rule refactors. Current det_score=8; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Sid: WorkSpacesActionsOnAllPools
    Effect: Allow
    Action:
    - workspaces:DescribeWorkspacesPools
    - workspaces:UpdateWorkspacesPool
    Resource:
    - '*'
    Condition:
      StringEquals:
        aws:ResourceAccount: ${aws:PrincipalAccount}
  - Sid: CloudWatchActionsOnAllAlarms
    Effect: Allow
    Action:
    - cloudwatch:DescribeAlarms
    Resource:
    - arn:aws:cloudwatch:*:*:alarm:*
    Condition:
      StringEquals:
        aws:ResourceAccount: ${aws:PrincipalAccount}
  - Sid: CloudWatchActionsOnTargetTrackingAlarms
    Effect: Allow
    Action:
    - cloudwatch:PutMetricAlarm
    - cloudwatch:DeleteAlarms
    Resource:
    - arn:aws:cloudwatch:*:*:alarm:TargetTracking*
    Condition:
      StringEquals:
        aws:ResourceAccount: ${aws:PrincipalAccount}
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 7
  score_max: 9
