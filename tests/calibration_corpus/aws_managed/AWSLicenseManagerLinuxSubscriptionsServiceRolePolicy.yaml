name: aws-managed-AWSLicenseManagerLinuxSubscriptionsServiceRolePolicy
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/aws-service-role/AWSLicenseManagerLinuxSubscriptionsServiceRolePolicy\n\
  AWS description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic\
  \ score against future rule refactors. Current det_score=7; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Sid: EC2Permissions
    Effect: Allow
    Action:
    - ec2:DescribeInstances
    - ec2:DescribeRegions
    Resource:
    - '*'
  - Sid: OrganizationPermissions
    Effect: Allow
    Action:
    - organizations:DescribeOrganization
    - organizations:ListAccounts
    - organizations:DescribeAccount
    - organizations:ListChildren
    - organizations:ListParents
    - organizations:ListAccountsForParent
    - organizations:ListRoots
    - organizations:ListAWSServiceAccessForOrganization
    - organizations:ListDelegatedAdministrators
    Resource:
    - '*'
  - Sid: SecretsManagerPermissions
    Effect: Allow
    Action:
    - secretsmanager:GetSecretValue
    Condition:
      StringEquals:
        aws:ResourceTag/LicenseManagerLinuxSubscriptions: enabled
        aws:ResourceAccount: ${aws:PrincipalAccount}
    Resource:
    - arn:aws:secretsmanager:*:*:secret:*
  - Sid: KMSPermissions
    Effect: Allow
    Action:
    - kms:Decrypt
    Condition:
      StringEquals:
        aws:ResourceTag/LicenseManagerLinuxSubscriptions: enabled
        aws:ResourceAccount: ${aws:PrincipalAccount}
      StringLike:
        kms:ViaService:
        - secretsmanager.*.amazonaws.com
    Resource:
    - arn:aws:kms:*:*:key/*
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 6
  score_max: 8
