name: aws-managed-AmazonEC2ContainerServiceEventsRole
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/service-role/AmazonEC2ContainerServiceEventsRole\n\
  AWS description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic\
  \ score against future rule refactors. Current det_score=9; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Effect: Allow
    Action:
    - ecs:RunTask
    Resource:
    - '*'
  - Effect: Allow
    Action: iam:PassRole
    Resource:
    - '*'
    Condition:
      StringLike:
        iam:PassedToService: ecs-tasks.amazonaws.com
  - Effect: Allow
    Action: ecs:TagResource
    Resource: '*'
    Condition:
      StringEquals:
        ecs:CreateAction:
        - RunTask
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 8
  score_max: 10
