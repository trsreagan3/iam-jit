name: aws-managed-SageMakerStudioBedrockKnowledgeBaseCustomResourcePolicy
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/service-role/SageMakerStudioBedrockKnowledgeBaseCustomResourcePolicy\n\
  AWS description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic\
  \ score against future rule refactors. Current det_score=8; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Sid: OpenSearchServerlessPermissions
    Effect: Allow
    Action: aoss:APIAccessAll
    Resource: arn:aws:aoss:*:*:collection/*
    Condition:
      StringEquals:
        aws:ResourceAccount: ${aws:PrincipalAccount}
      StringLike:
        aoss:collection: bedrock*
  - Sid: BedrockKnowledgeBasePermissions
    Effect: Allow
    Action:
    - bedrock:GetIngestionJob
    - bedrock:ListIngestionJobs
    - bedrock:StartIngestionJob
    Resource: arn:aws:bedrock:*:*:knowledge-base/*
    Condition:
      StringEquals:
        aws:ResourceAccount: ${aws:PrincipalAccount}
        aws:ResourceTag/AmazonDataZoneProject: ${aws:PrincipalTag/AmazonDataZoneProject}
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 7
  score_max: 9
