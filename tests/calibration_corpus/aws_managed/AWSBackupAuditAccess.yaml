name: aws-managed-AWSBackupAuditAccess
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/AWSBackupAuditAccess\nAWS description: (none)\nGenerated\
  \ by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic score against future rule refactors.\
  \ Current det_score=8; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Effect: Allow
    Action:
    - backup:CreateFramework
    - backup:UpdateFramework
    - backup:ListFrameworks
    - backup:DescribeFramework
    - backup:DeleteFramework
    - backup:ListBackupPlans
    - backup:ListBackupVaults
    - backup:CreateReportPlan
    - backup:UpdateReportPlan
    - backup:ListReportPlans
    - backup:DescribeReportPlan
    - backup:DeleteReportPlan
    - backup:StartReportJob
    - backup:ListReportJobs
    - backup:DescribeReportJob
    Resource: '*'
  - Effect: Allow
    Action:
    - config:DescribeConfigurationRecorders
    - config:DescribeConfigurationRecorderStatus
    - config:DescribeComplianceByConfigRule
    Resource: '*'
  - Effect: Allow
    Action:
    - config:GetComplianceDetailsByConfigRule
    Resource: arn:aws:config:*:*:config-rule/*
  - Effect: Allow
    Action:
    - s3:ListAllMyBuckets
    - s3:GetBucketLocation
    Resource: arn:aws:s3:::*
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 7
  score_max: 9
