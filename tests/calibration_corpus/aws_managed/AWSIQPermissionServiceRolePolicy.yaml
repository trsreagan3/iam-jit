name: aws-managed-AWSIQPermissionServiceRolePolicy
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/aws-service-role/AWSIQPermissionServiceRolePolicy\n\
  AWS description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic\
  \ score against future rule refactors. Current det_score=9; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Effect: Allow
    Action:
    - iam:DeleteRole
    - iam:ListAttachedRolePolicies
    Resource: arn:aws:iam::*:role/AWSIQPermission-*
  - Effect: Allow
    Action:
    - iam:AttachRolePolicy
    Resource: arn:aws:iam::*:role/AWSIQPermission-*
    Condition:
      ArnEquals:
        iam:PolicyARN: arn:aws:iam::aws:policy/AWSDenyAll
  - Effect: Allow
    Action:
    - iam:DetachRolePolicy
    Resource: arn:aws:iam::*:role/AWSIQPermission-*
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 8
  score_max: 10
