name: aws-managed-AWSMigrationHubStrategyConsoleFullAccess
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/AWSMigrationHubStrategyConsoleFullAccess\nAWS description:\
  \ (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic score against\
  \ future rule refactors. Current det_score=8; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Effect: Allow
    Action:
    - migrationhub-strategy:*
    Resource: '*'
  - Effect: Allow
    Action:
    - s3:ListAllMyBuckets
    Resource: arn:aws:s3:::*
  - Effect: Allow
    Action:
    - s3:GetObject
    - s3:CreateBucket
    - s3:PutEncryptionConfiguration
    - s3:PutBucketPublicAccessBlock
    - s3:PutBucketPolicy
    - s3:PutBucketVersioning
    - s3:PutLifecycleConfiguration
    Resource: arn:aws:s3:::migrationhub-strategy-*
  - Effect: Allow
    Action:
    - secretsmanager:ListSecrets
    Resource: '*'
  - Effect: Allow
    Action:
    - discovery:GetDiscoverySummary
    - discovery:DescribeTags
    - discovery:DescribeConfigurations
    - discovery:ListConfigurations
    Resource: '*'
  - Effect: Allow
    Action:
    - iam:CreateServiceLinkedRole
    Resource: '*'
    Condition:
      StringEquals:
        iam:AWSServiceName: migrationhub-strategy.amazonaws.com
  - Effect: Allow
    Action:
    - iam:GetRole
    Resource: arn:aws:iam::*:role/aws-service-role/migrationhub-strategy.amazonaws.com/AWSMigrationHubStrategyServiceRolePolicy*
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 7
  score_max: 9
