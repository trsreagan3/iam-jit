name: aws-managed-AmazonApplicationRecoveryControllerRegionSwitchPlanExecutionPolicy
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/AmazonApplicationRecoveryControllerRegionSwitchPlanExecutionPolicy\n\
  AWS description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic\
  \ score against future rule refactors. Current det_score=4; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Effect: Allow
    Action:
    - arc-region-switch:GetPlan
    - arc-region-switch:GetPlanExecution
    - arc-region-switch:ListPlanExecutions
    Resource: '*'
    Sid: GetPlanAndExecutions
  - Effect: Allow
    Action: iam:SimulatePrincipalPolicy
    Resource: arn:aws:iam::*:role/*
    Sid: PlanEvaluation
  - Effect: Allow
    Action:
    - cloudwatch:DescribeAlarms
    - cloudwatch:DescribeAlarmHistory
    - cloudwatch:GetMetricStatistics
    Resource: '*'
    Sid: CloudWatch
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 3
  score_max: 5
