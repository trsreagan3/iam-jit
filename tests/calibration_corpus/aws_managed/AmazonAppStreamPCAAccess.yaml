name: aws-managed-AmazonAppStreamPCAAccess
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/service-role/AmazonAppStreamPCAAccess\nAWS description:\
  \ (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic score against\
  \ future rule refactors. Current det_score=9; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Effect: Allow
    Action:
    - acm-pca:IssueCertificate
    - acm-pca:GetCertificate
    - acm-pca:DescribeCertificateAuthority
    Resource: arn:*:acm-pca:*:*:*
    Condition:
      StringLike:
        aws:ResourceTag/euc-private-ca: '*'
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 8
  score_max: 10
