name: aws-managed-AWSPartnerCentralSellingResourceSnapshotJobExecutionRolePolicy
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/AWSPartnerCentralSellingResourceSnapshotJobExecutionRolePolicy\n\
  AWS description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic\
  \ score against future rule refactors. Current det_score=1; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Effect: Allow
    Action:
    - partnercentral:CreateResourceSnapshot
    Resource:
    - arn:aws:partnercentral:*::catalog/AWS/engagement/*
    - arn:aws:partnercentral:*::catalog/Sandbox/engagement/*
  - Effect: Allow
    Action:
    - partnercentral:GetOpportunity
    Resource:
    - arn:aws:partnercentral:*:*:catalog/AWS/opportunity/*
    - arn:aws:partnercentral:*:*:catalog/Sandbox/opportunity/*
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 1
  score_max: 2
