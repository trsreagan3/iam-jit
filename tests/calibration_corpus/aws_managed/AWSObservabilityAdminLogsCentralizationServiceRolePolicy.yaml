name: aws-managed-AWSObservabilityAdminLogsCentralizationServiceRolePolicy
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/aws-service-role/AWSObservabilityAdminLogsCentralizationServiceRolePolicy\n\
  AWS description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic\
  \ score against future rule refactors. Current det_score=7; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Effect: Allow
    Action:
    - logs:CreateLogGroup
    - logs:CreateLogStream
    - logs:PutLogEvents
    Resource: '*'
  - Effect: Allow
    Action:
    - kms:Encrypt
    - kms:Decrypt
    - kms:GenerateDataKey
    Condition:
      ForAnyValue:StringEquals:
        aws:CalledVia:
        - logs.amazonaws.com
      StringEquals:
        aws:ResourceTag/LogsManaged: 'true'
      ArnLike:
        kms:EncryptionContext:aws:logs:arn: arn:aws:logs:*:*:log-group:*
    Resource: '*'
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 6
  score_max: 8
