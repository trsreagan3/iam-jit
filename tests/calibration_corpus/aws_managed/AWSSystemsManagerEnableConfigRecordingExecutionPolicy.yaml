name: aws-managed-AWSSystemsManagerEnableConfigRecordingExecutionPolicy
description: "AWS-managed policy snapshot. ARN: arn:aws:iam::aws:policy/AWSSystemsManagerEnableConfigRecordingExecutionPolicy\n\
  AWS description: (none)\nGenerated by scripts/fetch-aws-managed-policies.py \u2014 regression-protects the deterministic\
  \ score against future rule refactors. Current det_score=9; tolerance \xB11 reflects small calibration deltas."
policy:
  Version: '2012-10-17'
  Statement:
  - Sid: S3BucketCreatePermissions
    Effect: Allow
    Action:
    - s3:CreateBucket
    - s3:PutBucketPublicAccessBlock
    - s3:ListBucket
    - s3:PutBucketPolicy
    - s3:PutEncryptionConfiguration
    Resource:
    - arn:aws:s3:::aws-quick-setup-config-recording-*
    Condition:
      StringEquals:
        aws:ResourceAccount: ${aws:PrincipalAccount}
  - Sid: SNSTopicsListPermissions
    Effect: Allow
    Action:
    - sns:ListTopics
    Resource: '*'
  - Sid: DefaultSNSTopicCreatePermissions
    Effect: Allow
    Action:
    - sns:CreateTopic
    Resource: arn:aws:sns:*:*:ConfigRecording-Default-Topic
  - Sid: ConfigureAndStartConfigurationRecorderPermissions
    Effect: Allow
    Action:
    - config:DescribeConfigurationRecorders
    - config:DescribeDeliveryChannels
    - config:PutConfigurationRecorder
    - config:PutDeliveryChannel
    - config:StartConfigurationRecorder
    Resource: '*'
  - Sid: GetAndPassConfigSLRPermissions
    Effect: Allow
    Action:
    - iam:GetRole
    - iam:PassRole
    Resource:
    - arn:aws:iam::*:role/aws-service-role/config.amazonaws.com/AWSServiceRoleForConfig
    - arn:aws:iam::*:role/AWSServiceRoleForConfig
  - Sid: CreateConfigSLRPermissions
    Effect: Allow
    Action:
    - iam:CreateServiceLinkedRole
    Resource:
    - arn:aws:iam::*:role/aws-service-role/config.amazonaws.com/AWSServiceRoleForConfig
    Condition:
      StringEquals:
        iam:AWSServiceName: config.amazonaws.com
request:
  spec:
    access_type: read-write
    duration:
      duration_hours: 1
expected:
  score_min: 8
  score_max: 10
